# КОНТЕКСТ ДЛЯ ПРОДОЛЖЕНИЯ РАБОТЫ
# Система отбора фотографий для выпускных альбомов → платформа для школьных фотографов
# Версия: v191
# Обновлено: 18.06.2026 (редактор обложек — рендер по группировке, кроп, общее фото)
#
# Предыдущий контекст: yearbook-context-v190.md (история не переписывается).

---

## Контекст задачи

ТЗ tz-cover-editor. Редактор всех обложек заказа разом, с двумя глубинами правок
(шаблонные на тип + поштучный кроп портрета), автоподстановкой общего фото.
Учительская — ОТЛОЖЕНА (решение Сергея, нет модели «какой мастер учительский»).

Ключевая находка разведки: cover.data использует ТОТ ЖЕ формат служебных ключей,
что разворот (__scale__/__offset__/__rotate__/__color__/…), → холст переиспользуем.

## ✅ DONE (ветка feat/cover-editor, НЕ в main)

**Миграция (нужно применить):** `2026-06-18-cover-edits.sql` — таблица
`cover_edits` (album_id, cover_type|null, child_id|null, data jsonb) + 2 частичных
уникальных индекса. Дубль в schema.sql.

1. **Автоподстановка** (lib/cover/load-covers.ts): shared.common_photo_url +
   back_common_photo_url = ПОСЛЕДНЕЕ `photos.type='common_full'` (по created_at).
   Работает в сводке и превью/редакторе.
2. **Хранение правок** lib/cover/editor-merge.ts (mergeCoverEditsInto/indexCoverEdits/
   hiddenOverridesFromData, тест) + lib/cover/load-editor.ts (loadCoverEditor:
   все обложки + геометрия мастеров + слитые правки + база + галерея common_full).
   API /api/tenant: GET `cover_editor`, POST `cover_save_edit` (scope='type'|'student',
   ручной upsert под частичные индексы).
3. **CoverCanvas** (app/app/_components/CoverCanvas.tsx) — тонкая обёртка над
   AlbumSpreadCanvas: layoutCover раскладывает зоны (плавающий корешок из
   диапазонов типографии), декор/фон/кроп/тексты наследуются. template/instance
   приводятся к spread-форме (cast), pageSide='spread', spineMarginMm=null.
4. **Блок «Редактор обложек»** (CoverEditorBlock.tsx) во вкладке «Обзор» под
   сводкой: грид всех обложек (SVG-миниатюры), клик → модалка с холстом.
   Правки: **кроп фото** (портрет per-student у портретных → scope student;
   общее фото → scope type), **текст-контент** (inline-редактор холста),
   **замена общего фото** кликом по миниатюре common_full. Дебаунс-сохранение.

## ⏸️ ОТЛОЖЕНО (следующий инкремент)

- **Стили текста / шрифт / цвет / размер / выравнивание** и **смена фона** и
  **скрытие слотов** — нужна проводка host-панели TextStylePanel (в редакторе
  разворотов она host-managed, не через пропсы холста). Сейчас в модалке только
  контент текста + кроп + общее фото.
- **Учительская обложка** — нет модели (отложена Сергеем).
- Замена общего фото — пока клик по миниатюре (не drag из палитры); достаточно.

## Проверки

- tsc чисто, vitest 1035 passed (editor-merge тесты), next build зелёный.
- ⚠️ Вживую (в браузере) НЕ проверено — особенно рендер Konva-холста обложки
  (3 зоны через cast SpreadTemplate, кроп-координаты, декор). Проверять на preview.
- Страницы родителей и редактор разворотов не тронуты.

## 🚩 Следующий шаг

- Применить миграцию cover_edits; прогон на preview: открыть редактор, кропнуть
  портрет у одного ученика (проверить, что у него сохранилось, у других нет),
  поменять текст, заменить общее фото. Затем merge в main.
- Потом: стили/шрифты/фон (TextStylePanel в модалке), учительская обложка,
  экспорт обложек в печать.
