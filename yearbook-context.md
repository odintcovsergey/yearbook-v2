# КОНТЕКСТ ДЛЯ ПРОДОЛЖЕНИЯ РАБОТЫ
# Система отбора фотографий для выпускных альбомов

---

## КТО Я И ЧТО МЫ ДЕЛАЕМ

Я — фотограф/организатор выпускных альбомов. У меня штат фотографов и я работаю
с внешними фотографами (ретушь + вёрстка + печать). Мы делаем веб-систему для
отбора фотографий родителями вместо текущего неудобного процесса через Google Диск.

---

## ТИП АЛЬБОМА — «УНИВЕРСАЛ»

Структура альбома:
1. Обложка (портрет или общее фото класса или надпись)
2. Раздел учителей (ФИО, должность, портрет)
3. Личные страницы каждого ученика (1 страница на ученика)
4. Общий раздел (выбираем сами, вне системы)

Личная страница ученика содержит:
- 1 портрет (выбирает родитель из 5-10 вариантов)
- ФИ ученика
- Текст до 500 символов (пишет родитель)
- 2 фото с друзьями (выбирает родитель из пула 70-120 фото)

Правило групповых фото: 1 фото = 1 ученик (эксклюзивный выбор).
Планируется вариант где одно фото могут выбрать несколько учеников — на будущее.

---

## СЦЕНАРИЙ — 6 ШАГОВ ДЛЯ РОДИТЕЛЯ

Шаг 1 — Контакты (имя родителя + телефон для допродаж)
Шаг 2 — Портрет для личной страницы (выбрать 1 из пула портретов)
Шаг 3 — Портрет для обложки (4 варианта):
  - cover_mode = 'none'     → шаг пропускается
  - cover_mode = 'same'     → автоматически тот же что на странице
  - cover_mode = 'optional' → родитель выбирает: без портрета (бесплатно)
                              или с портретом (тот же или другой, +300р)
  - cover_mode = 'required' → все платят доплату
Шаг 4 — Текст от ученика (до 500 символов, необязательно)
Шаг 5 — 2 фото с друзьями (эксклюзивная блокировка)
Шаг 6 — Подтверждение (после этого изменить нельзя)

Отдельный сценарий: Ответственный родитель заполняет данные учителей
(ФИО, должность, портрет для каждого учителя).

---

## СТЕК

- Next.js 14 (App Router) + TypeScript
- Tailwind CSS
- Supabase (PostgreSQL + Storage)
- Vercel (деплой, ещё не сделан)
- Шрифты: Unbounded + Geologica (Google Fonts)

---

## SUPABASE ПРОЕКТ

- Project URL: https://bnotiyhamfyllcrqwquq.supabase.co
- Проект называется: yearbook-photos
- Organization: yearbook
- Region: West Europe (London)
- Storage bucket: photos (PUBLIC)
- Схема БД: уже создана и работает

Таблицы:
- albums — альбомы
- children — ученики (с access_token для уникальных ссылок)
- teachers — учителя
- responsible_parents — ответственный родитель (заполняет учителей)
- photos — фотографии (type: portrait/group/teacher)
- photo_children — привязка фото к ученикам (разметка)
- photo_teachers — привязка фото к учителям
- selections — выборы родителей
- parent_contacts — контакты родителей (имя + телефон)
- student_texts — тексты от учеников
- cover_selections — выбор обложки + доплата
- photo_locks — временная блокировка фото в процессе выбора

---

## СТРУКТУРА ПРОЕКТА

Папка: ~/Desktop/yearbook-v2

app/
  [token]/page.tsx       — интерфейс родителя (6 шагов) ✓
  admin/page.tsx         — панель администратора ✓
  api/
    child/route.ts       — загрузка данных по токену ✓
    select/route.ts      — блокировка фото + сохранение выбора ✓
    teacher/route.ts     — данные учителей ✓
    admin/route.ts       — весь API администратора ✓
lib/supabase.ts          — подключение к Supabase ✓
types/index.ts           — TypeScript типы ✓
schema.sql               — схема БД ✓

Файл .env.local содержит:
NEXT_PUBLIC_SUPABASE_URL=https://bnotiyhamfyllcrqwquq.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=legacy anon ключ
SUPABASE_SERVICE_ROLE_KEY=legacy service_role ключ
ADMIN_SECRET=пароль администратора

ВАЖНО: использовать Legacy API keys из Supabase (не новые sb_publishable_/sb_secret_)
Найти их: Supabase → Settings → API Keys → вкладка "Legacy anon, service_role API keys"

---

## ЧТО УЖЕ РАБОТАЕТ ✓

1. Админка /admin — создание альбомов, добавление учеников, загрузка фото,
   импорт CSV разметки, просмотр статистики, доплаты, контакты, экспорт CSV
2. Интерфейс родителя — все 6 шагов работают
3. Выбор портрета для страницы
4. Выбор обложки с доплатой (режим optional)
5. Текст от ученика
6. Выбор 2 групповых фото с эксклюзивной блокировкой
7. Финальное подтверждение
8. Запись в базу данных всех данных
9. Экспорт CSV для вёрстки
10. База контактов родителей

---

## ЧТО НЕ СДЕЛАНО / НУЖНО ДОДЕЛАТЬ

### Срочно (баги):
1. КЕШ Next.js — при изменении данных в БД нужно удалять .next папку
   и перезапускать сервер. Решение: добавить export const dynamic = 'force-dynamic'
   во все API routes (уже добавлено в child/route.ts, нужно в остальные)
2. Автосохранение черновика — данные сбрасываются при обновлении страницы
   до финального подтверждения. Нужно сохранять промежуточное состояние

### Важные функции:
3. Страница учителей (/teacher/[token]) — код написан в api но
   фронтенд страница app/teacher/[token]/page.tsx НЕ СОЗДАНА
4. Оптимизация фото — файлы по 5-7 МБ, нужны превьюшки.
   Решение: при загрузке создавать уменьшенную копию (thumbnail)
   через Sharp или сжимать на клиенте перед загрузкой
5. Деплой на Vercel — сайт доступен только локально

### На будущее:
6. Вариант где одно групповое фото могут выбрать несколько учеников
   (параметр max_selections на уровне фото)
7. Массовая рассылка ссылок (выгрузка всех ссылок одним CSV)
8. Скрипт генерации CSV разметки из папок с фото
9. Интеграция с автовёрсткой InDesign

---

## ИЗВЕСТНЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ

**Проблема:** API возвращает старые данные после изменений в БД
**Решение:** rm -rf .next && npm run dev (удалить кеш и перезапустить)
Постоянное решение: добавить во все файлы app/api/*/route.ts строки:
  export const dynamic = 'force-dynamic'
  export const revalidate = 0

**Проблема:** Supabase новые ключи (sb_publishable_, sb_secret_) не работают
с @supabase/supabase-js v2.43.0
**Решение:** Использовать Legacy ключи (eyJ... формат)

**Проблема:** truncate/delete в SQL Editor иногда не применяется сразу
**Решение:** Удалять по конкретным ID или по album_id

**Проблема:** Терминал открывается в домашней папке
**Решение:** Всегда начинать с: cd ~/Desktop/yearbook-v2

---

## КАК ЗАПУСТИТЬ ЛОКАЛЬНО

```bash
cd ~/Desktop/yearbook-v2 && npm run dev
```
Сайт: http://localhost:3000
Админка: http://localhost:3000/admin
Страница родителя: http://localhost:3000/[access_token]

Если данные не обновляются после изменений в БД:
```bash
cd ~/Desktop/yearbook-v2 && rm -rf .next && npm run dev
```

---

## СЛЕДУЮЩИЙ ШАГ

Добавить export const dynamic = 'force-dynamic' во все API routes
чтобы решить проблему с кешем раз и навсегда. Затем деплой на Vercel.
