# PROJECT_CONTEXT — текущее состояние yearbook-v2

> Снимок на **26.06.2026**. Источники: свежий `yearbook-context-v199.md` + `MEMORY.md` +
> `CLAUDE.md`. Обновляется в конце каждой сессии Claude Code.

> **✅ ЖИЗНЕННЫЙ ЦИКЛ АРХИВА — ПОЛНОСТЬЮ В ПРОДЕ (26.06.2026, релиз `6d68e8b`).**
> Архивный заказ через 90 дней автоматически освобождает место — удаляются ТОЛЬКО
> исходники фотографа (`photos.original_path`), превью и записи `photos` остаются →
> архив смотрибелен. (1) **Проба Фазы 5 пройдена** на «Школа 81» (269 собственных
> оригиналов, 1.448 ГБ): `--apply` → удалено 269 = **обнулено 269** (фикс чанк-обнуления
> по 50 подтверждён — был баг пробы 25.06 с 71), ошибок 0, висячих `original_path` 0,
> превью 269 целы, `originals_deleted_at` + `audit_log` проставлены, повторный dry-run=0
> (идемпотентно). (2) **UI-тексты сделаны честными** (раньше врали про мгновенное удаление
> при архивации): вкладка «Фото» архивного заказа показывает статус из единой
> `archiveLifecycleStatus` (countdown/not_started/deleted/forever); предупреждение при
> архивации — про «через 90 дней оригиналы удалятся, превью останутся» (снята ложная фраза
> про родительские ссылки — `/api/child` на `archived` не смотрит). Оба прокликаны Сергеем.
> (3) **Таймер `yearbook-cleanup.timer` УСТАНОВЛЕН + ВКЛЮЧЁН** (`/etc/systemd/system/`,
> `enable --now`): ежедневно 04:00 UTC, `Persistent=true`, ExecStart `--apply` (боевое
> удаление), холостой проход чистый (0/0/0), первый реальный запуск 27.06 04:00 UTC.
> Заморозить = `systemctl disable --now yearbook-cleanup.timer`. УРОК: `--ttl-days=0` НЕ
> делает заказ истёкшим без `archived_at` (отсекается раньше TTL); копия заказа НЕ дублирует
> файлы в хранилище (шарит ключи источника, own=0). Детали — `docs/tz-archive-lifecycle.md`,
> `knowledge/sessions/2026-06-26.md`.

> **🔴 ГРАБЛЯ ДЕПЛОЯ — GitHub SSL-флап (26.06.2026).** Авто-деплой завис ~15 мин: `git fetch`
> к GitHub с РФ-сервера то падал, то висел весь SSL-таймаут → сервис болтался в `activating`
> (это ЗАВИСШИЙ ФЕТЧ, а не сборка: `ExecMainStartTimestamp` пустой, нет процесса `next build`,
> нет нового `releases/<sha>`). ОБХОД (отработал): доставить коммит **git-бандлом с Mac** минуя
> сеть → подменить `origin` на бандл → штатный `deploy.sh` (build+health+авто-откат) → вернуть
> `origin` на GitHub → перезапустить таймер. Подробно — `knowledge/sessions/2026-06-26.md`.

> **✅ АУДИТ КОДА (мусор/дубли/неэффективность) — ПРОВЕДЁН 25.06.2026.** Двухэтапно: отчёт
> `docs/audit-report.md` → точечная уборка атомарными коммитами. Сделано: N+1 в /api/select
> (`02a684e`); категория A (импорты, 4 мёртвых экспорта `4d6866b`, 2 orphan `944d582`,
> 3 зависимости `ce5f462`); скрытый БАГ `effectiveHasQuote` — quote-согласованность combined-хвоста
> РЭ.37.9 после РЭ.40 (`e6e7857` + `docs/fix-quote-tail-consistency.md`, тест в обе стороны);
> категория B разобрана (из 10 «мёртвых» реально мёртв 1 — `tenant_detail` удалён `f7244a9`;
> остальное живое/заделы с TODO `1cd98a5`/`5cc6fd0`; врущий коммент `register_photo` исправлен
> `4fad550`). Тесты 1178/1178. ОТЛОЖЕНО (отдельные сессии): `tag_photo`/`untag_photo` (родительский
> флоу), категория C (низкий приоритет), A5 (дубль PT_PER_MM/POINTS_PER_MM). Детали —
> `knowledge/sessions/2026-06-25.md`.

> **✅ СМЕНА ДИЗАЙНА В РЕДАКТОРЕ — V1 ЭТАПЫ 1–3 В ПРОДЕ (25.06.2026, main `cffabf3`).**
> Кнопка «Сменить дизайн» в редакторе альбома. Подход — **remap (per-spread), НЕ rebuild**
> → ручные правки (тексты, кадрирование) сохраняются; распределение не пересчитываем; обложки
> не трогаем. Модуль `lib/design-switch/`: Этап 1 coverCheck (проверка совместимости + отказы
> «нужно 5 / максимум 4», `d0f21d5`), Этап 2 remap (перенос контент+кропы+`__hidden__`, стили/
> `__pos__` из нового дизайна, `d6175f4`), Этап 3 UI (модалка + действия `design_switch_check`/
> `design_switch_apply` в /api/layout, двойная защита, `cffabf3`). В проде деструктивного пути
> НЕТ: совместимых пар дизайнов нет («Белый» отказывает по 5-местному коллажу, текущий заблокирован).
> ОСТАЛОСЬ: выровнять дизайны (мастера «Акварели» — задание дизайнеру; «Белый»/J-Collage-5 — бэклог),
> первая боевая запись на «Тест 24» по явному «ок», Этап 4 (фоны/пере-подпись/края). ⚠️ Чистота
> переноса зависит от полноты наборов мастеров (нет близнеца → ближайший → раскладка меняется; не
> баг remap). Детали — `docs/tz-design-switch.md`, `knowledge/sessions/2026-06-25.md`.

> **✅ ФОНЫ ОБЛОЖЕК В ПРЕВЬЮ ГРУЗЯТСЯ В SAFARI (25.06.2026, main `4cee510`).**
> После переезда на Timeweb в миниатюрах обложек («Обзор») не грузились фоны (синие «?»).
> Причина многослойная: (1) `covers.background_url` хранит старый западный supabase-URL
> (из РФ без VPN заблокирован) → сервер конвертит в Timeweb при чтении; (2) сырой ключ
> `cover_portrait` в правках перекрывал подписанную базу → «синий ?» на портретах (это
> был баг ПОКАЗА, фото не терялись — «сброс обложки Петровой» не нужен); (3) Safari
> нестабильно грузит кросс-origin картинки в инлайн-SVG (CORS/кэш). **Финальное решение —
> same-origin прокси `app/api/img/route.ts`:** все `<image>` превью заворачиваются в
> `/api/img?u=<подписанный-url>` → картинки идут с нашего домена → для браузера это
> «свои» → ни CORS, ни кэш-граблей. Прокси: только image/*, белый список хостов (анти-SSRF),
> пересылает уже подписанную ссылку. Холст редактора (Konva) грузит напрямую — не трогали.
> Прокликано Сергеем в Safari. УРОК: cross-origin картинки в инлайн-SVG → сразу проксировать
> через свой домен. Хвост (просил не чинить): кнопка «Превью обложки»-глазик в настройках
> пустая. Детали — `knowledge/sessions/2026-06-25.md`, память `project_cover_bg_timeweb_debug`.

> **✅ ФОНОВАЯ ОЧЕРЕДЬ ЭКСПОРТА (ТЗ №2) В ПРОДЕ + ФИКС «СЕРЫХ ФОТО» (24.06.2026, main `b544287`).**
> Большие альбомы (>30 разворотов) экспортируются фоновым воркером (systemd
> `yearbook-render-worker`, дочерний процесс на задачу), малые — синхронно как раньше.
> Таблица `export_jobs`, ядро `lib/export-run/` (вынос рендера из роута), `lib/export-queue/`,
> статус в кабинете (ExportPanel: готовится→скачать, опрос, повтор). Заодно вылечен блокер:
> `storageKeyFromUrl` не срезал бакет для Timeweb path-style (`s3.twcstorage.ru`) → экспорт
> не находил оригиналы (`no_original`) и падал на протухающий selection-webp (серые фото).
> Теперь оба пути берут оригиналы — проверено на проде (`no_original=0`, фото из оригиналов
> глазами). Остаток: обложки (логотип/фон/овал-круг) отдельным ТЗ; Telegram-уведомления —
> точка заложена (заглушка). Детали — `knowledge/sessions/2026-06-24.md`.

> **✅ ПЕРЕЕЗД ЗАКРЕПЛЁН В MAIN + CI/CD АВТО-ДЕПЛОЙ РАБОТАЕТ (23.06.2026).**
> Боевой `app.okeybook.ru` на Timeweb (VDS, IP `5.129.221.197`, Ubuntu 24.04). Ветка
> `feat/migrate-storage-timeweb` влита в `main` (fast-forward). Деплой — **pull-модель**:
> сервер по таймеру (2 мин) опрашивает GitHub `main`, собирает релиз офлайн (общий
> `shared/node_modules` — реестр npm с РФ-сервера недоступен), переключает симлинк
> `current`, рестартит, health-check (`/login`+`/api/health`) с авто-откатом. Раскладка
> `/srv/yearbook/{repo,shared,releases,current}`; старый `/srv/yearbook-v2` оставлен как
> откат. Артефакты — `deploy/timeweb/`. **Как выкатить: `git push` в main.** Подробности —
> `knowledge/sessions/2026-06-23.md`. Следующее ТЗ — фоновая очередь экспорта PDF.

> **✅ НОЧНОЙ OFF-SITE БЭКАП БД — НА VDS (23.06.2026).** GitHub Actions до РФ-базы Timeweb
> НЕ доходит (timeout 5432 с заграничного раннера) → ночной дамп перенесён на сам сервер:
> `yearbook-backup.timer` (ежедневно 03:00 UTC), `pg_dump` боевой базы → off-site в приватный
> `github.com/odintcovsergey/yearbook-backups` по SSH deploy-key (храним 30 дней). Это 2-й слой
> поверх встроенных бэкапов Timeweb. `pg_dump 18` ставился через зеркало Яндекса. Старый
> `backup.yml` (Supabase) — расписание отключено, только ручной запуск. Пароль `gen_user` сменён
> (засветился при отладке). Детали — `knowledge/sessions/2026-06-23.md`, память `project_offsite_db_backup`.

> **⚡ ПЕРЕЕЗД НА TIMEWEB ИДЁТ по шагам (ветка `feat/migrate-storage-timeweb`, НЕ в main, прод не тронут).**
> ✅ **Шаг 1 — БД (20.06):** Timeweb Managed PostgreSQL 18.4 (Москва, SSL), полная копия Supabase
> `public` — 52 таблицы / 14060 строк, 0 расхождений. ✅ **Шаг 2 — хранилище (21.06):** все файлы
> скопированы КОПИЕЙ в один приватный бакет Timeweb S3 — фото Yandex (5631/13.3ГБ, 0 расхождений)
> + 3 бакета Supabase под префиксы (97 объектов); итог 5728 объектов/13.348ГБ; приватность (403/200)
> проверена. Код хранилища за флагом `STORAGE_BACKEND` (`supabase`=как сейчас).
> ✅ **Шаг 3 (21.06, коммиты `054e005`, `5ebeeca`):** живой канвас редактора+обложек И фото детей
> на Timeweb за флагом `STORAGE_BACKEND`. Прокликано Сергеем; из живой проверки починены: CORS
> бакета (загрузка фона), показ залитого фона обложки (`read_url`), опознание фото по стабильному
> ключу `storage_path` (бейджи/swap/контекст-меню). tsc/1113/build зелёные.
> ⚡ **Шаг 4 — РЕАЛИЗАЦИЯ ИДЁТ (21.06, коммиты `651e1ef`,`4528582`,`23467ca`,`3c20a61`):** выбран
> **Вариант 1 — self-host PostgREST** (приложение остаётся на `supabase.from()`, меняется
> endpoint+роль БД). **Сервер Timeweb поднят** (VDS, IP `5.42.103.168`, Ubuntu 24.04, root по
> SSH-ключу): установлены **Node 20.20.2 + nginx 1.24 + PostgREST v12.2.3** (та же major, что у
> Supabase). PostgREST заведён под systemd на `127.0.0.1:3001` против КОПИИ базы Timeweb
> (как `gen_user`, SSL verify-ca). **Сверка A4 на БОЕВЫХ данных** (Supabase vs новый PostgREST
> через SSH-туннель, 33 запроса = 9 синтетических + 24 реальных из кода): **30/33 PASS; 3 FAIL —
> ТОЛЬКО дрейф данных** (`albums.archived` true/false: альбом заархивирован в проде после снимка
> 20.06), структура/статусы совпадают → PostgREST ведёт себя идентично. Грабли: с РФ-сервера
> CDN релизов GitHub недоступен → бинарники качаем на Мак и шлём `scp`.
> ✅ **Шаг 4 / часть B ВЫПОЛНЕНА (21.06, коммит `cbddb2f`): стенд `https://app.okeybook.ru` ЖИВ**
> (читает КОПИЮ базы Timeweb; прод не тронут; вход в кабинет проверен Сергеем). Приложение в
> `/srv/yearbook-v2` (юзер `yearbook`), под systemd (`127.0.0.1:3000`), `.env.production` (тот же
> `JWT_SECRET`, `STORAGE_BACKEND=timeweb`, домен впечён). nginx: 80→301 https, 443 ssl,
> `/rest/v1`→PostgREST, остальное→приложение. HTTPS — сертификат Let's Encrypt через **DNS-01
> reg.ru** (acme.sh + dns_regru, автопродление), т.к. **HTTP-01 из РФ не работает** (LE не
> достукивается до РФ-IP). 🛑 Две пойманные засады: +2 ГБ swap (без него build OOM); **в nginx
> срезан `Authorization`** — supabase-js шлёт Bearer, PostgREST без jwt-secret отвечал 500
> PGRST300 и ломал ВЕСЬ доступ к БД (логин не находил юзера).
> 🔴 **БЛОКЕР CUTOVER:** из-за среза Authorization путь `/rest/v1` сейчас открыт наружу как
> привилегир. роль — закрыть боевыми ролями least-privilege (`scripts/step4-postgrest-roles.sql`)
> + настоящими JWT перед боем.
> 🔜 **Осталось до cutover:** (1) боевые роли + JWT (см. блокер выше); (2) cutover: финальная
> дельта-синхронизация БД+storage (rclone) → снять дрейф → переключить домен/ENV пользователей →
> smoke → откат наготове. Подробно — `sessions/2026-06-21.md`, память [[project_migration_to_ru]].
> Шаблоны/грабли: `deploy/timeweb/` (README обновлён).

> **🛑 Экспорт ОСТАНОВЛЕН до переезда (20.06.2026).** Ветка
> `feat/export-acceleration-naming` (НЕ в main): типографская выгрузка целиком
> написана и зелёная по тестам, но бесплатный Vercel (Hobby) рубит функцию на
> 60с — реальный альбом синхронно не собрать (РИСК №1). Решено: НЕ дорабатывать
> под Vercel; переезжаем на **Timeweb** и делаем экспорт **фоновой очередью**
> там. Прод (main) не трогаем, старый «Экспортировать» работает. Ветку не вливаем,
> новых задач по экспорту до переезда нет. Подробно — `sessions/2026-06-20.md`,
> память [[project_export_typography]], [[project_migration_to_ru]].

## Что это за продукт

Платформа для школьных фотографов: от сбора фото у родителей (вход по уникальной
ссылке) → автовёрстка альбома из учеников/учителей/общих разделов → редактор
разворотов и обложки → экспорт в PDF. Плюс кабинет партнёра, супер-админка,
обложки, типографии, реферальные программы, «Идеи».

## Стек

- **Frontend/Server:** Next.js 14.2 (App Router), React 18, TypeScript 5
- **БД/Storage:** Supabase (Postgres + Storage). Миграции применяет Сергей вручную
  через Supabase Studio → SQL Editor.
- **Канвас-редактор:** Konva 10 + react-konva
- **PDF-экспорт:** pdf-lib + @pdf-lib/fontkit + sharp
- **IDML-парсинг:** fast-xml-parser (preserveOrder)
- **Хранилище оригиналов/PDF:** Yandex Cloud S3 (@aws-sdk/client-s3 + presigner), приватный бакет + signed URL
- **Стили:** Tailwind 3 · **Тесты:** Vitest 4 (~1091 passed на 19.06)
- **Деплой:** Vercel (auto-deploy на push в `main`), тариф БЕСПЛАТНЫЙ (Hobby)
- **Git:** двойное зеркало — GitHub (origin) + GitFlic, push идёт в оба разом

## Ключевые подсистемы (точки входа)

| Зачем | Где |
|---|---|
| Сборка альбома (вход) | `lib/smart-fill/build-album-input.ts` |
| Движок секций | `lib/rule-engine/` (`build-from-section-structure.ts`, `sections/*`) |
| Парсер IDML | `lib/idml-converter/*` |
| Канвас редактора | `app/app/_components/AlbumSpreadCanvas.tsx` (Konva) |
| Редактор разворотов | `app/app/album/[id]/layout/page.tsx` |
| Редактор обложки | `app/app/album/[id]/cover/page.tsx` (CoverCanvas) |
| PDF pipeline | `lib/pdf-export/pipeline.ts` |
| Типографии/корешок | `lib/printers/*` (spine.ts, types.ts) |
| Адаптация под формат | `lib/format-adapt/` |
| Нарезка на per-student книги | `lib/album-split/` |
| API tenant (CRUD) | `app/api/tenant/route.ts` |
| API layout/экспорт | `app/api/layout/route.ts` |

## Что готово и в проде (свежее → старее)

- **Адаптация макета под формат типографии** (19.06): uniform-масштаб дизайна под
  формат заказа в превью и редакторе разворотов И обложки; семейство дизайна
  `format_family`. Корешок обложки физический. (merge e37040a)
- **Персональный раздел** (19.06): признак `is_personal` у students-секции, движок
  метит личные страницы, модель нарезки `lib/album-split` (книги 000/00X, нумерация,
  корешок собранной книги). Видимо в проде — галочка; нарезка/корешок к экранам пока
  не подключены. (merge cea310f)
- **Профиль типографии** (19.06): форматы блока, режим приёма, формат файла, загибы
  обложки, 3 режима корешка (ranges/formula/fixed), выбор типография→формат→тип листа
  в заказе. (merge 241fc40)
- **Обложки** (17–19.06): объединённая система, редактор обложек, фон + ручное скрытие
  элементов, корешок в превью, сводка в Обзоре.
- **Развязка шаблон↔дизайн**, **два равных воспитателя**, **конструктор личного
  раздела**, **вход как партнёр (impersonation)**, **редизайн (мятная ДНК)**,
  **тёмная тема**, **списочный дашборд**, **приватные фото детей** (signed URL,
  закрытый бакет), **идеи/голосование**, **категорийные фоны**, **привязанный
  декор + текст-эффекты**, **аудит безопасности** (все 🔴/🟠 закрыты).

## Известные ограничения / технический долг

- **Экспорт PDF синхронный** — главный риск. Падает по таймауту Vercel (300с) уже на
  ~27 разворотах (рендер ~5–6 с/страницу, фото грузятся последовательно). Большие
  альбомы (80+ разворотов) не собираются. Лечение: ускорить рендер (параллельная
  загрузка фото) и/или фоновая очередь рендера на Yandex Cloud. См. `DECISIONS.md`
  и `docs/tz-load-readiness.md`.
- **Экспорт не учитывает формат/формат файла типографии** (масштаб в файлы, JPG
  постранично) — отдельное «ТЗ экспорта», ещё не сделано.
- Серверный popup «собранная обложка» (cover_album_preview) не адаптирован под формат.
- Нет staging/Sentry; тестовая среда = preview-ссылки Vercel (план — dev-ветка + тестовая БД к приходу реальных партнёров).

## Жизненный цикл архива (код фаз 1-4 доехал на сервер 25.06 вечером; Фаза 5 Шаг 1 пройден)

Архивный заказ через 90 дней автоматически освобождает место — удаляются ТОЛЬКО
исходники фотографа (`photos.original_path`), превью и записи `photos` остаются.
Анти-шаринг (~13% файлов делят клоны): оригинал удаляем, только если на него не
ссылается ни один не-истёкший заказ. Заодно чинит баг (archive/delete били в
Supabase Storage вместо Timeweb `yc:`-ключей). Код: `lib/archive-cleanup/`,
`scripts/archive-cleanup.mts` (DRY-RUN по умолчанию, `--apply` для реального),
`deploy/timeweb/yearbook-cleanup.{service,timer}` (**таймер выключен, на сервер не
ставился**). UI отсчёта в модалке заказа.

**ВАЖНО:** код фаз 1-4 фактически доехал на боевой сервер только 25.06 вечером —
сервер отставал на 14 коммитов (релиз `944d582`), деплой был застрял (смена deps
аудитом + GitHub-флап). Расклинили обновлением `shared/package-lock.json` → релиз
`8abddaa`, затем фикс бага → `30b584e`. (🔴 при refresh node_modules НЕ rsync с Mac —
затрёт linux-бинарники сервера; см. лог сессии.)

**Фаза 5 (боевое включение, необратимо) — в процессе, по «ок» на шаг:**
- ✅ Шаг 1 dry-run на проде — 0 заказов (защита бэкфилла подтверждена).
- ✅ Проба `--apply` на тестовом Тест 24 ПОЙМАЛА БАГ обнуления (`.in()` пачкой 200 →
  лимит URL → молчаливый сбой → 200 висячих `original_path`). Анти-шаринг отработал.
- ✅ Баг ПОЧИНЕН (`30b584e`, в проде): пачки→50, проверка error, сверка кол-ва
  обнулённых, +тесты. Тест 24 прибран (200 висячих обнулены).
- 🚩 ОСТАЛОСЬ: повторная проба на НОВОМ тестовом заказе (Вариант A, ≥130 фото) →
  полный прогон → enable таймер. ТЗ — `docs/tz-archive-lifecycle.md`.

## Большие отложенные задачи

1. **ТЗ экспорта** (рендер файлов под формат, имена/папки, нарезка per-student в
   файлы, таймаут больших альбомов / фоновая очередь).
2. **Готовность к нагрузке** (`docs/tz-load-readiness.md`): очередь render_jobs на
   чистом Postgres + воркер на YC.
3. **Переезд на российские серверы** (Vercel+Supabase → **Timeweb**), триггеры
   152-ФЗ/школы/масштаб. ⚡ ветка `feat/migrate-storage-timeweb` (НЕ в main).
   ✅ **CUTOVER ВЫПОЛНЕН 22.06.2026**: `app.okeybook.ru` боевой на Timeweb (БД-
   переснимок 52 табл./0 расхождений, хранилище в синхроне, `/rest/v1` закрыт
   jwt-secret, пароль gen_user сменён, Vercel уведён). 🟡 БЛОКЕР доступности: старый
   IP `5.42.103.168` режется ТСПУ (РФ-DPI, подтвердил Timeweb) → сменили на новый
   доп.IP `5.129.221.197` (с телефона без VPN открывается). A-запись переключена,
   **ЖДЁМ распространения DNS**. След.шаг: `dig app.okeybook.ru` → ждём новый IP, затем
   телефон по домену без VPN. Vercel+Supabase — резерв, не гасить; пользователей пока
   не звать. Детали — память `project_migration_to_ru` + `knowledge/sessions/2026-06-22.md`.
4. **AI-помощник для партнёров** — отдельный большой проект.
