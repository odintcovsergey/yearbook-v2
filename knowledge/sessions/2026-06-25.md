# Сессия 2026-06-25 — фоны обложек в превью после переезда на Timeweb (Safari)

## Итог
Фоны (и портреты/декор) в миниатюрах обложек во вкладке «Обзор» **грузятся** —
проверено Сергеем в Safari без VPN. Причина оказалась многослойной, чинили по
слоям; финал — **same-origin прокси картинок превью** `/api/img`. Всё в проде
(main `4cee510`), оба зеркала.

## Хронология причин (почему «?» имел разные корни на разных экранах)
1. **Старый западный адрес фона.** `covers.background_url` хранит supabase-URL
   (`bnotiyhamfyllcrqwquq.supabase.co`) — из РФ без VPN заблокирован. Отсюда
   «фон только с VPN». Сервер теперь конвертит его в российский Timeweb при чтении.
2. **Сырой ключ в правках.** Портрет «синий ?» у учеников с поштучной правкой:
   `cover_edits` хранят `cover_portrait` КЛЮЧОМ; клиентский патч перекрывал
   подписанную базу сырым ключом → `<image href="ключ">` → 404. **Фотографии НЕ
   терялись — баг показа** (поэтому «сброс обложки Петровой» не понадобился).
3. **Safari + инлайн-SVG.** Картинки из чужого origin (хранилище) Safari в
   `<image>` грузил нестабильно: без CORS-заголовка не рисует, а Timeweb отдаёт
   `Access-Control-Allow-Origin` ТОЛЬКО на запрос с `Origin` (supabase слал всегда
   → раньше с supabase-URL было видно). `crossorigin="anonymous"` не помог —
   переводил запрос в CORS-режим и конфликтовал с no-cors кэшем холста (фон один
   раз загружен холстом без CORS → Safari отдавал тот кэш на cors-запрос из SVG →
   CORS-ошибка). Ни с `crossorigin`, ни без него стабильно не выходило.

## Коммиты (по слоям, все в проде)
- `7fd758a` — `lib/cover/resign-bg.ts`: пере-подпись фона из любой формы (ключ /
  старый supabase-URL / протухший presigned → свежий `resolveReadUrl`);
  `signCoverBg` проверяет карту подписей ПЕРВОЙ. Починило фон в редакторе (Konva)
  и превью разворотов.
- `1719dae` — анти-кэш (`cache:'no-store'`+`&_t=`) на запрос `cover_album_preview`.
- `76e4576` — `crossorigin`+`xlink:href` (ОТКАЧЕНО ниже, crossorigin ломал).
- `cd0da81` — `load-editor.ts`: подпись фото-меток в правках (`byType/byChild`
  через `resignCoverPhotoData`). Фикс «синий ?» портретов. Сохранение заново
  keyify-ит (`cover_save_edit`) → база остаётся на ключах.
- `0853f1c` — убрал `crossorigin` (он и ломал фон).
- `4cee510` ⭐ **ФИНАЛ** — `app/api/img/route.ts` (same-origin прокси) +
  `lib/cover/preview-svg.ts` заворачивает ВСЕ `<image>` в `/api/img?u=<подпись>`.
  Картинки превью идут с нашего домена → для Safari same-origin → ни CORS, ни
  кэш-граблей. Прокси: только `image/*`, белый список хостов (анти-SSRF),
  пересылает уже подписанную ссылку (новых прав не даёт). Холст редактора (Konva)
  грузит напрямую — не трогали.

## Урок на будущее
Картинки из ЧУЖОГО origin внутри инлайн-SVG в Safari — **сразу проксировать через
свой домен**, не возиться с `crossorigin`/`xlink`. Это снимает весь класс проблем
(CORS + кэш) разом. Портрет/QR/декор в одном SVG грузились, а фон нет — это и
вывело на кэш-конфликт, а не «CORS вообще».

## Что осталось (Сергей просил НЕ чинить)
- Кнопка «Превью обложки» (глазик) в настройках альбома (`app/app/page.tsx`
  ~4330, модалка ~3848) — у Сергея пустая. Тот же путь данных
  (`cover_album_preview` → тот же `p.svg`), что и заработавшая лента Обзора →
  вероятно, доедет после полного refresh. Назначение кнопки Сергею неясно,
  оставили мелким хвостом.

## Приём диагностики на проде (зафиксировано)
SSH root `5.129.221.197` (флапает — повторять короткими командами). Релизы
`/srv/yearbook/releases/<sha>`, симлинк `current`, юзер `yearbook`, env
`.env.production`, `node_modules/.bin/tsx`, `STORAGE_BACKEND=timeweb`. Приём: scp
tsx-скрипт в `current/`, `chown yearbook`, `sudo -u yearbook env PATH=$PATH
node_modules/.bin/tsx x.mts`, env грузить вручную из `.env.production`, в конце
`rm`, AWS-warning'и грепать. Альбом «Тест 24» id=`b3994ae3-...` (поле названия
`title`). public-файлы на корне НЕ отдаются — роут `app/[token]` перехватывает.

---

# Смена дизайна в редакторе — V1, Этапы 1–3 в проде (25.06.2026)

Новая фича: кнопка «Сменить дизайн» в редакторе альбома. Подход — **remap
(per-spread), НЕ rebuild** → ручные правки (тексты, кадрирование) сохраняются.
ТЗ и прогресс — `docs/tz-design-switch.md`. Модуль — `lib/design-switch/`.

## Что переносим / что из нового дизайна
- Переносим: контент `data[<label>]` (тексты, фото), кропы
  `__scale__/__offset__/__rotate__`, `__hidden__` (скрытие пустого слота — про
  контент, не геометрию; иначе пустые слоты вылезают после смены).
- Из нового дизайна: стили текста (`__fontSize__/__color__/__font__/__halign__/
  __valign__`), рамки, фоны, расположение слотов. `__pos__` — роняем (привязан к
  координатам старого мастера). Распределение НЕ пересчитываем. Обложки не трогаем.

## Коммиты
- **Этап 1 — coverCheck** (`d0f21d5`): `lib/design-switch/compatibility.ts`
  (`findReplacementMaster` по page_role+slot_capacity — переиспользуется remap'ом;
  `checkCoverage`) + `cover-check.ts` (загрузчик). Вариант B — по ТЕКУЩИМ
  разворотам (`album_layouts.spreads`), не пере-прогоном движка. Тексты отказа
  правило «нужное N + максимум M»: «…страница-коллаж из 5 фотографий, а в этом
  дизайне максимум 4 фотографии в коллаже…». `photos_friend` — мягкая ёмкость.
- **Этап 2 — remap** (`d6175f4`): `remapData` (template-replace) расширен
  аддитивным параметром `contentKeyPrefixes` (дефолт = прежнее поведение).
  `lib/design-switch/remap.ts` (ядро `remapAlbumSpreads`, кроп-онли+`__hidden__`)
  + `remap-album.ts` (загрузчик; `write:false`=DRY-RUN; `write:true` пишет
  `album_layouts.spreads`+`template_set_id` и `albums.template_set_id`;
  `has_user_edits` не трогает; фото КАК ЕСТЬ — пере-подпись отдельный долг).
- **Этап 3 — UI** (`cffabf3`): `/api/layout` действия `design_switch_check`
  (read-only) + `design_switch_apply` (пишет, guard как у save_album_layout,
  внутри повторный coverCheck = двойная защита). `DesignSwitchModal.tsx` + кнопка
  в шапке редактора (текущий дизайн помечен «Текущий» и заблокирован; перед apply
  досылаются несохранённые правки; после — reload). Прокликано локально.

## Проверки на проде (read-only / dry-run, БЕЗ записи)
- coverCheck «Тест 24» (дизайн «Акварельные мечты», 8 разв.): текущий → OK;
  «Белый» → отказ по 5-местному коллажу (у «Белого» только J-Collage-4) —
  **настоящий отказ**.
- apply DRY-RUN: «Белый» → ok=false/written=false (guard работает); сам в себя →
  8/8 remapped, lost:0, без записи.

## ОСТАЛОСЬ / зависимости
- На проде всего **2 дизайна** (Акварельные 24 мастера / Белый 37), совместимых
  пар НЕТ → happy-path между РАЗНЫМИ раскладками вживую не проверен.
- ⚠️ **Чистота переноса зависит от ПОЛНОТЫ наборов мастеров.** Нет точного
  близнеца типа → `findReplacementMaster` берёт ближайший достаточный → раскладка
  меняется (пример учителей: «Акварель» только на 6, «Белый» 9/12/16 + неточный
  тег F-Head-SmallGrid). Это не баг remap, лечится доработкой наборов.
- **Блокеры выравнивания дизайнов:** доработка мастеров «Акварели» (задание
  дизайнеру готово) + фикс «Белого»/**J-Collage-5** (бэклог).
- **Первая боевая запись** (design_switch_apply write) — на «Тест 24» по явному
  «ок» Сергея, после выравнивания. **Этап 4** (фоны нового дизайна в превью/
  экспорте, пере-подпись фото, граничные случаи) — делать на реальном кросс-
  дизайн переносе. В проде деструктивного пути нет (совместимых пар нет).

---

# Сессия: Аудит кода (Этапы 1 + 2), 25.06.2026

Двухэтапный аудит «мусор/дубли/неэффективность»: сначала отчёт, потом точечная уборка по подтверждению. Принцип — «данные раньше гипотез», «не натягивать мёртвое».

## Этап 1 — отчёт (read-only)
Инструменты: `tsc --noEmit` (чисто), `tsc --noUnusedLocals/Parameters`, `knip`, `depcheck` + ручной греп. Отчёт — `docs/audit-report.md`.
Главное: код чистый. knip пугает числами (64 экспорта, 170 типов) — но это почти всё ложь (barrel-реэкспорты, потребление воркером/скриптами, лишний `export` на живом коде). Реально мёртвого мало.

## Этап 2 — уборка (атомарными коммитами, гейты tsc+build+vitest перед/после каждого)
- **N+1 в /api/select** (`02a684e`) — цикл по фото → один `.in()`; горячий родительский путь (проверка занятости групповых фото при каждой отправке родителем). Логика та же.
- **13 неиспользуемых импортов/переменных** — в `dc2337a` (склеилось с чужим коммитом из-за гонки двух сессий в одном дереве; история не переписывалась).
- **Категория A добита** (`4d6866b` 4 мёртвых экспорта, `944d582` 2 orphan-файла, `ce5f462` 3 зависимости; npm убрал 11 пакетов).
- **effectiveHasQuote — настоящий БАГ, не мусор** (`e6e7857` + docs/fix-quote-tail-consistency.md). РЭ.40 (aaf4910) заменил хвостовой поиск на quote-слепой цикл → combined-хвост мог показать цитаты при сетке без цитат (quote-fallback). Восстановлен жёсткий фильтр `masterHasQuote === effectiveHasQuote`. Тест проверен в обе стороны (без фикса падает). Регресс 1178/1178. Актуально, т.к. «смена дизайна в редакторе» делает неполные дизайны реальным путём.
- **Категория B разобрана** (по одному, с диагностикой). Из 10 «мёртвых» action'ов реально мёртвый ОДИН: `tenant_detail` УДАЛЁН (`f7244a9`). Остальное живое/заделы:
  - `template_set_my_list` оставлен+TODO (`1cd98a5`) — уникален vs designs_list.
  - `pricing`/`update_notes`/`update_data`/`rule_preset_create` оставлены+TODO (`5cc6fd0`) — заделы. `rule_preset_create` НЕ дубль клона (пустой пресет с дефолтами).
  - `register_photo` — врущий комментарий «используется» ИСПРАВЛЕН (`4fad550`): вызовов 0, реальная регистрация через /api/upload+register_original. Ложный «используется» опаснее мёртвого кода.
  - `rule_preset_update` — оказался ЖИВОЙ (PresetEditorModal), зря был в B.

## Отложено (отдельные сессии)
- `tag_photo`/`untag_photo` — не трогали, риск родительского флоу (привязка фото↔ребёнок). Разобрать с проверкой /[token], /teacher/[token].
- Категория C (последовательные подписи cover/tenant) — низкий приоритет.
- A5 — дубль константы `PT_PER_MM`/`POINTS_PER_MM` — не срочно.

## Уроки
- Параллельная вторая сессия в одном рабочем дереве → `git commit` подхватил чужие застейдженные файлы. Перед git-операциями проверять `git log`/reflog на неожиданные чужие коммиты.
- Двухэтапный подход (отчёт → подтверждение → уборка) спас от удаления живого: воркер-код, заделы под названные фичи, и врущий комментарий, который выглядел как «используется».

---

# Сессия 2026-06-25 (продолжение) — жизненный цикл архива: отложенное автоудаление исходников

## Что это
Архивный заказ через **90 дней** автоматически освобождает место: удаляются ТОЛЬКО
исходники фотографа (тяжёлые JPG, `photos.original_path`). Превью (`storage_path`) и
записи `photos` остаются — архив остаётся смотрибельным. Удаление необратимо (живое
Timeweb-хранилище) → фича с буфером, отменой, предохранителями. Заодно закрывает баг:
`archive_album`/`delete_album` били удалением в Supabase Storage API вместо Timeweb
(`yc:`-ключи) → место не освобождалось.

## КРИТИЧНОЕ ядро — анти-шаринг
~13% записей `photos` делят файлы (клоны ссылаются на storage заказа-источника).
Удалять по префиксу `<album_id>/` НЕЛЬЗЯ — сломает живой клон. Единственный
безопасный способ: оригинал удаляем, ТОЛЬКО если на этот `original_path` не
ссылается НИ ОДИН не-истёкший заказ. Реализовано в `lib/archive-cleanup/core.ts`
(`computeDeletableOriginals`, `protectedPaths`).

## Сделано за сессию (4 фазы, всё в main, оба зеркала)
- **Фаза 1** (`3ad5142`) — миграция `2026-06-25-archive-lifecycle.sql`: albums +=
  `archived_at`, `keep_originals_forever`, `originals_deleted_at`. ПРИМЕНЕНА на проде
  через `scripts/db-migrate.mjs` (первая миграция, применённая Claude напрямую).
  11 старых архивных НЕ бэкфилл (archived_at=null = safe default). archive_album
  ставит archived_at=now, unarchive снимает.
- **Фаза 2** (`f06cdaf` + docs `bfed62d`) — `lib/archive-cleanup/{core,run}.ts`:
  ядро отбора + анти-шаринг, `runCleanup({dryRun})`. 14 тестов (вкл. анти-шаринг,
  падающий без фильтра). Dry-run на проде: сейчас 0 (защита бэкфилла); симуляция
  «Тест 30 истёк / клон Тест 13 живой» → 242 общих оригинала исключены.
- **Фаза 3** (`2554ad9`) — `scripts/archive-cleanup.mts` (по умолчанию DRY-RUN,
  реальное удаление только с `--apply`; опции `--ttl-days`, `--only`). systemd:
  `deploy/timeweb/yearbook-cleanup.{service,timer}` — сервис oneshot с `--apply`,
  таймер ежедневно 04:00 UTC. **Таймер СОЗДАН, но НЕ включён.** Две развилки защиты:
  таймер выключен + скрипт без флага не удаляет.
- **Фаза 4** (`2452c4d`) — UI отсчёта в модалке заказа (`archiveLifecycleStatus`,
  чистая): not_started (archived_at=null=«отсчёт не начат») / countdown N / forever /
  deleted. Кнопки «Продлить на 90»/«Запустить отсчёт»/«Оставить навсегда»/«Включить
  снова» → tenant API (`extend_archive_ttl`, `keep_originals_forever`,
  `resume_archive_autodelete`, все tenant-aware). +8 тестов. Гейты: tsc, 1200, build.

## Проверено на сервере (только чтение)
Юнитов чистильщика в `/etc/systemd/system/` НЕТ, `yearbook-cleanup.timer` = not-found/
inactive. Активны только штатные deploy.timer + backup.timer. Ничего не удаляется.

## Фаза 5 (боевое включение) — СЛЕДУЮЩАЯ СЕССИЯ, по «ок» на каждый шаг
1. dry-run на проде `tsx scripts/archive-cleanup.mts` (без флага).
2. проба `--apply --only=<один_заказ>`.
3. полный прогон `--apply`.
4. `systemctl enable --now yearbook-cleanup.timer`.
Закладка у Сергея — комментарием к карточке Notion (отдельную запись не делать).
ТЗ — `docs/tz-archive-lifecycle.md`.
