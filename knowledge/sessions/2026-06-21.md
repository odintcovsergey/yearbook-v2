# Сессия 2026-06-21 — Переезд на Timeweb: шаг 1 (БД) + шаг 2 (хранилище)

Два ТЗ за сессию. Принцип обоих: переносим **копией**, прод (Vercel+Supabase+Yandex)
НЕ трогаем и НЕ переключаем. Ветка работ — `feat/migrate-storage-timeweb` (НЕ в main).
Реквизиты/сертификат — только в `.env.local`; `ca.crt` (SSL Timeweb) в корне, в .gitignore
(добавлены `*.crt`/`*.dump`, коммит `fd08a37`).

## Шаг 1 — перенос БД Supabase → Timeweb PostgreSQL (✅ выполнен)

- Цель: Timeweb Managed **PostgreSQL 18.4** (Москва, публичный IP). Источник: Supabase
  **PG 17.6**. Инструменты — клиент **postgresql@18** (brew install, не системный 16).
- SSL: подключение по IP → `sslmode=verify-ca` с `ca.crt` (НЕ `verify-full` — в сертификате
  доменное имя, по IP не сходится). `SELECT version()` → 18.4. ✅
- Дамп: `pg_dump "$SUPABASE_DATABASE_URL" --schema=public --no-owner --no-privileges -Fc`
  (через Shared Pooler `aws-1-eu-west-2.pooler.supabase.com:5432`). Заливка `pg_restore`.
- Грабли:
  - 4 таблицы (children/teachers/invitations/responsible_parents) не создавались — юзают
    `extensions.gen_random_bytes` (pgcrypto). Решение: `CREATE SCHEMA extensions; CREATE
    EXTENSION pgcrypto WITH SCHEMA extensions;` (доверенное, владельцу БД можно) → перелив начисто.
  - 2 RLS-политики на `children` (роли `anon`/`service_role`) НЕ доехали — у пользователя
    Timeweb нет CREATEROLE. Решили «потом» (приложение ходит через сервис-ключ). RLS включён
    на всех 52 таблицах, 8/10 политик создано.
- **Сверка: 52 таблицы, 14060 строк, 0 расхождений** (per-table count). Один альбом со связями
  читается (Тест 30: 30 детей/242 фото/5 учителей).

## Шаг 2 — перенос хранилища Supabase Storage + фото → Timeweb S3 (✅ выполнен)

Целевой бакет — один приватный Timeweb S3 (`s3.twcstorage.ru`, `forcePathStyle:true`).

### Код (за переключателем `STORAGE_BACKEND`, по умолчанию `supabase` = поведение как сейчас)
- `lib/storage-twc.ts` — S3-клиент Timeweb (upload/signed-GET/presigned-PUT/getBuffer/list/delete/copy).
- `lib/blob-storage.ts` — диспетчер: `resolveReadUrl` (понимает и старый полный supabase-URL, и
  относительный ключ), `storedValue` (что писать в БД: полный URL в supabase / ключ в timeweb —
  пропущенный читатель ломает только timeweb, не прод), `signForClient`, `signDecorPlaceholders`,
  `serverUpload` (опц. supabase-клиент для тестов), `createUploadTarget`, `removeBlobs`,
  `listBlobs`, `copyBlob`. В Timeweb ключ = `<bucket>/<path>`.
- `lib/blob-upload-client.ts` — клиентская загрузка ветвится по ответу сервера (Supabase token
  vs Timeweb presigned PUT).
- Переписаны: запись/загрузка/удаление в 6 серверных файлах (idml upload, cover upload, covers,
  referral-programs, super backgrounds, tenant) + 4 клиентских места загрузки. Чтение на signed:
  covers-список, реф-картинки (referral-programs/referral/child/tenant — 5 эндпоинтов),
  backgrounds GET, **PDF-экспорт (фон+декор)**.
- ⚠️ ОТЛОЖЕНО на шаг 3 (нужна живая проверка): подпись данных РЕДАКТОРА в `app/api/layout/route.ts`
  (пул фонов/`__bg__`/master-override/default + декор), cover-канвас, `lib/cover/preview-svg.ts`.
  ТОНКОСТЬ: «пикер» фона сохраняет выбор в БД → нельзя класть signed (протухнет за 24ч); хранить
  ПУТЬ, показывать signed — разделить поля и проверить вживую.
- tsc / **1113 тестов** / next build — зелёные. 1 тест декора поправлен под `storedValue`.

### Копирование (Часть B) — rclone + node-скрипт
- Фото Yandex `yearbook-photos` → корень бакета Timeweb (те же ключи, UUID-префиксы не
  конфликтуют с supabase-префиксами): **5631 файл / 13.3 ГБ**. Было 32 временных `GOAWAY`
  (Timeweb перезапускал узел) — rclone повторил и долил. `rclone check --one-way` →
  **0 расхождений, 5631 совпадает**.
- 3 бакета Supabase (публичные) → Timeweb под префиксы через `scripts/migrate-supabase-storage.mjs`
  (читает env, без секретов): template-backgrounds 40, template-decorations 55, referral-images 2
  (decoration/referrer/invitee пусты). Сверка на Timeweb совпала.
- **Итог бакета: 5728 объектов / 13.348 ГБ.** Источники (Yandex/Supabase) НЕ чистились.

### Проверка (Часть D)
- Приватность: прямой доступ без подписи → **403**; signed-GET → **200** (проверено на фото
  `image/webp` и на фоне дизайна). Read-механизм доказан.
- Прод не переключён.

## Коммиты (ветка feat/migrate-storage-timeweb)
- `fd08a37` chore: .gitignore `*.crt`/`*.dump`
- `018c7a7` feat(переезд): хранилище за `STORAGE_BACKEND` (17 файлов)
- `d93ad1b` chore(переезд): скрипт копии Supabase Storage → Timeweb

## Шаг 3, подэтап 1 + фото — живой канвас на Timeweb (✅ выполнен, коммит `054e005`)

Всё за переключателем `STORAGE_BACKEND` (при `supabase` поведение прежнее, прод не тронут;
новое — только при `timeweb`). 11 файлов.

- **Фото детей за тем же флагом** — `lib/storage.ts`: 5 функций (`getPhotoSignedUrl`,
  `ycGetObjectBuffer` для PDF, `ycUpload`, `getYcUploadUrl`, `ycDelete`) при `timeweb` ходят в
  Timeweb с тем же ключом (`stripYcPrefix`; фото скопированы в корень бакета). Покрыло палитру,
  портреты, общие фото, PDF, загрузку — разом. Флаг читается из env напрямую (не через
  blob-storage) во избежание цикла импортов `storage → blob-storage → supabase → storage`.
- **Канвас разворота** — `/api/layout?action=template_set_detail` при `timeweb` отдаёт карту
  `bg_signed` (ключ→signed для пула фонов/default/master-override) + подписывает декор шаблонов
  (`signDecorPlaceholders`). Клиент (`page.tsx` `toBgPublicUrl`) и `SpreadBackgroundPicker`
  показывают signed; в `__bg__` сохраняется КЛЮЧ (signed протух бы за 24ч).
- **Обложки** — `loadCoverEditor` отдаёт `bg_signed` + подписывает декор мастеров; клиент
  (`CoverCanvas`, `CoverBackgroundPanel`, лента-превью `coverThumb`, `CoverPreviewFullscreen`)
  конвертирует ключ→signed через хелпер `signCoverBg` (`lib/cover/editor-merge.ts`); covers API
  библиотечное превью (`coverPreviewSvg`) подписывает фон+декор.
- **Проверка:** tsc / **1113 тестов** / build — зелёные. API-смоук локально с
  `STORAGE_BACKEND=timeweb` (минтил superadmin-JWT через jose): фоны/декор (37 ключей в
  `bg_signed`), фото детей (242 в палитре), общие фото — все Timeweb-signed; signed→**200**,
  без подписи→**403**. Окружение вернул (флаг убрал, dev заглушил). Визуально на холсте НЕ
  прокликано — за Сергеем (включить флаг локально/preview).
- **NB (существующая хрупкость, не чинили):** signed-URL фото ВШИТ в `album_layouts.spreads[].data`
  при drag&drop. Старые развороты тянут фото из Yandex (жив), свежие сборки/палитра — с Timeweb.

## 🛑 Шаг 3, подэтапы 3-4 (БД на Timeweb) — архитектурный стоп

В рантайме приложение ходит в БД **только через supabase-js (PostgREST)** — прямого
`DATABASE_URL`/`pg` в `app/`+`lib/` НЕТ. Timeweb Postgres PostgREST не отдаёт → «переключить
`DATABASE_URL`» это НЕ флаг, а переписывание всего слоя данных (`supabaseAdmin.from(...)` →
pg/свой API). Это **шаг 4** (приложение на своём VDS). `TIMEWEB_DATABASE_URL` сейчас юзается
только скриптами дампа/сверки, не рантаймом.

## Шаг 3 — живая проверка timeweb + фиксы багов (коммит `5ebeeca`)

Сергей включил `STORAGE_BACKEND=timeweb` локально и прокликал. Найдено и починено:
- **№3 Загрузка фона обложки — «Failed to fetch».** Причина: у бакета Timeweb не было
  CORS (`NoSuchCORSConfiguration`); браузерный presigned-PUT упирался в CORS (для показа
  `<img>` CORS не нужен, поэтому всё отображалось). Настроил CORS на бакете
  (`PutBucketCorsCommand`: origins `*`, GET/PUT/POST/HEAD). Проверил preflight → 200.
- **№3b Залитый фон обложки не отображался.** На timeweb новый фон сохраняется коротким
  ключом, а карта `bg_signed` собиралась при загрузке редактора (нового ключа нет).
  Починка: `cover_bg_sign` теперь возвращает `read_url` (signed), клиент держит `bgSigned`
  в state и подмешивает туда новый ключ при загрузке. Прокликано — фон виден.
- **№2 Нет пометок «фото занято/номер разворота».** Опознание фото шло по нестабильной
  signed-URL-строке. Перевёл на стабильный ключ (`storage_path`): `PhotoPalette.findUsage`
  + `matchPhoto` ×4 в layout/page.tsx (бейджи, drag-swap, контекст-меню, замена оригинала).
  Проверено на данных: 41 фото получат бейдж в редакторе РАЗВОРОТА (в обложках номеров
  нет by design — там `spreads=[]`).
- **№1 Нет полупрозрачного превью при drag из палитры** — не переезд (UI dnd-kit, превью
  из `thumb_url` грузится). Отложено в память [[project-timeweb-editor-followups]].
- Пустая плитка портрета — давний осиротевший файл (нет НИ в Yandex, НИ в Timeweb), не переезд.
- Медленная загрузка палитры (~12с на 514 фото) — стоимость подписи >1000 ссылок + удалённая
  БД; не регресс timeweb; ускорится после шага 4; варианты в followups-памяти.
- Грабли процесса: запустил `next build` поверх работающего `next dev` → `ChunkLoadError`
  у Сергея. Урок в память [[feedback-no-build-while-dev]]: глушить dev перед билдом.

## Шаг 4 — АНАЛИЗ + подготовка (выбран Вариант 1: self-host PostgREST)

ТЗ перевести приложение с Supabase API (PostgREST) на Timeweb Postgres напрямую (~34 файла).
Проанализировал 3 варианта (2 разведки по коду). **Замеры:** ~868 `.from()`, `.rpc()`=0,
используется ТОЛЬКО PostgREST (нет Supabase Auth/Realtime/Functions); БД строго server-only
(прямых браузер→БД нет); RLS не рантайм-зависимость (service_role обходит, авторизация в коде);
auth своя (JWT). PostgREST-специфики много: 37 эмбедов, 14 `!inner`/`!fk`, 26 `.or()` сырых,
53 count/head, 239 `.single/.maybeSingle`.

**Решение — Вариант 1 (self-host PostgREST)** рядом с Postgres: приложение остаётся на
`supabase.from()`, меняются endpoint+роль БД, НЕ 868 запросов. Shim = переписать сам PostgREST
(отверг), полный rewrite на Drizzle = недели + ломает «плавно» (отверг; опц. позже strangler).

**Артефакты (коммиты `651e1ef`, `4528582`):**
- `docs/tz-step4-deployment.md` — ТЗ: роли/гранты, конфиг PostgREST, nginx `/rest/v1`,
  проверка на копии, чек-лист cutover, разворачивание приложения на VDS, процедура миграций+reload.
- `scripts/compare-rest-backends.mjs` — сверка A4 (одинаковые запросы на 2 бэкендах + дифф).
- `scripts/db-migrate.mjs` — apply миграции + `NOTIFY pgrst reload`, защита от боевого.
- `scripts/step4-postgrest-roles.sql` — роли authenticator/web_app + гранты (least-privilege).

**✅ A4 ДОКАЗАНА ЛОКАЛЬНО:** поставил PostgREST 14.13 (brew), поднял против КОПИИ Timeweb
(как `gen_user`, db-anon-role=gen_user — CREATEROLE не нужен, владелец обходит RLS), прогнал
сверку: **Supabase vs self-hosted PostgREST = 9/9 PASS** на реальных данных (эмбеды, !inner,
!fk, вложенный count, .or(), count=exact). Связь Timeweb PG18.4 ок (albums=19/photos=3936 =
как в Supabase). PostgREST остановлен по завершении.

## ГДЕ ОСТАНОВИЛИСЬ (продолжить отсюда)

Состояние: вся работа в ветке `feat/migrate-storage-timeweb` (НЕ в main, прод не тронут),
последний коммит `4528582`. Хранилище (шаги 2-3) — на timeweb за флагом, прокликано/починено.
Шаг 4 — Вариант 1 выбран и ДОКАЗАН на копии (A4 9/9).

Дальше (требует сервера — панель Timeweb/SSH, с ноутбука Claude не может):
1. **Расширить сверку A4** перед cutover: добавить в `compare-rest-backends.mjs` реальные
   эмбеды из CRM и др. (сейчас покрыты классы фич, не все 37 эмбедов).
2. **Боевые роли БД:** authenticator/web_app требуют суперюзера/панели Timeweb (gen_user без
   CREATEROLE/BYPASSRLS). Запросить у Timeweb или сделать через панель → применить
   `scripts/step4-postgrest-roles.sql`.
3. **Развернуть PostgREST на сервере** (закрепить версию под Supabase v12), nginx `/rest/v1`.
4. **VDS Timeweb для приложения:** Node, домен, HTTPS (certbot), авто-деплой (CI/CD),
   перенести тот же `JWT_SECRET` (иначе разлогин всех).
5. **Cutover:** ENV приложения → внутренний PostgREST + `STORAGE_BACKEND=timeweb` + финальная
   дельта-синхронизация БД и storage (rclone — помним, ≥1 файл добавился после снимка).
6. Мелочь вне объёма: логотип партнёра + QR обложки в старом Supabase-бакете `photos` (Фаза 3).

Claude по запросу готовит: SQL грантов, конфиги PostgREST/nginx, CI-workflow, расширение сверки.
