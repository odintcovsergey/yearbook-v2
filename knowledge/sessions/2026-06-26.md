# Сессия 2026-06-26 — Жизненный цикл архива: проба, честный UI, включение таймера

Итог: **фича «Жизненный цикл архива» закрыта целиком и переведена в Notion в «Сделано».**
За сессию: диагностика шаринга файлов → боевая проба автоудаления (фикс обнуления
подтверждён) → две UI-правки про честный текст → доставка релиза через GitHub-флап →
установка и включение systemd-таймера чистильщика.

## 1. Диагностика: почему объём Timeweb не растёт после копии заказа

Сергей сделал копию «Тест 30 — копия» (`78cc476a…`) и заметил, что объём хранилища
не вырос. Проверка по БД (только чтение):
- У копии **242 записи `photos`**, но **все 242** `storage_path`/`original_path`
  указывают на префикс **источника** `yc:def23fce…` (album_id «Тест 30»), НЕ на
  собственный префикс копии.
- **Копирование заказа НЕ дублирует файлы в хранилище** — создаются только строки в
  БД, ссылающиеся на те же ключи источника. Поэтому объём не растёт. Это подтверждение
  полного шаринга.
- Own-оригиналов у копии = **0**, shared = 242 (те же файлы держат живые «Тест 30»,
  «Тест 13» + архивный «Тест 24»). Для пробы автоудаления копия НЕ годится — удалять
  нечего (анти-шаринг защитит всё).

## 2. Выбор заказа для пробы

Искали мусорный заказ с СОБСТВЕННЫМИ оригиналами (own>0, не делят живые). Нашли 5
(все в тенанте OkeyBook, демо-данные): Лицей 25 11А (282), **Школа 81 (269, archived)**,
Школа 62 (239), МБОУ Заречье (108), Детский сад 201 (86). Тест-семья 13/24/30/копия —
все own=0 (шарят).

Сергей выбрал **«Школа 81»** (`bb97b2b4…`) осознанно: заказы сданы, исходники есть
локально, удаление не критично.

### 🔑 Урок: `--ttl-days=0` НЕ делает заказ истёкшим без `archived_at`
Первый dry-run `--only=bb97b2b4 --ttl-days=0` вернул **0**. Причина — порядок веток в
`isExpiredAlbum` (core.ts): `if (!a.archived_at) return false` срабатывает РАНЬШЕ проверки
TTL. `--ttl-days=0` схлопывает только ожидание «90 дней», но дата архивации обязана быть.
У всех 5 own-заказов `archived_at` был null (старые архивные до бэкфилла). Сергей
предложил НЕ править боевую БД руками — вместо этого нажал штатную кнопку **«Запустить
отсчёт (90 дней)»** (`extend_archive_ttl`, ставит `archived_at=now`) в карточке заказа.
Это продуктовый флоу, не SQL-хак.

## 3. Проба автоудаления (Фаза 5) — ФИКС ОБНУЛЕНИЯ ПОДТВЕРЖДЁН

Числа «Школы 81»: 269 строк = 269 уникальных оригиналов, 0 дублей, 269 превью (расклад
1:1, удобно для проверки).

- **dry-run** `--only=bb97b2b4 --ttl-days=0`: 269 ключей / 0 анти-шаринг / 1.448 ГБ ✅
- **`--apply`**: `удалено 269 = обнулено 269, ошибок 0` ✅ — скрипт НЕ упал на сверке
  количества. Обнуление прошло 6 пачками по 50 (5×50+19) — то самое место, где раньше
  (баг пробы 25.06) длинный `.in()` молча упирался в лимит URL и обновлял только первую
  пачку (71). Фикс (NULL_CHUNK=50 + обязательная проверка error + сверка `nulledRows ==
  expectedNulled`) работает на боевом объёме.
- **Сверка по БД**: висячих `original_path` = 0, превью 269 на месте (смотрибелен),
  `originals_deleted_at` проставлен, `audit_log` запись `album.originals_autodeleted`
  (keys 269, ~1.55 ГБ), повторный dry-run = 0 (идемпотентно).

## 4. UI-правки — честный текст про отложенное удаление (релиз `6d68e8b`)

Старый UI остался от сломанной логики мгновенного удаления при архивации → два места
врали. Обе правки в `app/app/page.tsx`:

1. **Вкладка «Фото» архивного заказа.** Было: «фотографии удалены из хранилища при
   архивировании». Стало: статус по факту из той же `archiveLifecycleStatus` (core.ts,
   вторую логику не плодим) — countdown «удалятся через N дн.» / not_started / deleted
   «[дата]» / forever. В `PhotosTab` проброшены `archivedAt`/`keepForever`/
   `originalsDeletedAt` (раньше знал только булево `archived`).
2. **Подтверждение архивации.** Было: «удалены с сервера / ссылки для родителей
   перестанут работать». Стало: «через 90 дней оригиналы удалятся автоматически, превью
   останутся». Фразу про ссылки СНЯЛИ: проверка фактом — `/api/child` на `archived` не
   смотрит → после архивации ссылки продолжают работать, claim был ложным.

Гейты: tsc чисто, 1204/1204 vitest, next build зелёный. Прокликано Сергеем на проде.

## 5. Деплой через GitHub SSL-флап (грабля)

Авто-деплой завис ~15 мин: `git fetch origin main` к GitHub с РФ-сервера то падал
(`SSL connection timeout`), то висел весь таймаут → `yearbook-deploy.service` болтался
в `activating`. Распознали, что это ЗАВИСШИЙ ФЕТЧ, а не сборка: `ExecMainStartTimestamp`
пустой, нет процесса `next build`, нет нового `releases/<sha>`.

**Обход (отработал):** доставить коммит git-бандлом с Mac минуя сеть.
1. Mac: `git bundle create yb.bundle ^df30675 main` (инкрементальный, 2.4 КБ; валиден —
   prerequisite `df30675` уже в repo сервера).
2. `scp` → `/srv/yearbook/`, `chown yearbook`.
3. Стоп гонок: `systemctl stop yearbook-deploy.timer yearbook-deploy.service` + мой
   nudge-цикл.
4. Детачем через `systemd-run`: `git -C repo remote set-url origin <bundle>` →
   `env YB_ROOT=/srv/yearbook deploy.sh` (штатный build+health+авто-откат, читает
   `origin/main` из бандла) → ОБЯЗАТЕЛЬНО вернуть `remote set-url origin <github-url>`.
5. `systemctl start yearbook-deploy.timer`, убрать бандл.

Результат: `current → 6d68e8b`, health OK (login/api/health 200), origin назад на GitHub,
таймер авто-деплоя восстановлен.

## 6. Включение таймера чистильщика

1. `cp deploy/timeweb/yearbook-cleanup.{service,timer} /etc/systemd/system/` +
   `daemon-reload`.
2. Сверка `.timer`: `OnCalendar=*-*-* 04:00:00 UTC`, `Persistent=true`, `AccuracySec=5min`.
   ⚠️ ExecStart сервиса = `--apply` (боевое удаление по таймеру — так и задумано).
3. **Холостой прогон** `systemctl start yearbook-cleanup.service` (targets трижды
   подтверждены = 0): журнал `режим APPLY / истёкших 0 / удалено 0 / обнулено 0 / ошибок 0`,
   `Result=success`, `ExecMainStatus=0` — сервис поднялся с правильным окружением под
   юзером yearbook.
4. `systemctl enable --now yearbook-cleanup.timer` → enabled+active. Первое реальное
   срабатывание **Sat 2026-06-27 04:00 UTC**.

Заморозить авточистку = `systemctl disable --now yearbook-cleanup.timer`. 11 старых
архивных с `archived_at=null` чистильщик не трогает, пока не нажать «Запустить отсчёт».

## Коммиты
- `6d68e8b` — fix(archive): честный текст про отложенное автоудаление (2 UI-правки).
- (проба и включение таймера — действия на сервере, не коммиты в репо.)

## Связанные доки/память
- `docs/tz-archive-lifecycle.md` — ТЗ фичи.
- MEMORY: `project_archive_lifecycle` (обновлён — фича в проде), `project_cicd_autodeploy`
  (добавлен приём обхода GitHub-флапа бандлом).

---

# Часть 2 (вечер): фикс delete_album + чистка сирот Timeweb

## Симптом (от Сергея)
Удаление части фото из раздела освобождало место на Timeweb, а удаление **всего
заказа** через шестерёнку — нет. Гипотеза: тот же баг-класс, что у архивации
(удаление бьёт в Supabase API вхолостую на Timeweb).

## Диагностика (на фактах)
- `delete_album` (route.ts ~5961) слал `supabaseAdmin.storage.from('photos').remove(['yc:...'])`
  по всем путям — на Timeweb no-op (объекта с именем `yc:...` в Supabase-бакете нет).
  Записи `photos`/`children`/`teachers`/альбом стирались → **осиротевшие файлы**.
  Брал только storage_path+thumb_path, **`original_path` не трогал вообще**.
- `delete_photo` (~6791) и `delete_photos_by_type` (~6725) — ВЕТВЯТСЯ `isYcPath→ycDelete`,
  потому работают; второе ещё и берёт original_path.
- **Физически подтверждено** на живом Timeweb (листинг + сверка с прод-БД через
  PostgREST/psql на VDS): 5 «осиротевших» префиксов-заказов = 1062 файла / 2.31 ГБ,
  которых нет ни в `albums`, ни в `photos`. Крупнейший 6e069835 (546 файлов/1.3 ГБ).

## Часть А — фикс (релиз `d3dba22`)
- `lib/storage-twc.ts`: + `twcDeleteStrict` (бросает при ошибке, в отличие от тихого twcDelete).
- `lib/storage.ts`: + `ycDeleteStrict` (ветка timeweb→twcDeleteStrict, иначе Yandex DeleteObject без catch).
- `route.ts delete_album`: select += original_path; сбор всех 3 путей (уникализация);
  ветка `isYcPath→ycDeleteStrict`, legacy supabase-пути через Supabase Storage;
  `Promise.allSettled` батчами по 50, сбор провалов; при любом сбое — **заказ НЕ
  удаляется (502)**, записи БД целы (урок бага обнуления: не делать вид, что всё ок);
  порядок файлы→БД; лог `album.delete` += `files_deleted`.
- Гейты: tsc чистый, vitest 1204/1204, next build зелёный.

## Проба (тестовый заказ «Еще один тест», 119d5f05…)
- ДО: Timeweb 40 файлов / 111.1 МБ (20 оригиналов 103.6 МБ + 20 портретов 7.5 МБ), photos=20.
- Сергей удалил через UI.
- ПОСЛЕ: Timeweb 0, photos=0, children=0, альбом исчез. audit_log.meta
  `{"files_deleted":40,"photos_deleted":20}` (поле `files_deleted` = маркер нового кода;
  у удалений старым кодом сегодня — только `photos_deleted`).

## Часть Б — разовая чистка накопленных сирот
- Дроган с **двойной страховкой**: сирота = UUID-префикс, которого НЕТ И в `albums`,
  И среди префиксов `photos`. Страховка поймала 2 живых альбома (381cb27f, c857f67d)
  с файлами но без photos-записей → корректно ОСТАВЛЕНЫ (510 файлов спасены).
- Удаление строго по явному списку 1062 ключей (`orphan-keys.txt`, НЕ по маске),
  батчами S3 `DeleteObjects` по 1000 с проверкой `Errors`, предохранитель «каждый ключ
  внутри 5 утверждённых префиксов». Результат: 1062 удалено, 0 ошибок.
- Контроль: 5 префиксов = 0 объектов; бакет 5753→4691 объекта, 12303→9934 МБ (−2.31 ГБ).

## Деплой
GitHub SSL-флап снова застопорил pull-деплой (бесконечный `git fetch origin main` → SSL
timeout). Расклинено git-бандлом с Mac: `git bundle create 6d68e8b..main` → scp в
`/srv/yearbook/shared` → временно `origin`→путь бандла → `systemctl start
yearbook-deploy.service` (штатный build+switch+health) → вернул `origin` на GitHub,
убрал бандл. Health 200/200.

## Хвосты
- 🚩 **archive_album** (~5778) — ТОТ ЖЕ БАГ (supabase remove no-op на Timeweb по
  storage_path+thumb_path) → архивация оставляет display-файлы сиротами. НЕ фикшено.
- 🚩 **Аномалия:** 2 живых альбома (381cb27f, c857f67d) держат 276 и 234 файла в
  Timeweb без записей `photos`. Причина неясна — отдельный разбор.

## Коммиты (Часть 2)
- `d3dba22` — fix(delete_album): реально удаляет файлы из Timeweb + original_path.

## Связанная память (Часть 2)
- `project_delete_album_orphan_fix` (новый), `project_supabase_storage_remove_tail`
  (обновлён — delete_album закрыт, archive_album остаётся).

---

# Часть 3 (вечер): фикс archive_album — вариант (а), радикальный

## Диагностика (перед фиксом — архивацию переосмыслили)
`archive_album` (route.ts ~5788) был из СТАРОЙ модели «архив = стереть файлы и записи
ради места» (апрельский `2b07ce2`). Lifecycle-коммит `3ad5142` (25.06) лишь ДОБАВИЛ
`archived_at` сверху, разрушительную логику не убрал. Старый код делал:
- select storage_path+thumb_path (без original_path);
- `supabase.storage.remove(...)` → **no-op на Timeweb** → display-файлы сиротами;
- **`from('photos').delete()`** → стирал ВСЕ записи photos + каскад selections;
- ставил archived/archived_at.

Это противоречит (1) новой отложенной модели — cleanup (`lib/archive-cleanup/run.ts`)
ОБНУЛЯЕТ `original_path` через UPDATE, строки/превью обязаны выжить; (2) UI-обещанию
(page.tsx ~4716): «через 90 дней оригиналы удалятся, ПРЕВЬЮ ОСТАНУТСЯ, выборы сохранятся».
Эмпирика прод-БД подтвердила: lifecycle спроектирован под выживание строк (архивные
заказы держат записи photos; cleanup Школы 81 обнулил 269 original_path, строки целы).

Заодно объяснилась «аномалия» Части Б: 2 альбома (Павловская/Школа 68) с файлами но
0 записей photos — это ЖЕРТВЫ старого archive_album (записи стёрты, файлы осиротели).
Третий такой — Тест 13.

## Фикс (вариант «а», релиз `f6cd13d`)
Убран весь разрушительный блок (select путей + supabase.remove + photos.delete).
Осталось: assertAlbumAccess → `update albums SET archived=true, archived_at=now()` →
log. Ответ `{ok:true}` (было `{ok:true,deleted:N}` — UI не читал). Симметрично
unarchive_album. Вариант (б) «перевести на ycDelete» отвергнут: удалять при архивации
нечего — оригиналы сносит таймер через 90 дней (с анти-шарингом клонов ~13%).
Гейты: tsc чистый, vitest 1204/1204, build зелёный.

## Проба (Тест 30, def23fce — тестовый заказ с данными)
До: photos 242 / original 242 / selections 130. После архивации через UI:
- записи photos **242=242**, original_path **242** (не тронуты), display **242** — целы;
- selections **130=130** (старый код снёс бы каскадом);
- Timeweb: **484 файла на месте** (242 ориг + 242 display) → архив смотрибелен;
- `archived=true, archived_at` проставлен; audit `album.archive {}` (новый код).
- cleanup **dry-run** (`--ttl-days=0`, БЕЗ `--apply`): заказ ПОДХВАЧЕН, 242 оригинала
  исключены анти-шарингом — клон **Тест 24** (`b3994ae3`) держит те же файлы (защита из ТЗ).

Тест 30 оставлен в архиве (тестовый). Баг-класс delete закрыт полностью.

## Деплой
GitHub SSL-флап ещё активен (ls-remote с сервера пуст) → снова доставка git-бандлом
(`d3dba22..main`) тем же приёмом. Health 200/200, origin возвращён на GitHub.

## Хвост (НЕ сейчас, отдельная операция)
3 архива, пострадавшие от старого archive_album ДО фикса: **Павловская** (381cb27f, 276
файлов), **Школа 68** (c857f67d, 234), **Тест 13** (91400b30). Записи photos стёрты
безвозвратно (0 строк) → заказы уже не смотрибельны. Осиротевшие display-файлы чистить
отдельно: они числятся за ЖИВЫМИ альбомами, поэтому Часть-Б-критерий (двойное НЕТ в
albums И photos) их верно НЕ тронул — нужен др. критерий (archived + 0 photos-строк + есть файлы).

## Коммиты (Часть 3)
- `f6cd13d` — fix(archive): archive_album только помечает, не рушит данные.

---

# Часть 4 (вечер): чистка осиротевших файлов 3 пострадавших архивов — ХВОСТ ЗАКРЫТ

Жертвы старого archive_album (ДО фикса f6cd13d): Павловская (381cb27f), Школа 68
(c857f67d), Тест 13 (91400b30) — записи `photos` стёрты безвозвратно (0 строк →
несмотрибельны), display-файлы осиротели в Timeweb (supabase.remove был no-op).

## Критерий (точный, отличается от Части Б)
`archived=true` И 0 строк в `photos` И есть файлы под префиксом. Это файлы ВНУТРИ
ЖИВЫХ архивных альбомов — критерий «нет в albums» (Часть Б) тут НЕ применим.

## Анти-шаринг (по всем 4036 ключам photos: storage+thumb+original)
Для каждого own-кандидата проверено, не ссылается ли на него запись `photos` любого
другого заказа. Разбивка own (никем не делимые) vs shared (держит другой заказ):
- Павловская: 276 own (517.7 МБ: originals 138 + portrait 131 + teacher 7), shared 0.
- Школа 68: 234 own (44.4 МБ, только portrait), shared 0.
- Тест 13: **0 файлов** под своим префиксом — клон-пара, его оригиналы физически под
  префиксом ЖИВЫХ Тест 30/24 → в кандидаты не попадают, верно исключены.
- SHARED итого = 0 (клоны нигде не задеты).

## Удаление (по явному списку, как утром)
510 own-ключей (`dead-own-keys.txt`), батчами S3 DeleteObjects с проверкой `Errors`,
предохранитель «ключ внутри 3 утверждённых префиксов». Результат: 510 удалено, 0 ошибок.
Контроль: 3 префикса = 0 объектов; бакет 4691→4181 объекта, −562.1 МБ (≈ 0.549 ГБ).

## 💯 Тема delete-класса закрыта полностью
Оба бага кода (delete_album d3dba22 + archive_album f6cd13d) + все сироты обоих типов.
За день освобождено суммарно: −1572 объекта, бакет 12303→9372 МБ (≈ −2.86 ГБ) —
утро −1062/−2.31 ГБ (delete_album-сироты) + вечер −510/−0.55 ГБ (archive_album-жертвы).
Чистки — данные, без коммитов в репо.
