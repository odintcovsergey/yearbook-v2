# КОНТЕКСТ ДЛЯ ПРОДОЛЖЕНИЯ РАБОТЫ
# Система отбора фотографий для выпускных альбомов → платформа для школьных фотографов
# Версия: v190
# Обновлено: 18.06.2026 (сводка обложек заказа — отслеживание выбора)
#
# Предыдущий контекст: yearbook-context-v189.md (история не переписывается).

---

## Контекст задачи

ТЗ tz-cover-summary. Менеджеру нужна сводка: кто какую обложку выбрал, сколько
обложек пойдёт в печать, что не потеряно. Только просмотр (cover_choices не
меняем). Первый шаг перед редактором обложек.

Расхождения ТЗ с реальной схемой (учтены):
- В `cover_choices` НЕТ `photo_id` (колонки: child_id, cover_type, cover_id,
  photo_option, paid_personalization, surcharge). Портрет для обложки —
  `selections` где selection_type='portrait_cover'.
- Таблицы `common_photos` нет — общее фото класса = `photos.type='common_full'`
  («последнее» = по created_at; для сводки нужен лишь факт наличия).

## ✅ DONE (ветка feat/cover-summary, НЕ в main; миграций НЕТ — только чтение)

- **lib/cover/summary.ts** — чистая `buildCoverSummary(input)`:
  - counts: portrait/common/design/none/total (none = без cover_choices; в
    fixed-режиме все считаются default_type, none=0).
  - print (дедупликация): portrait = число выбравших портрет (каждая своя),
    common = 1 если есть выбравшие общую, design = 1 если есть дизайн. total = сумма.
    **Учительская пока НЕ считается** (решение Сергея — нет признака у заказа).
  - rows: статус ok / no_choice / needs_photo (портрет «другой» без фото).
  - warnings: общая выбрана без common_full; «X: портрет другой, фото не выбрано»;
    «Не выбрали обложку: N».
  - Тест lib/cover/__tests__/summary.test.ts (6 кейсов, включая пример 10+1+1=12).
- **API** `/api/tenant?action=cover_summary&album_id=` — грузит album cover-поля,
  children, cover_choices, selections.portrait_cover (миниатюра), наличие
  common_full; зовёт buildCoverSummary. assertAlbumAccess.
- **UI** `app/app/_components/CoversSummaryBlock.tsx` (dynamic) во вкладке «Обзор»
  заказа (app/app/page.tsx, после блока учителей/доплат): «На печать: N»,
  агрегаты, предупреждения (amber), список учеников (миниатюра портрета для
  «другое фото», тип, фото same/other, доплата, статус). Если обложка в заказе не
  настроена (cover_layout_mode пуст) — спокойная подсказка.

## Проверки

- tsc чисто, vitest 1030 passed (новые summary-тесты агрегации/дедупликации),
  next build зелёный. Страницы родителей не тронуты.

## 🚩 Следующий шаг

- Прогон на preview: заказ с разными выборами → проверить агрегаты, число на
  печать, предупреждения, миниатюры. Затем merge в main.
- Дальше (отдельное ТЗ): редактор обложек — рендер/кроп портрета, перетаскивание
  общего фото, автоподстановка ПОСЛЕДНЕГО common_full в общую/учительскую,
  собственно учительская обложка (тогда же вернём её в счёт сводки).
