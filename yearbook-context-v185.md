# КОНТЕКСТ ДЛЯ ПРОДОЛЖЕНИЯ РАБОТЫ
# Система отбора фотографий для выпускных альбомов → платформа для школьных фотографов
# Версия: v185
# Обновлено: 17.06.2026 (личный раздел multi_spread: авто-коллажи + ручной выбор раскладок; лимит фото с друзьями 10→30)
#
# Предыдущий контекст: yearbook-context-v184.md (история не переписывается).

---

## Контекст: живой прогон v184 выявил, что нужно

На «Аква меч» режим multi_spread (из v184) деградировал: строился только парадный
разворот + дубль портрета на правой странице. ПРИЧИНА — не баг логики, а нехватка
коллажных мастеров личного раздела (страниц с фото ученика БЕЗ портрета). ТЗ Сергея:
сделать личный раздел «несколько разворотов» КАК ОБЩИЙ РАЗДЕЛ — два режима (Авто +
Вручную). Модель блока ученика: 1 парадная ЛЕВАЯ страница (портрет+ФИО+цитата) +
коллажные страницы (правая и дальше). Парад = одна страница → дубль портрета уходит.

## ✅ DONE — Этап 1: ТЗ дизайнеру (коммит e3621b3)

`docs/tz-personal-collage-2026-06-17.md` (+ DOC на Рабочем столе Сергея, сделан
через `textutil` html→doc; PDF на macOS без доустановок не вышло — нет cups-фильтров).
Построено по РЕАЛЬНЫМ данным (распарсил IDML «Аква меч» локально tsx-скриптом).
Заказ: `E-Collage-2/4/6` (на 3 уже было), фото-слоты `studentphoto_N`, без портрета.

## ✅ DONE — Этап 2: движок «Авто» (коммит e3621b3)

`lib/rule-engine/sections/students.ts` — переписан `buildMultiSpreadSemantic`:
- Парад = ОДНА левая (findStudentMaster student_left, portrait, photos_friend:0).
- Правая и дальше — коллажи, автопак под число фото (жадно крупнейший помещающийся).
- Блок ученика = ЦЕЛОЕ число разворотов (чётно) — у следующего парад снова слева.
  Последняя правая может быть пустой (слоты __hidden__) — редко, ради чётности.
- **`findPersonalCollageMasters`** распознаёт коллаж по РЕАЛЬНЫМ плейсхолдерам
  (есть studentphoto/friendphoto, НЕТ портрета/имени/цитаты), НЕ по slot_capacity.
  Причина: одно имя в разных наборах значит разное (E-Standard-Right в «Аква меч» =
  3 фото-коллаж, метаданные family-mapping врут). По решению Сергея «Белый» не беречь.
- `bindGalleryPhotos`→`bindCollagePhotos` (скрывает пустые слоты). Удалён findGalleryMaster.
- Degrade без коллажей → парад + правая через findStudentMaster + warning
  `students_multi_spread_no_collage_master`.
- family-mapping: добавлены `E-Collage-2/3/4/5/6` (students:0, photos_friend:N, page-any).

## ✅ DONE — Этап 3: режим «Вручную» + UI (коммит 5a39fc5)

- Тип `StudentsSectionConfig.multi_spread` получил `manual_pages?: string[]|null`
  (имена мастеров по страницам, чётная длина, без миграции — в section_structure).
- `buildMultiSpreadManual` + универсальный `bindStudentPageWithOffset` (портрет/имя/
  цитата если есть + фото со смещением cursor). Неизвестный мастер → warning + пропуск.
- Валидация `validateStudentsConfig` (чётность, ≤16, непустые строки).
- UI `PresetEditorModal.tsx`: переключатель Авто/Вручную + `MultiSpreadManualEditor`
  (конструктор разворотов, левая/правая из E-* мастеров; -Right НЕ фильтруем).

## ✅ DONE — Лимит фото с друзьями 10→30 (согласованно в 3 местах)

- `lib/smart-fill/build-album-input.ts` — `MAX_FRIEND_PHOTOS = 30` (slice на входе,
  главный резак). `app/api/tenant/route.ts` — валидация student_friend_photos 0..30
  (+ текст). `lib/rule-engine/types.ts` — `friend_photos_max?: 2|3|4` → `number`
  (+ doc 0..10→0..30). Нужно для multi_spread (тест с 15+ фото не проедет с лимитом 10).
- NB: лимит отбора у родителя = `album.group_max` (настройка альбома, дефолт 2), НЕ хардкод.

## Состояние «Аква меч» (распарсил обновлённый IDML 17.06, 24 мастера)

Дизайнер прислал ВЕСЬ набор коллажей: `E-Collage-2/3/4/5/6` (чистые фото-страницы,
все распознаются движком как коллаж). `E-Standard-Right` УБРАН. Личные мастера:
E-Standard-Left (парад: портрет+ФИО+цитата, 0 фото), E-Universal-Left/Right
(портрет+2 фото+ФИО+цитата), E-Collage-2..6. Лесенка автопака полная 6→5→4→3→2.

## 🚩 ОСТАЛОСЬ — Этап 4: ЖИВОЙ ПРОГОН (НИЧЕГО из этого в браузере НЕ открыто)

План тестирования отдан Сергею (Авто базовый / Авто лимит разворотов / Вручную /
несколько личных разделов / PDF). Нужен тестовый класс с РАЗНЫМ числом фото
(2-3 / 8-12 / 15+). Смотреть: парад слева, нет дубля портрета, коллажи под число фото,
плашка предупреждений (lost_photos — ок; master_not_found — тревога), редкая пустая
правая страница. Сергей прокликивает; при косяках — докрутить.

## Проверки
980/980 vitest, tsc чистый, next build зелёный (на момент v185).

## Не тронуто / отложено
PDF-export глобальных стилей и шрифтов (раскладку фото НЕ затрагивает). Зачистка
глобальных полей student_* пресета (нужны для отката). teachers/common/cover/
родительские — не тронуты.
