# Инструкция для подэтапа v42 (закрытие фазы 0.5)

**Цель:** Обновить контекст-файл с v41 на v42. Зафиксировать что фаза
0.5 закрыта целиком (включая 0.5.6 UI пресетов).

Этот подэтап НЕ содержит изменений кода — только обновление документа.

## Шаги

### Шаг 1. Положить готовый файл v42

Файл `yearbook-context-v42.md` подготовлен заранее (Сергей загрузит его
в корень репо). Стратег уже применил все патчи к v41 локально:

- Шапка: версия v42, дата 08.05.2026, описание изменений (0.5.6.1
  + 0.5.6.1 fix sentinel + 0.5.6.2)
- Roadmap-таблица: 0.5 помечена как сделанная (объединены 0.5.3 и 0.5.6
  в одну строку «Refactor под пресеты в БД + UI пресетов»)
- Раздел «Продукт B — фаза 0.5» расширен: добавлено описание UI пресетов
  (0.5.6.1 + 0.5.6.2)
- Раздел «ПРОДУКТ B — ФАЗА 0.5 ЗАВЕРШЕНА ✅» переименован из «0.5.3 ЗАВЕРШЕНА»
  и расширен новой подсекцией про UI (AlbumFormModal, ChildPresetSelect,
  чип в строке таблицы), UX-нюансы (sentinel, чип, legacy print_type)
- Описание `app/api/tenant/route.ts` дополнено новыми actions
  (presets_list, update_child_preset, расширения create_album/update_album/children)
- Описание `app/app/page.tsx` дополнено новыми UI компонентами
  (AlbumFormModal секция «Пресет вёрстки», AlbumDetailModal Обзор,
  AlbumCard с пресетом, ChildPresetSelect)

### Шаг 2. Коммит

Один коммит:

```bash
git add yearbook-context-v42.md docs/internal/v42-instructions.md
git commit -m "docs(context): v42 — фаза 0.5 закрыта целиком (0.5.6.1 + 0.5.6.2)"
```

### Шаг 3. Push (после OK от Сергея)

```bash
git push origin main
```

## Что в результате

- ✅ `yearbook-context-v42.md` существует в корне репо
- ✅ Старые версии v2-v41 НЕ удалены (исторические записи)
- ✅ Любой следующий чат стартует с v42 (актуальное состояние, фаза 0.5
  закрыта целиком, готовность к фазе 1 Smart-fill)

## Контрольный список

- [ ] 0.5.6.2 запушен в main (коммит `329ea7c` на origin/main)
- [ ] Файл `yearbook-context-v42.md` лежит в корне репо
- [ ] Файл `docs/internal/v42-instructions.md` лежит в репо
- [ ] Коммит сделан, ожидание OK на push
