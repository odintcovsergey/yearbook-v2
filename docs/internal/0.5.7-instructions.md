# Инструкция для подэтапа 0.5.7 (закрытие фазы 0.5.3)

**Цель:** Обновить контекст-файл с v40 на v41. Зафиксировать что фаза
0.5.3 закрыта (4 подэтапа реализованы и запушены в main).

Этот подэтап НЕ содержит изменений кода — только обновление документа.

## Шаги

### Шаг 1. Положить готовый файл v41

Файл `yearbook-context-v41.md` подготовлен заранее (Сергей загрузит его в
корень репо). Стратег уже применил все патчи к v40 локально:

- Шапка: версия v41, дата 08.05.2026, описание изменений относительно v40
- Список миграций пополнен `2026-05-08-config-presets-add-grid-base-pages.sql`
- Раздел «Продукт B — фаза 0.5» обновлён со статуса «В РАБОТЕ» на
  «фаза 0.5.3 ЗАВЕРШЕНА ✅», добавлен список 4 подэтапов 0.5.3.X
- Roadmap-таблица: 0.5.3 помечена как сделанная, добавлена 0.5.6 (опц.)
- Структура `lib/album-builder/` обновлена: build.ts и scenarios.ts
  помечены как УДАЛЁН, build-from-preset.ts описан подробно
- Добавлен новый раздел «ПРОДУКТ B — ФАЗА 0.5.3 ЗАВЕРШЕНА ✅» с
  описанием архитектурных принципов (semantic discovery, capacity-pool,
  унификация функций), решением по Универсалу, и критерием
  безрегрессионного рефакторинга (smoke 58/58)
- Правило #7 обновлено: «продукт B изолирован в feature/layout-engine»
  → «продукт B полностью смержен в main»

### Шаг 2. Коммит

Один коммит:

```bash
git add yearbook-context-v41.md docs/internal/0.5.7-instructions.md
git commit -m "docs(context): v41 — фаза 0.5.3 завершена (4 подэтапа), builder работает только с Preset"
```

### Шаг 3. Push (после OK от Сергея)

```bash
git push origin main
```

## Что в результате

- ✅ `yearbook-context-v41.md` существует в корне репо
- ✅ Старые версии v2-v40 НЕ удалены (исторические записи)
- ✅ Любой следующий чат стартует с v41 (актуальное состояние)

## Контрольный список

- [ ] 0.5.3.4 запушен в main (коммит `c242233` на origin/main)
- [ ] Файл `yearbook-context-v41.md` лежит в корне репо
- [ ] Файл `docs/internal/0.5.7-instructions.md` лежит в репо
- [ ] Коммит сделан, ожидание OK на push
