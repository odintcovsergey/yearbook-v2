# Инструкция для подэтапа 1.0 — config_type/print_type в albums

> Это временный файл-инструкция. После выполнения подэтапа удалить и закоммитить вместе с основными изменениями.

## Цель подэтапа

Первый шаг фазы 1 — дать каждому альбому возможность хранить **тип комплектации** и **тип печати**. Это нужно для будущей автосборки (подэтапы 1.1-1.3): builder должен знать как этот конкретный альбом верстать.

**Важно:** в этом подэтапе мы только добавляем поля в БД и расширяем существующее API (`update_album`). UI выбора будет в 1.3 (там же где кнопка «Собрать»). Сейчас UI трогаем минимально — добавляем только показ текущей конфигурации в обзоре альбома (если задана).

Один коммит:
```
feat(layout/1.0): config_type/print_type в albums
```

## Контекст

Из `docs/phase-1-spec.md`:

> **config_type / print_type — в таблице albums**
> Простые поля прямо в `albums`. Если в будущем настроек будет больше — расширим в отдельную таблицу.
> Поля **nullable**. Если не заданы — UI запросит выбор перед первой сборкой.

В `app/api/tenant/route.ts:1271` уже есть allowlist `allowedFields` для `update_album` action — нам надо просто добавить туда два новых поля.

## Файлы к правке

3 элемента:
1. `migrations/2026-05-08-album-config.sql` — **новый файл миграции**
2. `app/api/tenant/route.ts` — расширить `allowedFields` в `update_album`
3. `app/app/page.tsx` — показ текущей конфигурации в обзоре альбома (1-2 строки)

(Никаких изменений в `lib/album-builder/` или smoke — там продолжаем работать с синтетическими параметрами как раньше.)

## Точка старта работы

```bash
git checkout main
git pull origin main
git status  # должно быть чисто
```

HEAD = `6c0902c` (docs phase-1-spec).

---

## 1. `migrations/2026-05-08-album-config.sql`

**Шаг 1.1 — выполнить миграцию через Supabase Studio.** Затем создать файл-копию в репо.

```sql
-- Миграция 1.0 — config_type/print_type в albums.
-- Поля nullable: существующие альбомы получат NULL, новая фаза 1 spec
-- говорит что UI запросит выбор перед первой сборкой.
--
-- CHECK-constraint'ы повторяют валидацию из buildAlbum (lib/album-builder/types.ts).

ALTER TABLE albums
ADD COLUMN config_type text CHECK (
  config_type IS NULL OR config_type IN (
    'standard', 'universal', 'maximum', 'medium',
    'light', 'mini', 'individual'
  )
);

ALTER TABLE albums
ADD COLUMN print_type text CHECK (
  print_type IS NULL OR print_type IN ('layflat', 'soft')
);

-- Проверка
SELECT
  COUNT(*) AS total_albums,
  COUNT(config_type) AS with_config,
  COUNT(print_type) AS with_print
FROM albums;
```

**Применить через Supabase Studio.** Backfill существующих альбомов **не делаем** — они получат NULL, и при попытке собрать через UI пользователь увидит форму выбора.

---

## 2. `app/api/tenant/route.ts`

В функции `update_album` (строка 1260) расширить `allowedFields`:

```typescript
const allowedFields = [
  'title', 'city', 'year', 'deadline',
  'cover_mode', 'cover_price',
  'group_enabled', 'group_min', 'group_max', 'group_exclusive',
  'personal_spread_enabled', 'personal_spread_price', 'personal_spread_min', 'personal_spread_max',
  'text_enabled', 'text_max_chars', 'text_type',
  'classes', 'template_title',
  'config_type', 'print_type',  // ← НОВОЕ (1.0)
]
```

**Валидация значений:** пока полагаемся на CHECK-constraint в БД. Если придёт невалидное значение — Supabase вернёт ошибку, которую endpoint пробросит как 500. Это OK для текущего этапа (UI в 1.3 будет ограничивать значения через select).

---

## 3. `app/app/page.tsx` — минимальный UI

В AlbumDetailModal (строка 664) во вкладке «Обзор» добавить **строку с текущей конфигурацией** под существующими полями альбома (название/город/год/дедлайн).

**Локация:** найди в коде AlbumDetailModal раздел где показываются базовые свойства альбома (где видно title, city, year). Сразу после них или рядом добавь:

```tsx
{album.config_type && album.print_type ? (
  <div className="text-sm text-gray-600">
    Конфигурация вёрстки:{' '}
    <span className="font-mono">{album.config_type}</span>
    {' / '}
    <span className="font-mono">{album.print_type}</span>
  </div>
) : (
  <div className="text-sm text-gray-400 italic">
    Конфигурация вёрстки не задана
  </div>
)}
```

**Тип Album:** добавить в интерфейс альбома (где-то наверху page.tsx определён) поля:
```typescript
config_type?: string | null
print_type?: string | null
```

(Или поищи существующий `interface Album` / `type Album` — обычно это где-то в начале файла или в `types/database.ts`. Расширь его.)

**Не делаем UI выбора в этом подэтапе.** Просто показ. Выбор будет в 1.3 во вкладке «Вёрстка».

---

## Порядок действий

1. **СНАЧАЛА** выполнить SQL-миграцию через Supabase Studio (см. шаг 1)
2. Создать файл миграции в репо: `migrations/2026-05-08-album-config.sql`
3. Расширить allowedFields в `app/api/tenant/route.ts:1271`
4. Найти тип Album в page.tsx (или связанном файле), добавить config_type/print_type
5. В AlbumDetailModal добавить отображение конфигурации (см. секцию 3)
6. `cp .env.local .env.local.real-backup`
7. fake `.env.local`
8. `npx tsc --noEmit --project .` → пусто
9. `npx next build` → зелёный
10. `rm -f .env.local && mv .env.local.real-backup .env.local`
11. Smoke не нужен (мы не трогаем lib/album-builder/), но для подстраховки:
    `set -a && . ./.env.local && set +a && npx tsx scripts/smoke-album-builder.ts`
    → 58/58 (как было)
12. `rm docs/internal/1.0-instructions.md && rmdir docs/internal 2>/dev/null || true`
13. `git add migrations/ app/api/tenant/route.ts app/app/page.tsx docs/internal/`
14. `git status` — должно быть:
    ```
    A  migrations/2026-05-08-album-config.sql
    D  docs/internal/1.0-instructions.md
    M  app/api/tenant/route.ts
    M  app/app/page.tsx
    ```
15. `git commit -m "feat(layout/1.0): config_type/print_type в albums"`
16. **НЕ пушить** — дождись OK Сергея

## Отчёт

- Hash коммита (локальный)
- tsc/build статусы
- Smoke результат (58/58 — как было)
- Размеры файлов в строках (изменения)
- Подтверждение что миграция БД применена

## Анти-цели

- Никакого UI выбора config_type/print_type — это 1.3
- Никакого endpoint для autosave — пользователь использует существующий update_album когда форма выбора появится в 1.3
- Никаких изменений в lib/album-builder/ — там продолжаем синтетику
- Не пытаться угадывать — если что-то падает, репортнуть и ждать
