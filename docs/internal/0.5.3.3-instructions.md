# Инструкция для подэтапа 0.5.3.3

**Цель:** Реализовать ученические секции для 3 grid-комплектаций
(**medium**, **light**, **mini**) и thumbnails-секцию для **individual**.
После 0.5.3.3 в новом builder'е работают все 7 комплектаций.

**Архитектурный путь:** Г1, продолжение. Старый `build.ts` и
`scenarios.ts` остаются параллельно. Smoke 58/58 на старом — обязателен.
Удаление старого — в 0.5.3.4.

---

## Принципиальные решения

### Развилка `buildGridStudents` по `grid_base_pages`

Текущий `build.ts` имеет две разные функции для grid:
- `buildMediumStudents` — fixed grid + last_spread
- `buildAdaptiveGridStudents` — adaptive grid + overflow

В новом builder'е объединяем через dispatcher:

```typescript
function buildGridStudents(ctx, ss) {
  if (ss.grid_base_pages === null) {
    buildFixedGridStudents(ctx, ss);          // Медиум
  } else {
    buildAdaptiveGridStudents(ctx, ss, ss.grid_base_pages);  // Лайт/Мини
  }
}
```

### Семантический discovery вместо декларативного `last_spread`

В текущем `scenarios.ts` `last_spread.left_filter` явно указывает имя
`D-Medium-Last-WithPhoto`. В новом builder'е — поиск через семантику:

```typescript
const lastFilter: MasterFilter = {
  page_role: 'student_last',
  applies_to_config: cfgType,
  slot_capacity_min: { students: remainder },
};
```

- Если мастер найден → используем (поведение эквивалентно текущему `useLastSpecial=true`)
- Если не найден (например remainder=3 при capacity=2 у D-Medium-Last) →
  fallback на обычный grid с null'ами + warning `students_grid_no_special_master`

Это эквивалентно текущему поведению с `remainder_min/max` через `slot_capacity_min`.

### Семантический discovery overflow

Для Лайт/Мини overflow находится через `page_role`:
- `student_overflow` — основной row (Лайт capacity=3, Мини capacity=4)
- `student_overflow_right` — правый row (только для Лайт 31-32 в branch 3)

Без декларативной структуры `overflow.row_filter`, `overflow.row_right_filter`
из текущего scenarios.ts — поиск через findMaster по семантике.

### Thumbnails — переиспользование adaptive grid

`buildThumbnailsSection` использует тот же adaptive grid algorithm, что
Лайт/Мини. Параметры:
- `basePages = Math.ceil(total / preferred_grid_size)`
- `hasQuote = false` (всегда — у thumbnails-мастеров нет quote-слотов)

Через выделение core-функции `buildAdaptiveGridStudentsCore(ctx, basePages, hasQuote)`
оба места (Лайт/Мини и thumbnails) используют общий код.

---

## Шаги

### Шаг 1. Добавить shared helpers в build-from-preset.ts

Перенести из `build.ts` следующие функции с минимальной адаптацией.
Адаптация одна и та же для всех:
- `ctx.config.config_type` → `ctx.cfgType`
- `ctx.config.template_set.spreads` → `ctx.templateSet.spreads`
- `ctx.config.template_set` → `ctx.templateSet`

#### 1.1. `buildGridStudentData` (build.ts:494-516)

Заполняет data-объект для grid-страницы. Студенты идут с `_1.._N`,
пустые слоты — `null`. Универсальный helper для всех grid функций.

```typescript
function buildGridStudentData(
  students: Student[],
  slotsPerPage: number,
  hasQuote: boolean,
): Record<string, string | null>
```

Без адаптаций (не обращается к ctx).

#### 1.2. `pickAdaptiveGridMaxCapacity` (build.ts:599-619)

Возвращает мастер с **максимальной** `slot_capacity.students` среди доступных
для текущего page_role и cfgType. Используется для clamping.

```typescript
function pickAdaptiveGridMaxCapacity(
  ctx: PresetBuildContext,
  baseFilter: MasterFilter,
): SpreadTemplate | null
```

Адаптации:
- `filter.applies_to_config` → `ctx.cfgType` (через spread-merge как в build.ts)
- `ctx.config.template_set.spreads` → `ctx.templateSet.spreads`

#### 1.3. `pickAdaptiveGrid` (build.ts:635-671)

Возвращает мастер с **минимально-достаточной** `slot_capacity.students`
для требуемого `required_capacity = ceil(totalStudents / basePages)`.
Если ни один не подходит — fallback на максимальный + warning
`adaptive_grid_fallback`.

```typescript
function pickAdaptiveGrid(
  ctx: PresetBuildContext,
  baseFilter: MasterFilter,
  totalStudents: number,
  basePages: number,
): SpreadTemplate | null
```

Аналогичные адаптации.

#### 1.4. `buildOverflowRow` (build.ts:844-871)

Сборка overflow-row страницы. Заполняет `studentportrait_N`/`studentname_N`
и опционально `classphotoframe`.

```typescript
function buildOverflowRow(
  ctx: PresetBuildContext,
  master: SpreadTemplate,
  students: Student[],
  rowCapacity: number,
): void
```

Адаптации: `ctx.config` → `ctx`, `ctx.input.common_photos` остаётся.

#### 1.5. `hasPlaceholderPrefix` — НОВАЯ функция

Проверка, есть ли в мастере placeholder с заданным префиксом
(нужно для определения `hasQuote` мастера last_spread по-семантически):

```typescript
function hasPlaceholderPrefix(master: SpreadTemplate, prefix: string): boolean {
  for (let i = 0; i < master.placeholders.length; i++) {
    if (master.placeholders[i].label.indexOf(prefix) === 0) return true;
  }
  return false;
}
```

### Шаг 2. Заменить заглушку `buildGridStudents` диспетчером

```typescript
function buildGridStudents(
  ctx: PresetBuildContext,
  ss: StudentSectionConfig,
): void {
  if (ss.grid_base_pages === null) {
    buildFixedGridStudents(ctx, ss);
  } else {
    buildAdaptiveGridStudents(ctx, ss);
  }
}
```

### Шаг 3. Реализовать `buildFixedGridStudents` (Медиум)

Логика — обобщение `buildMediumStudents` (build.ts:414-485) с заменой
декларативного `last_spread` на семантический discovery.

```typescript
function buildFixedGridStudents(
  ctx: PresetBuildContext,
  ss: StudentSectionConfig,
): void {
  const cfgType = ctx.cfgType;
  const total = ctx.input.students.length;
  const hasQuote = ss.first_spread_content.text?.enabled === true;

  // 3.1. Найти left + right grid мастера
  const leftR = findMaster(ctx.templateSet.spreads, {
    page_role: 'student_grid_left',
    applies_to_config: cfgType,
  });
  const rightR = findMaster(ctx.templateSet.spreads, {
    page_role: 'student_grid_right',
    applies_to_config: cfgType,
  });
  if (!leftR.ok) {
    pushWarning(ctx, {
      code: 'master_not_found',
      detail: `fixed_grid student_grid_left for ${cfgType}`,
    });
    return;
  }
  if (!rightR.ok) {
    pushWarning(ctx, {
      code: 'master_not_found',
      detail: `fixed_grid student_grid_right for ${cfgType}`,
    });
    return;
  }
  if (leftR.warning) pushWarning(ctx, leftR.warning);
  if (rightR.warning) pushWarning(ctx, rightR.warning);

  // 3.2. Capacity из найденного left мастера
  const N = leftR.master.slot_capacity?.students ?? 0;
  if (N < 1) {
    pushWarning(ctx, {
      code: 'master_not_found',
      detail: `fixed_grid: ${leftR.master.name} has slot_capacity.students<1`,
    });
    return;
  }

  // 3.3. Last_spread semantic discovery
  const remainder = total % N;
  let lastLeftMaster: SpreadTemplate | null = null;
  let lastSlotsPerPage = 0;
  let lastHasQuote = false;

  if (remainder > 0) {
    const lastR = findMaster(ctx.templateSet.spreads, {
      page_role: 'student_last',
      applies_to_config: cfgType,
      slot_capacity_min: { students: remainder },
    });
    if (lastR.ok) {
      lastLeftMaster = lastR.master;
      lastSlotsPerPage = lastR.master.slot_capacity?.students ?? 0;
      // hasQuote determined dynamically through placeholders prefix
      lastHasQuote = hasPlaceholderPrefix(lastR.master, 'studentquote_');
      if (lastR.warning) pushWarning(ctx, lastR.warning);
    }
  }

  const useLastSpecial = lastLeftMaster !== null;
  const regularPages = useLastSpecial ? Math.floor(total / N) : Math.ceil(total / N);

  // 3.4. Заполняем regular pages с чередованием Left/Right
  for (let i = 0; i < regularPages; i++) {
    const slice = ctx.input.students.slice(i * N, (i + 1) * N);
    const isLeft = i % 2 === 0;
    const master = isLeft ? leftR.master : rightR.master;

    if (slice.length < N) {
      pushWarning(ctx, {
        code: 'students_grid_no_special_master',
        detail: `${cfgType}: remainder=${remainder} учеников на последней странице (${master.name}) — нет специального мастера, пустые слоты заполнены null (см. master-cleanup-tz §A5)`,
      });
    }

    ctx.spreads.push({
      spread_index: ctx.spreadCounter.value++,
      template_id: master.id,
      template_name: master.name,
      data: buildGridStudentData(slice, N, hasQuote),
    });
  }

  // 3.5. Special last spread (если найден семантически)
  if (useLastSpecial && lastLeftMaster) {
    const consumed = regularPages * N;
    const lastSlice = ctx.input.students.slice(consumed);

    // Левая страница: ученики + опционально classphotoframe
    const leftData = buildGridStudentData(lastSlice, lastSlotsPerPage, lastHasQuote);
    if (hasPlaceholder(lastLeftMaster, 'classphotoframe')) {
      const fc = ctx.input.common_photos.full_class;
      if (fc.length >= 1) {
        leftData.classphotoframe = fc[0];
      } else {
        leftData.classphotoframe = null;
        pushWarning(ctx, {
          code: 'class_photo_missing',
          detail: `master ${lastLeftMaster.name} has classphotoframe placeholder but common_photos.full_class is empty`,
        });
      }
    }

    ctx.spreads.push({
      spread_index: ctx.spreadCounter.value++,
      template_id: lastLeftMaster.id,
      template_name: lastLeftMaster.name,
      data: leftData,
    });

    // Правая страница: dynamic G-* через pickRightCommonPhotoMaster
    const rightMaster = pickRightCommonPhotoMaster(ctx);
    if (!rightMaster) {
      pushWarning(ctx, {
        code: 'no_right_teacher_master',
        detail: `last_spread right: ни half (>=2) ни full_class (>=1) — правая страница пропущена`,
      });
      return;
    }
    ctx.spreads.push({
      spread_index: ctx.spreadCounter.value++,
      template_id: rightMaster.id,
      template_name: rightMaster.name,
      data: buildTeacherRightData(ctx, rightMaster, []),
    });
  }
}
```

### Шаг 4. Реализовать `buildAdaptiveGridStudentsCore` (Лайт/Мини общая часть)

Перенос логики `buildAdaptiveGridStudents` из build.ts:685-833 с
заменой декларативного `section.overflow.*_filter` на семантический
discovery `findMaster({page_role: 'student_overflow' | 'student_overflow_right'})`.

```typescript
function buildAdaptiveGridStudentsCore(
  ctx: PresetBuildContext,
  basePages: number,
  hasQuote: boolean,
): void {
  if (basePages < 1) {
    pushWarning(ctx, {
      code: 'master_not_found',
      detail: `adaptive_grid: basePages must be >= 1, got ${basePages}`,
    });
    return;
  }

  const cfgType = ctx.cfgType;
  const total = ctx.input.students.length;

  const baseLeftFilter: MasterFilter = {
    page_role: 'student_grid_left',
    applies_to_config: cfgType,
  };
  const baseRightFilter: MasterFilter = {
    page_role: 'student_grid_right',
    applies_to_config: cfgType,
  };

  // 4.1. Clamping через max-capacity мастер
  const maxLeftMaster = pickAdaptiveGridMaxCapacity(ctx, baseLeftFilter);
  if (!maxLeftMaster) {
    pushWarning(ctx, {
      code: 'master_not_found',
      detail: `adaptive_grid LEFT: no candidates for cfgType=${cfgType}`,
    });
    return;
  }
  const maxCapacityPerPage = maxLeftMaster.slot_capacity?.students ?? 0;
  if (maxCapacityPerPage < 1) {
    pushWarning(ctx, {
      code: 'master_not_found',
      detail: `adaptive_grid LEFT: max master ${maxLeftMaster.name} has slot_capacity.students<1`,
    });
    return;
  }
  const totalForBase = Math.min(total, basePages * maxCapacityPerPage);

  // 4.2. Подбор минимально-достаточных мастеров
  const leftMaster = pickAdaptiveGrid(ctx, baseLeftFilter, totalForBase, basePages);
  const rightMaster = pickAdaptiveGrid(ctx, baseRightFilter, totalForBase, basePages);
  if (!leftMaster || !rightMaster) {
    pushWarning(ctx, {
      code: 'master_not_found',
      detail: `adaptive_grid: missing left or right master for ${cfgType}`,
    });
    return;
  }

  const slotsPerPage = leftMaster.slot_capacity?.students ?? 0;
  if (slotsPerPage < 1) {
    pushWarning(ctx, {
      code: 'master_not_found',
      detail: `adaptive_grid LEFT: ${leftMaster.name} has slot_capacity.students<1`,
    });
    return;
  }

  const maxOnBase = basePages * slotsPerPage;

  // 4.3. Базовые страницы (с чередованием Left/Right)
  const baseStudents = ctx.input.students.slice(0, Math.min(total, maxOnBase));
  for (let i = 0; i < basePages; i++) {
    const slice = baseStudents.slice(i * slotsPerPage, (i + 1) * slotsPerPage);
    if (slice.length === 0) break;
    const isLeft = i % 2 === 0;
    const master = isLeft ? leftMaster : rightMaster;
    ctx.spreads.push({
      spread_index: ctx.spreadCounter.value++,
      template_id: master.id,
      template_name: master.name,
      data: buildGridStudentData(slice, slotsPerPage, hasQuote),
    });
  }

  // 4.4. Overflow
  const overflowCount = total - maxOnBase;
  if (overflowCount <= 0) return;

  // Семантический discovery overflow_row
  const rowR = findMaster(ctx.templateSet.spreads, {
    page_role: 'student_overflow',
    applies_to_config: cfgType,
  });
  if (!rowR.ok) {
    pushWarning(ctx, {
      code: 'students_overflow',
      detail: `${cfgType}: total=${total} > maxOnBase=${maxOnBase}, overflow strategy не задана (нет мастера student_overflow)`,
    });
    return;
  }
  if (rowR.warning) pushWarning(ctx, rowR.warning);
  const rowCapacity = rowR.master.slot_capacity?.students ?? 0;

  const overflowStudents = ctx.input.students.slice(maxOnBase);

  if (overflowCount <= rowCapacity) {
    // Branch 1: single overflow-row
    buildOverflowRow(ctx, rowR.master, overflowStudents, rowCapacity);
  } else if (overflowCount <= slotsPerPage) {
    // Branch 2: extra обычная сетка с null'ами
    ctx.spreads.push({
      spread_index: ctx.spreadCounter.value++,
      template_id: leftMaster.id,
      template_name: leftMaster.name,
      data: buildGridStudentData(overflowStudents, slotsPerPage, hasQuote),
    });
  } else {
    // Branch 3: full сетка + overflow_row_right (только Лайт 31-32)
    const rightRowR = findMaster(ctx.templateSet.spreads, {
      page_role: 'student_overflow_right',
      applies_to_config: cfgType,
    });
    if (!rightRowR.ok) {
      pushWarning(ctx, {
        code: 'master_not_found',
        detail: `overflow grid_plus_row: no student_overflow_right master, overflowCount=${overflowCount}`,
      });
      return;
    }
    if (rightRowR.warning) pushWarning(ctx, rightRowR.warning);

    const fullSlice = overflowStudents.slice(0, slotsPerPage);
    ctx.spreads.push({
      spread_index: ctx.spreadCounter.value++,
      template_id: leftMaster.id,
      template_name: leftMaster.name,
      data: buildGridStudentData(fullSlice, slotsPerPage, hasQuote),
    });

    const remainSlice = overflowStudents.slice(slotsPerPage);
    const rightRowCapacity = rightRowR.master.slot_capacity?.students ?? rowCapacity;
    buildOverflowRow(ctx, rightRowR.master, remainSlice, rightRowCapacity);
  }
}
```

### Шаг 5. Обёртка `buildAdaptiveGridStudents` для основной student_section

```typescript
function buildAdaptiveGridStudents(
  ctx: PresetBuildContext,
  ss: StudentSectionConfig,
): void {
  if (ss.grid_base_pages === null) {
    // Защитная проверка — buildGridStudents dispatcher должен был
    // отправить в buildFixedGridStudents
    pushWarning(ctx, {
      code: 'master_not_found',
      detail: 'buildAdaptiveGridStudents called with grid_base_pages=null',
    });
    return;
  }
  const hasQuote = ss.first_spread_content.text?.enabled === true;
  buildAdaptiveGridStudentsCore(ctx, ss.grid_base_pages, hasQuote);
}
```

### Шаг 6. Реализовать `buildThumbnailsSection`

Заменяет TODO-заглушку. Использует `buildAdaptiveGridStudentsCore`.

```typescript
function buildThumbnailsSection(
  ctx: PresetBuildContext,
  ss: StudentSectionConfig,
): void {
  const ts = ss.thumbnails_section;
  if (!ts || !ts.enabled) return;

  const total = ctx.input.students.length;
  const preferred = ts.preferred_grid_size;
  if (preferred < 1) {
    pushWarning(ctx, {
      code: 'master_not_found',
      detail: `thumbnails: preferred_grid_size must be >= 1, got ${preferred}`,
    });
    return;
  }

  const basePages = Math.ceil(total / preferred);
  if (basePages < 1) return;

  // hasQuote=false: thumbnails-мастера не имеют quote-слотов
  buildAdaptiveGridStudentsCore(ctx, basePages, false);
}
```

### Шаг 7. Что НЕ трогать

- `lib/album-builder/build.ts` — продолжает работать
- `lib/album-builder/scenarios.ts` — продолжает работать
- `lib/album-builder/index.ts` — новый builder всё ещё не экспортирован
- `scripts/smoke-album-builder.ts` — продолжает тестировать старый
- `app/api/layout/route.ts` — без изменений
- `find-master.ts`, `utils.ts`, `types.ts` — не трогаем

### Шаг 8. Проверка

#### 8.1. TypeScript
```bash
npx tsc --noEmit --project .
```

#### 8.2. Build
```bash
cat > .env.local <<'EOF'
NEXT_PUBLIC_SUPABASE_URL=https://fake.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=fake
SUPABASE_SERVICE_ROLE_KEY=fake
ADMIN_SECRET=fake
JWT_SECRET=fake_jwt_secret_for_build_only_32chars
DEFAULT_TENANT_ID=00000000-0000-0000-0000-000000000000
EOF
npx next build
rm -f .env.local
```

#### 8.3. Smoke на старом builder'е
```bash
set -a && . ./.env.local && set +a && npx tsx scripts/smoke-album-builder.ts 2>&1 | tail -3
```
Ожидание: `Result: 58/58 scenes passed`.

#### 8.4. Ad-hoc тест нового builder'а

Создать **временный** `/tmp/test-build-from-preset-0533.ts` (НЕ коммитить).
Расширенный набор сцен (все 7 комплектаций).

```typescript
import { createClient } from '@supabase/supabase-js';
import { buildAlbumFromPreset } from '../lib/album-builder/build-from-preset';
import { loadTemplateSet } from '../lib/album-builder/load-template-set';
import type { AlbumInput, Preset, Student } from '../lib/album-builder/types';

const supabase = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.SUPABASE_SERVICE_ROLE_KEY!,
);

async function loadPreset(slug: string): Promise<Preset> {
  const { data, error } = await supabase.from('config_presets')
    .select('*').eq('slug', slug).single();
  if (error || !data) throw new Error(`preset ${slug} not found`);
  return data as Preset;
}

function makeStudent(name: string, friendCount: number): Student {
  return {
    full_name: name,
    quote: `Цитата ${name}`,
    portrait: `https://fake/${name}-portrait.jpg`,
    friend_photos: Array.from({ length: friendCount }, (_, i) => `https://fake/${name}-f${i}.jpg`),
  };
}

const EMPTY_COMMON = { full_class: [], half: [], quarter: [], sixth: [], collage: [] };
const ONE_FULL_CLASS = { full_class: ['https://fake/c.jpg'], half: [], quarter: [], sixth: [], collage: [] };

function students(n: number, fcount: number = 0): Student[] {
  return Array.from({ length: n }, (_, i) => makeStudent(`У${i+1}`, fcount));
}

type Scene = {
  label: string;
  presetSlug: string;
  students: Student[];
  commonPhotos?: typeof EMPTY_COMMON;
  withHeadTeacher?: boolean;
  expect: {
    spreadsCount?: number;
    spreadsCountAtLeast?: number;
    masterNameSequence?: string[];
    warningCodes?: string[];
    noWarningCodes?: string[];
  };
};

const SCENES: Scene[] = [
  // ─── Грид-комплектации ──────────────────────────────────────
  {
    label: 'medium-layflat / 4 студента (без остатка)',
    presetSlug: 'medium-layflat',
    students: students(4),
    expect: {
      spreadsCount: 1,
      masterNameSequence: ['D-Medium-Left'],
      noWarningCodes: ['master_not_found'],
    },
  },
  {
    label: 'medium-layflat / 5 студентов (с last_spread, common_photos.full_class=1)',
    presetSlug: 'medium-layflat',
    students: students(5),
    commonPhotos: ONE_FULL_CLASS,
    expect: {
      spreadsCount: 3,
      // [D-Medium-Left, D-Medium-Last-WithPhoto, G-FullClass]
      masterNameSequence: ['D-Medium-Left', 'D-Medium-Last-WithPhoto', 'G-FullClass'],
      noWarningCodes: ['master_not_found'],
    },
  },
  {
    label: 'medium-layflat / 7 студентов (remainder=3, нет last_spread)',
    presetSlug: 'medium-layflat',
    students: students(7),
    expect: {
      spreadsCount: 2,
      // [D-Medium-Left (4), D-Medium-Right (3 + 1 null)]
      masterNameSequence: ['D-Medium-Left', 'D-Medium-Right'],
      warningCodes: ['students_grid_no_special_master'],
    },
  },
  {
    label: 'light-layflat / 8 студентов (≤24, base_pages=4)',
    presetSlug: 'light-layflat',
    students: students(8),
    expect: {
      spreadsCountAtLeast: 1,
      noWarningCodes: ['master_not_found'],
    },
  },
  {
    label: 'light-layflat / 24 студента (полный base)',
    presetSlug: 'light-layflat',
    students: students(24),
    expect: {
      spreadsCount: 4,
      // [L-6-Left, L-6-Right, L-6-Left, L-6-Right]
      masterNameSequence: ['L-6-Left', 'L-6-Right', 'L-6-Left', 'L-6-Right'],
      noWarningCodes: ['master_not_found'],
    },
  },
  {
    label: 'light-layflat / 27 студентов (overflow branch 1: single row)',
    presetSlug: 'light-layflat',
    students: students(27),
    expect: {
      // 4 базовые (L-6 × 4) + 1 overflow_row (3 студ.)
      spreadsCount: 5,
      noWarningCodes: ['master_not_found'],
    },
  },
  {
    label: 'mini-layflat / 24 студента (полный base, base_pages=2)',
    presetSlug: 'mini-layflat',
    students: students(24),
    expect: {
      spreadsCount: 2,
      // [N-12-Left, N-12-Right]
      masterNameSequence: ['N-12-Left', 'N-12-Right'],
      noWarningCodes: ['master_not_found'],
    },
  },

  // ─── Individual + thumbnails (новое в 0.5.3.3) ──────────────
  {
    label: 'individual-layflat / 4 студента (0 фото) + thumbnails',
    presetSlug: 'individual-layflat',
    students: students(4, 0),
    expect: {
      // 4 × (E-Max-Left + E-Ind-Right-3) = 8 spreads
      // + thumbnails: ceil(4/12)=1 страница (одна сторона)
      // итого 9 spreads
      spreadsCountAtLeast: 8,
      noWarningCodes: ['master_not_found'],
    },
  },

  // ─── Регрессия предыдущих подэтапов ─────────────────────────
  {
    label: 'standard-layflat / 5 студентов',
    presetSlug: 'standard-layflat',
    students: students(5, 0),
    expect: {
      spreadsCount: 3,
      masterNameSequence: ['E-Student-Standard', 'E-Student-Standard', 'E-Student-Standard'],
      warningCodes: ['students_odd_in_standard'],
      noWarningCodes: ['master_not_found'],
    },
  },
  {
    label: 'maximum-layflat / 3 студента по 4 фото',
    presetSlug: 'maximum-layflat',
    students: students(3, 4),
    expect: {
      spreadsCount: 6,
      masterNameSequence: [
        'E-Max-Left', 'E-Max-Right',
        'E-Max-Left', 'E-Max-Right',
        'E-Max-Left', 'E-Max-Right',
      ],
      noWarningCodes: ['master_not_found'],
    },
  },
];

async function main() {
  const ts = await loadTemplateSet(supabase);
  console.log(`template_set okeybook-default: ${ts.spreads.length} spreads`);
  console.log('');

  let passed = 0;
  for (const scene of SCENES) {
    console.log(`=== ${scene.label} ===`);
    const preset = await loadPreset(scene.presetSlug);
    const input: AlbumInput = {
      template_set_id: ts.id,
      head_teacher: scene.withHeadTeacher ? {
        name: 'Иванова', role: 'кл. рук.', text: 'текст', photo: 'https://fake/h.jpg',
      } : null,
      subjects: [],
      students: scene.students,
      common_photos: scene.commonPhotos ?? EMPTY_COMMON,
    };

    const result = buildAlbumFromPreset(input, preset, ts);
    console.log(`  spreads: ${result.spreads.length}`);
    result.spreads.forEach(s => console.log(`    [${s.spread_index}] ${s.template_name}`));
    if (result.warnings.length > 0) {
      console.log(`  warnings: ${result.warnings.length}`);
      result.warnings.forEach(w => console.log(`    ! ${w.code}: ${w.detail}`));
    }

    let ok = true;
    if (scene.expect.spreadsCount !== undefined && result.spreads.length !== scene.expect.spreadsCount) {
      console.log(`  ❌ spreadsCount expected=${scene.expect.spreadsCount} got=${result.spreads.length}`);
      ok = false;
    }
    if (scene.expect.spreadsCountAtLeast !== undefined && result.spreads.length < scene.expect.spreadsCountAtLeast) {
      console.log(`  ❌ spreadsCountAtLeast expected>=${scene.expect.spreadsCountAtLeast} got=${result.spreads.length}`);
      ok = false;
    }
    if (scene.expect.masterNameSequence) {
      const got = result.spreads.map(s => s.template_name);
      const exp = scene.expect.masterNameSequence;
      if (got.length !== exp.length || !got.every((n, i) => n === exp[i])) {
        console.log(`  ❌ sequence expected=${JSON.stringify(exp)} got=${JSON.stringify(got)}`);
        ok = false;
      }
    }
    const codes = result.warnings.map(w => w.code);
    scene.expect.warningCodes?.forEach(c => {
      if (codes.indexOf(c) < 0) {
        console.log(`  ❌ expected warning '${c}' not found`);
        ok = false;
      }
    });
    scene.expect.noWarningCodes?.forEach(c => {
      if (codes.indexOf(c) >= 0) {
        console.log(`  ❌ unexpected warning '${c}'`);
        ok = false;
      }
    });
    if (ok) {
      console.log(`  ✅ passed`);
      passed++;
    }
    console.log('');
  }

  console.log(`Result: ${passed}/${SCENES.length} scenes passed`);
  process.exit(passed === SCENES.length ? 0 : 1);
}

main().catch(e => { console.error(e); process.exit(1); });
```

Запуск:
```bash
set -a && . ./.env.local && set +a && npx tsx /tmp/test-build-from-preset-0533.ts
```

Ожидание: **10/10 scenes passed**.

После проверки удалить:
```bash
rm -f /tmp/test-build-from-preset-0533.ts
```

#### 8.5. Возможные расхождения

Тестовые ожидания построены на следующих допущениях БД:
- `D-Medium-Last-WithPhoto.slot_capacity.students = 2`
- `L-6.slot_capacity.students = 6`
- `N-12.slot_capacity.students = 12`
- `L-Overflow-Row.slot_capacity.students = 3`
- `D-Medium-Last-WithPhoto.default_for_configs` включает `medium`
- `L-Overflow-Row.default_for_configs` включает `light`
- `N-12-Left/Right.default_for_configs` включает `mini` и `individual`

Если какой-то assert не сходится — пришли вывод полностью, разберёмся
по фактическим значениям из БД (миграции 0.11.1.5, 0.11.3 могли
поменять состояние).

### Шаг 9. Коммиты

#### 9.1. docs-коммит
```bash
git add docs/internal/0.5.3.3-instructions.md
git commit -m "docs(layout/0.5.3.3): инструкция buildGridStudents + buildThumbnailsSection (semantic discovery)"
```

#### 9.2. feat-коммит
```bash
git add lib/album-builder/build-from-preset.ts
git commit -m "feat(layout/0.5.3.3): buildGridStudents (medium fixed + light/mini adaptive) + thumbnails"
git status  # должно быть чисто
```

#### 9.3. Push (после OK от Сергея)
```bash
git push origin main
```

---

## Что в результате 0.5.3.3

- ✅ `buildGridStudents` диспетчер по `grid_base_pages`
- ✅ `buildFixedGridStudents` (Медиум) с semantic discovery `student_last`
- ✅ `buildAdaptiveGridStudents` обёртка над core
- ✅ `buildAdaptiveGridStudentsCore` (Лайт/Мини) с semantic discovery
  `student_overflow` / `student_overflow_right`
- ✅ `buildThumbnailsSection` (Индивидуальный) через core
- ✅ Все 7 комплектаций работают в новом builder'е
- ✅ Старый builder продолжает работать, smoke 58/58 зелёный
- ✅ Ad-hoc 10/10 сцен на новом builder'е

## Что в следующем подэтапе (0.5.3.4)

- Замена `buildAlbum` export → `buildAlbumFromPreset`
- Удаление `lib/album-builder/scenarios.ts` и старого build.ts
- Адаптация `index.ts` (без SCENARIOS, ScenarioDef)
- Адаптация `scripts/smoke-album-builder.ts` (configType → presetSlug,
  загрузка preset через `loadPresetBySlug` helper)
- Адаптация `app/api/layout/route.ts` (action=build_album_test) под
  preset_slug
- Полный smoke 58/58 на новом builder'е

---

## Контрольный список

### Перед стартом
- [ ] 0.5.3.2 запушен в main, два коммита `4fcbf1b` + `4038fb5` на origin/main
- [ ] Vercel задеплоился без ошибок

### После применения
- [ ] Helpers перенесены: `buildGridStudentData`, `pickAdaptiveGridMaxCapacity`,
      `pickAdaptiveGrid`, `buildOverflowRow`, `hasPlaceholderPrefix`
- [ ] `buildGridStudents` dispatcher реализован
- [ ] `buildFixedGridStudents` реализован (semantic last_spread)
- [ ] `buildAdaptiveGridStudentsCore` реализован (semantic overflow)
- [ ] `buildAdaptiveGridStudents` wrapper реализован
- [ ] `buildThumbnailsSection` реализован (через core)
- [ ] Stubs `buildGridStudents` / `buildThumbnailsSection` удалены
- [ ] `npx tsc --noEmit --project .` чисто
- [ ] `npx next build` зелёный
- [ ] Smoke на старом builder'е 58/58
- [ ] Ad-hoc тест 10/10 на новом builder'е
- [ ] Два коммита локально, ожидание OK на push
