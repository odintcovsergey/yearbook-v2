# Инструкция для подэтапа 0.10b.3

> Это временный файл-инструкция. После выполнения подэтапа удалить и закоммитить вместе с основными изменениями.

## Цель подэтапа

Реализовать комплектацию **Медиум** (`config_type: 'medium'`) — четвёртую layflat-комплектацию.

Особенности Медиума:
- Сетка по 4 ученика на страницу — D-Medium-Left / D-Medium-Right с чередованием
- При остатке 1-2 ученика — спецмастер D-Medium-Last-WithPhoto + динамический G-* (HalfClass/FullClass) на правой странице
- При остатке 3 — обычная D-Medium-Right с тремя заполненными слотами + 1 null + warning (отсутствующий мастер `D-Medium-Last-3-Centered`, см. master-cleanup-tz §A5 — отложен на P1)

Один коммит:
```
feat(layout/0.10b.3): комплектация Медиум (D-Medium-* + last-разворот)
```

## Ключевые факты из build_album.jsx (строки 558-591, 1307-1359, 2071-2102)

**Логика для Медиум:**
- `mFullPages = floor(students.length / 4)` — обычные страницы по 4 ученика
- `mRemainder = students.length % 4`
- Чередование Left/Right по чётности индекса страницы
- Если remainder 1-2 И мастер D-Medium-Last-WithPhoto доступен → пара (D-Medium-Last-WithPhoto + dynamic G)
- Если remainder 3 → обычная D-Medium-Right с пустыми слотами

**Заполнение data:**
- D-Medium-Left/Right (4 ученика): `studentportrait_N`, `studentname_N`, `studentquote_N` для N=1..4 (lowercase, как и везде)
- D-Medium-Last-WithPhoto (1-2 ученика): `studentportrait_1..2`, `studentname_1..2`, `studentquote_1..2`, `classphotoframe`
- G-HalfClass: `halfleftphoto`, `halfrightphoto`
- G-FullClass: `classphotoframe`

**State в БД (проверено SELECT'ом):**
- D-Medium-Left/Right: `slot_capacity = {students: 4}`, page_role `student_grid_left/student_grid_right`
- D-Medium-Last-WithPhoto: `slot_capacity = {students: 2, photos_full: 1}`, page_role `student_last`
- Все три: `applies_to_configs = ['medium']`, `is_fallback = false`

## Файлы к правке

5 файлов:
1. `lib/album-builder/types.ts` — добавить `BuildWarningCode` `students_grid_no_special_master`
2. `lib/album-builder/scenarios.ts` — новый тип `LastSpread`, расширить `StudentSection`, добавить новый режим `'grid_alternate'`, добавить запись `medium` в `SCENARIOS_LAYFLAT`
3. `lib/album-builder/build.ts` — переименовать `pickRightTeacherMaster` → `pickRightCommonPhotoMaster`, добавить `buildMediumStudents`, расширить switch в `buildStudentsSection`
4. `lib/album-builder/index.ts` — экспорты типа `LastSpread`
5. `scripts/smoke-album-builder.ts` — добавить сцены для Медиум

---

## 1. `lib/album-builder/types.ts`

В `BuildWarningCode` добавить один новый код:

```typescript
| 'students_grid_no_special_master';
```

В JSDoc типа добавить описание:
```
 * - `students_grid_no_special_master` — в grid-комплектации остался остаток
 *                                       (например 3 в Медиум) для которого
 *                                       нет специального мастера; используется
 *                                       обычный мастер с пустыми слотами
 *                                       (см. master-cleanup-tz §A5 для D-Medium)
```

---

## 2. `lib/album-builder/scenarios.ts`

### 2.1 Новый тип `LastSpread`

Добавить ПОСЛЕ типа `MasterFilter`:

```typescript
/**
 * Конфигурация специального последнего разворота для grid-комплектаций (Медиум).
 *
 * Применяется когда в обычном grid-потоке остался остаток учеников в диапазоне
 * [remainder_min, remainder_max]. Например, для Медиум: при остатке 1-2 ученика
 * используется D-Medium-Last-WithPhoto на левой странице + динамический G-*
 * мастер (HalfClass/FullClass/null) на правой.
 *
 * Если remainder вне диапазона — last_spread не активируется, остаток ложится
 * на обычный grid-мастер с пустыми слотами + warning students_grid_no_special_master.
 */
export type LastSpread = {
  /** Минимум remainder для активации (включительно). */
  remainder_min: number;
  /** Максимум remainder для активации (включительно). */
  remainder_max: number;
  /** Фильтр для левого мастера (например D-Medium-Last-WithPhoto). */
  left_filter: MasterFilter;
  /** Сколько учеников помещается в left мастере (например 2 для D-Medium-Last-WithPhoto). */
  left_slots_per_page: number;
  /** Содержит ли left мастер слоты для цитаты. */
  left_has_quote: boolean;
  /**
   * Использовать ли pickRightCommonPhotoMaster для правой страницы.
   * - true → динамический выбор G-HalfClass/G-FullClass/null по common_photos
   * - false → правая страница не генерируется
   */
  right_dynamic: boolean;
};
```

### 2.2 Расширить `StudentSection`

Добавить новый вариант в `right_filter_mode` и три новых поля:

```typescript
export type StudentSection = {
  students_per_unit: number;
  unit_is_spread: boolean;
  student_master_filter: MasterFilter;
  student_master_filter_right?: MasterFilter;
  /**
   * Режим использования second filter:
   * - 'alternate'      — Универсал: разные ученики Left/Right по чётности
   * - 'paired'         — Максимум: один ученик на двух страницах разворота
   * - 'grid_alternate' — Медиум: chunk по N учеников, чередование Left/Right
   *                      по чётности индекса страницы (НЕ ученика)
   */
  right_filter_mode?: 'alternate' | 'paired' | 'grid_alternate';
  /**
   * Содержат ли мастера grid-комплектации слот для цитаты (studentquote_N).
   * По умолчанию false. true для Медиум.
   */
  has_quote?: boolean;
  /**
   * Конфигурация специального последнего разворота. Применяется только при
   * right_filter_mode === 'grid_alternate'. По умолчанию undefined.
   */
  last_spread?: LastSpread;
};
```

### 2.3 Добавить `medium` в SCENARIOS_LAYFLAT

В `SCENARIOS_LAYFLAT` добавить четвёртую запись:

```typescript
medium: {
  config_type: 'medium',
  print_type: 'layflat',
  description:
    'Медиум — сетка 4 ученика на странице (D-Medium-Left/Right с чередованием), последняя при остатке 1-2 — D-Medium-Last-WithPhoto + dynamic G-*',
  student_section: {
    students_per_unit: 4,
    unit_is_spread: false,
    has_quote: true,
    student_master_filter: {
      page_role: 'student_grid_left',
      applies_to_config: 'medium',
      slot_capacity_min: { students: 4 },
      expected_name_hint: 'D-Medium-Left',
    },
    student_master_filter_right: {
      page_role: 'student_grid_right',
      applies_to_config: 'medium',
      slot_capacity_min: { students: 4 },
      expected_name_hint: 'D-Medium-Right',
    },
    right_filter_mode: 'grid_alternate',
    last_spread: {
      remainder_min: 1,
      remainder_max: 2,
      left_filter: {
        page_role: 'student_last',
        applies_to_config: 'medium',
        slot_capacity_min: { students: 2, photos_full: 1 },
        expected_name_hint: 'D-Medium-Last-WithPhoto',
      },
      left_slots_per_page: 2,
      left_has_quote: true,
      right_dynamic: true,
    },
  },
  teacher_section: TEACHER_SECTION_LAYFLAT,
},
```

### 2.4 Экспорт `LastSpread`

Добавить в существующие `export type` блоки.

---

## 3. `lib/album-builder/build.ts`

### 3.1 Переименовать `pickRightTeacherMaster` → `pickRightCommonPhotoMaster`

Имя устаревшее — функция используется не только в учительском разделе. JSDoc обновить:

```typescript
/**
 * Подбор правого мастера с общим фото класса.
 *
 * Используется в двух местах:
 *  1. Учительский раздел (variants 0-8 без явного right_filter)
 *  2. Медиум last_spread.right_dynamic (после D-Medium-Last-WithPhoto)
 *
 * Логика из build_album.jsx (pickRightPhotoMaster):
 *   1) common_photos.half >= 2  → G-HalfClass
 *   2) common_photos.full_class >= 1 → G-FullClass
 *   3) иначе → null
 *
 * Фильтры включают slot_capacity_min для дискриминации:
 *   G-HalfClass:  slot_capacity_min: { photos_half: 2 }
 *   G-FullClass:  slot_capacity_min: { photos_full: 1 }
 *
 * Внутренние warning'и (fallback/name_mismatch) пушатся в ctx. Если мастер
 * не нашёлся — молча идём к следующей ветке. Финальный warning о пропущенной
 * правой странице пишется на уровне вызывающей стороны.
 */
function pickRightCommonPhotoMaster(ctx: BuildContext): SpreadTemplate | null {
  // ... тело без изменений (slot_capacity_min уже добавлены в фиксе 0.10b.2)
}
```

В двух местах `buildTeacherSection` обновить вызов:
```typescript
rightMaster = pickRightCommonPhotoMaster(ctx);
```

### 3.2 Расширить switch в `buildStudentsSection`

Добавить ветку для `'grid_alternate'`:

```typescript
function buildStudentsSection(ctx: BuildContext, section: StudentSection): void {
  const mode = section.right_filter_mode;
  if (mode === undefined) {
    buildStandardStudents(ctx, section);
  } else if (mode === 'alternate') {
    buildUniversalStudents(ctx, section);
  } else if (mode === 'paired') {
    buildMaximumStudents(ctx, section);
  } else if (mode === 'grid_alternate') {
    buildMediumStudents(ctx, section);
  } else {
    pushWarning(ctx, {
      code: 'master_not_found',
      detail: `unknown right_filter_mode=${String(mode)}`,
    });
  }
}
```

### 3.3 Новая функция `buildMediumStudents`

Добавить ПОСЛЕ `buildMaximumStudents`:

```typescript
/**
 * Медиум — сетка по N учеников на странице с чередованием Left/Right по чётности
 * индекса страницы (не ученика, как в Универсале).
 *
 * Полные страницы заполняются обычными мастерами (D-Medium-Left/D-Medium-Right).
 * Остаток обрабатывается по правилу:
 *   - remainder в [last_spread.remainder_min, last_spread.remainder_max]
 *     → специальный last_spread (например D-Medium-Last-WithPhoto + dynamic G)
 *   - иначе → обычный grid-мастер с null'ами + warning students_grid_no_special_master
 *   - remainder = 0 → специальная обработка не требуется
 */
function buildMediumStudents(ctx: BuildContext, section: StudentSection): void {
  const filterRight = section.student_master_filter_right;
  if (!filterRight) {
    pushWarning(ctx, {
      code: 'master_not_found',
      detail: 'grid_alternate scenario requires student_master_filter_right',
    });
    return;
  }

  // Адаптируем фильтры под текущий config_type (как в teacher section)
  const leftFilter = { ...section.student_master_filter, applies_to_config: ctx.config.config_type };
  const rightFilter = { ...filterRight, applies_to_config: ctx.config.config_type };

  const left = findMaster(ctx.config.template_set.spreads, leftFilter);
  const right = findMaster(ctx.config.template_set.spreads, rightFilter);

  if (!left.ok) {
    pushWarning(ctx, {
      code: 'master_not_found',
      detail: `medium student master (left): expected_name_hint=${section.student_master_filter.expected_name_hint ?? '?'}`,
    });
    return;
  }
  if (!right.ok) {
    pushWarning(ctx, {
      code: 'master_not_found',
      detail: `medium student master (right): expected_name_hint=${filterRight.expected_name_hint ?? '?'}`,
    });
    return;
  }
  if (left.warning) pushWarning(ctx, left.warning);
  if (right.warning) pushWarning(ctx, right.warning);

  const N = section.students_per_unit; // 4 для Медиум
  const hasQuote = section.has_quote === true;
  const total = ctx.input.students.length;
  const remainder = total % N;

  // Определяем активен ли last_spread для текущего remainder'а
  const ls = section.last_spread;
  const useLastSpecial =
    ls !== undefined &&
    remainder >= ls.remainder_min &&
    remainder <= ls.remainder_max;

  // Сколько обычных страниц с N учениками
  const regularPages = useLastSpecial ? Math.floor(total / N) : Math.ceil(total / N);

  // Полные/неполные обычные страницы
  for (let i = 0; i < regularPages; i++) {
    const slice = ctx.input.students.slice(i * N, (i + 1) * N);
    const isLeft = i % 2 === 0;
    const master = isLeft ? left.master : right.master;

    if (slice.length < N) {
      // Неполная страница — записываем warning
      pushWarning(ctx, {
        code: 'students_grid_no_special_master',
        detail: `${ctx.config.config_type}: remainder=${remainder} учеников на последней странице (${master.name}) — нет специального мастера, пустые слоты заполнены null (см. master-cleanup-tz §A5)`,
      });
    }

    ctx.spreads.push({
      spread_index: ctx.spreadCounter.value++,
      template_id: master.id,
      template_name: master.name,
      data: buildGridStudentData(slice, N, hasQuote),
    });
  }

  // Специальный last_spread (для Медиум при remainder 1-2)
  if (useLastSpecial && ls) {
    const consumed = regularPages * N;
    const lastSlice = ctx.input.students.slice(consumed);
    buildLastSpread(ctx, ls, lastSlice);
  }
}

/**
 * Заполнить data объект для grid-страницы. Студенты идут с _1, _2, ..., _N.
 * Пустые слоты заполняются null (для скрытия placeholder'ов в фазе 2-4 рендера).
 */
function buildGridStudentData(
  students: Student[],
  slotsPerPage: number,
  hasQuote: boolean,
): Record<string, string | null> {
  const data: Record<string, string | null> = {};
  for (let i = 0; i < students.length; i++) {
    const n = i + 1;
    data[`studentportrait_${n}`] = students[i].portrait;
    data[`studentname_${n}`] = students[i].full_name;
    if (hasQuote) {
      data[`studentquote_${n}`] = students[i].quote;
    }
  }
  // null для пустых слотов
  for (let n = students.length + 1; n <= slotsPerPage; n++) {
    data[`studentportrait_${n}`] = null;
    data[`studentname_${n}`] = null;
    if (hasQuote) {
      data[`studentquote_${n}`] = null;
    }
  }
  return data;
}

/**
 * Специальный последний разворот (Медиум: D-Medium-Last-WithPhoto + dynamic G-*).
 *
 * Левая страница — заполняется студентами + classphotoframe (если есть placeholder).
 * Правая страница — динамический выбор G-HalfClass/G-FullClass/null
 * (если ls.right_dynamic === true).
 */
function buildLastSpread(
  ctx: BuildContext,
  ls: LastSpread,
  lastStudents: Student[],
): void {
  const leftFilter = { ...ls.left_filter, applies_to_config: ctx.config.config_type };
  const leftR = findMaster(ctx.config.template_set.spreads, leftFilter);

  if (!leftR.ok) {
    pushWarning(ctx, {
      code: 'master_not_found',
      detail: `last_spread left: ${ls.left_filter.expected_name_hint ?? '?'}`,
    });
    return;
  }
  if (leftR.warning) pushWarning(ctx, leftR.warning);

  // Левая страница: студенты + classphotoframe
  const leftData = buildGridStudentData(lastStudents, ls.left_slots_per_page, ls.left_has_quote);

  if (hasPlaceholder(leftR.master, 'classphotoframe')) {
    const fc = ctx.input.common_photos.full_class;
    if (fc.length >= 1) {
      leftData.classphotoframe = fc[0];
    } else {
      leftData.classphotoframe = null;
      pushWarning(ctx, {
        code: 'class_photo_missing',
        detail: `master ${leftR.master.name} has classphotoframe placeholder but common_photos.full_class is empty`,
      });
    }
  }

  ctx.spreads.push({
    spread_index: ctx.spreadCounter.value++,
    template_id: leftR.master.id,
    template_name: leftR.master.name,
    data: leftData,
  });

  // Правая страница (опционально, динамическая)
  if (!ls.right_dynamic) return;

  const rightMaster = pickRightCommonPhotoMaster(ctx);
  if (!rightMaster) {
    pushWarning(ctx, {
      code: 'no_right_teacher_master',
      detail: `last_spread right: ни half (>=2) ни full_class (>=1) — правая страница пропущена`,
    });
    return;
  }

  // Заполняем правую страницу — переиспользуем buildTeacherRightData
  // (она универсально обрабатывает halfclass/fullclass через hasPlaceholder).
  // Передаём пустой rightSubjects — на G-HalfClass/G-FullClass нет учительских слотов.
  ctx.spreads.push({
    spread_index: ctx.spreadCounter.value++,
    template_id: rightMaster.id,
    template_name: rightMaster.name,
    data: buildTeacherRightData(ctx, rightMaster, []),
  });
}
```

### 3.4 Импорты в build.ts

Убедиться что в верху файла импортируется `Student`:

```typescript
import type { Student, ... } from './types';
```

И `LastSpread`:

```typescript
import type { TeacherSection, LastSpread } from './scenarios';
```

---

## 4. `lib/album-builder/index.ts`

Добавить:

```typescript
export type { LastSpread } from './scenarios';
```

---

## 5. `scripts/smoke-album-builder.ts`

### 5.1 Добавить в `STUDENT_SETS`

```typescript
'4': Array.from({ length: 4 }, (_, i) => makeStudent(`Ученик${i + 1}`, 0)),
'5_med': Array.from({ length: 5 }, (_, i) => makeStudent(`Ученик${i + 1}`, 0)),
'7': Array.from({ length: 7 }, (_, i) => makeStudent(`Ученик${i + 1}`, 0)),
'8': Array.from({ length: 8 }, (_, i) => makeStudent(`Ученик${i + 1}`, 0)),
'11': Array.from({ length: 11 }, (_, i) => makeStudent(`Ученик${i + 1}`, 0)),
'13': Array.from({ length: 13 }, (_, i) => makeStudent(`Ученик${i + 1}`, 0)),
```

(Имена сетов произвольны — главное чтобы было удобно ссылаться.)

### 5.2 Добавить 7 сцен Медиум в `SCENES`

После всех существующих сцен:

```typescript
// ─── Медиум (0.10b.3) ───
// 4 ученика, чётно — 1 страница D-Medium-Left + 0 для пары не нужно
{
  label: 'medium / 4 students (1 full page)',
  configType: 'medium', studentsKey: '4',
  subjectsCount: 0, withHeadTeacher: false,
  expect: {
    spreadsCount: 1,
    noWarningCodes: ['master_not_found', 'students_grid_no_special_master'],
    masterNameSequence: ['D-Medium-Left'],
  },
},
// 5 учеников: 1 полная D-Medium-Left + remainder=1 → last_spread (left + dynamic right with full_class)
{
  label: 'medium / 5 students + full_class (last_spread активен)',
  configType: 'medium', studentsKey: '5_med',
  subjectsCount: 0, withHeadTeacher: false,
  expect: {
    spreadsCount: 3,
    noWarningCodes: ['master_not_found', 'students_grid_no_special_master', 'class_photo_missing'],
    masterNameSequence: ['D-Medium-Left', 'D-Medium-Last-WithPhoto', 'G-FullClass'],
  },
  commonPhotos: { full_class: ['url-fc-1', 'url-fc-2'] }, // 2 штуки: одно на classphotoframe в Last, второе в G-FullClass
},
// 5 учеников БЕЗ common photos — last_spread активен, classphotoframe пустой + warnings
{
  label: 'medium / 5 students no common photos',
  configType: 'medium', studentsKey: '5_med',
  subjectsCount: 0, withHeadTeacher: false,
  expect: {
    spreadsCount: 2,
    warningCodes: ['class_photo_missing', 'no_right_teacher_master'],
    masterNameSequence: ['D-Medium-Left', 'D-Medium-Last-WithPhoto'],
  },
},
// 7 учеников: 1 полная + remainder=3 → обычная D-Medium-Right с null + warning
{
  label: 'medium / 7 students (remainder=3, no special master)',
  configType: 'medium', studentsKey: '7',
  subjectsCount: 0, withHeadTeacher: false,
  expect: {
    spreadsCount: 2,
    warningCodes: ['students_grid_no_special_master'],
    masterNameSequence: ['D-Medium-Left', 'D-Medium-Right'],
  },
},
// 8 учеников: ровно 2 полных страницы (Left + Right)
{
  label: 'medium / 8 students (2 full pages)',
  configType: 'medium', studentsKey: '8',
  subjectsCount: 0, withHeadTeacher: false,
  expect: {
    spreadsCount: 2,
    noWarningCodes: ['master_not_found', 'students_grid_no_special_master'],
    masterNameSequence: ['D-Medium-Left', 'D-Medium-Right'],
  },
},
// 11 учеников: 2 полных + remainder=3 → 3-я обычная (D-Medium-Left) с null + warning
{
  label: 'medium / 11 students (remainder=3 на 3-й странице)',
  configType: 'medium', studentsKey: '11',
  subjectsCount: 0, withHeadTeacher: false,
  expect: {
    spreadsCount: 3,
    warningCodes: ['students_grid_no_special_master'],
    masterNameSequence: ['D-Medium-Left', 'D-Medium-Right', 'D-Medium-Left'],
  },
},
// 13 учеников + 5 учителей: учительский раздел + 3 полных + last_spread (remainder=1)
{
  label: 'medium / 13 students + 5 subjects + half',
  configType: 'medium', studentsKey: '13',
  subjectsCount: 5, withHeadTeacher: true,
  expect: {
    spreadsCount: 7,  // 2 учителя + 3 полных + last_spread (2 страницы)
    noWarningCodes: ['master_not_found', 'class_photo_missing', 'students_grid_no_special_master'],
    masterNameSequence: [
      'F-Head-LargeGrid', 'G-HalfClass',           // учителя (1-4 → SmallGrid; 5-8 → LargeGrid)
      'D-Medium-Left', 'D-Medium-Right', 'D-Medium-Left',
      'D-Medium-Last-WithPhoto', 'G-HalfClass',     // last_spread (right выбирается по half >= 2)
    ],
  },
  commonPhotos: { half: ['url-h1', 'url-h2'], full_class: ['url-fc-1'] },
},
```

После добавления должно быть 22 сцены (15 старых + 7 новых).

---

## Порядок действий

1. Внести правки в 5 файлов (см. секции 1-5)
2. `cp .env.local .env.local.real-backup`
3. Записать fake `.env.local`
4. `npx tsc --noEmit --project .` → пусто
5. `npx next build` → зелёный
6. `rm -f .env.local && mv .env.local.real-backup .env.local`
7. `set -a && . ./.env.local && set +a && npx tsx scripts/smoke-album-builder.ts`
   → `Result: 22/22 scenes passed`, exit 0
8. Если меньше — НЕ коммить, репортни в чат с конкретным выводом
9. Удалить файл-инструкцию: `rm docs/internal/0.10b.3-instructions.md`
   (если папка `docs/internal/` пустая — удалить её: `rmdir docs/internal 2>/dev/null || true`)
10. `git add lib/album-builder/ scripts/smoke-album-builder.ts docs/internal/`
11. `git status` — проверить что в staging:
    ```
    D  docs/internal/0.10b.3-instructions.md
    M  lib/album-builder/build.ts
    M  lib/album-builder/index.ts
    M  lib/album-builder/scenarios.ts
    M  lib/album-builder/types.ts
    M  scripts/smoke-album-builder.ts
    ```
12. `git commit -m "feat(layout/0.10b.3): комплектация Медиум (D-Medium-* + last-разворот)"`
13. **НЕ пушить** — жду OK от Сергея в чате после отчёта

## Отчёт

- Hash коммита (локальный, до push)
- tsc/build статусы
- Smoke результат (Result: N/N)
- Размеры файлов в строках (lib/album-builder/, изменения)

## Анти-цели

- Никакого Mini-soft — это для 0.11
- Никаких Лайт/Мини/Индивидуальный — это для 0.11
- Никакого общего раздела J-* — НЕ генерируется в фазе 0
- Не трогать teacher_section logic — она готова, привязывается к Медиум через ту же общую константу TEACHER_SECTION_LAYFLAT
- Не пытаться угадывать (как в 0.10b.2) — если smoke падает, репортнуть и ждать
- Контекст-файл — НЕ обновляем (это 0.14)
