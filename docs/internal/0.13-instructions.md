# Инструкция для подэтапа 0.13 — POST endpoint + UI Build Test

> Это временный файл-инструкция. После выполнения подэтапа удалить и закоммитить вместе с основными изменениями.

## Цель подэтапа

Дать Сергею **первую практическую точку тестирования** автовёрстки. Через UI он сможет:
1. Открыть `/super/templates/<id>` (страница которая уже работает — Konva-просмотрщик template_set'а от 0.8)
2. Увидеть новую панель «Build Test»
3. Задать параметры: config_type, print_type, число учеников, число предметников, есть ли head_teacher, common photos
4. Нажать «Build» → серверный endpoint вызывает `buildAlbum` с синтетическими данными
5. Увидеть результат: список разворотов в JSON-формате (template_name + data ключи на каждой странице) + warnings

Это **НЕ финальный визуал на canvas** — этот этап даёт структурный JSON-результат. Canvas-рендер с подставленными данными — отдельная задача (0.13.5 или фаза 2).

Один коммит:
```
feat(layout/0.13): POST /api/layout?action=build_album_test + UI Build Test
```

## Технический контекст

- Endpoint работает с **синтетическими** данными (не реальный альбом из БД). Это упрощает: никаких миграций, никакой работы с YC, никакой загрузки реальных photos.
- Endpoint возвращает результат `buildAlbum` как JSON (`spreads` + `warnings`).
- UI добавляется на существующую страницу `/super/templates/[id]/page.tsx`.
- Только superadmin (как и весь импорт IDML).

## Файлы к правке

3 элемента:
1. `lib/album-builder/load-template-set.ts` — **новый файл**: helper для загрузки template_set из Supabase в формат который ожидает `buildAlbum`. Извлекаем из `scripts/smoke-album-builder.ts`.
2. `app/api/layout/route.ts` — добавить новый POST action `build_album_test`
3. `app/super/templates/[id]/page.tsx` — добавить UI панель «Build Test»

`scripts/smoke-album-builder.ts` рефакторим — теперь использует общий `loadTemplateSet` из `lib/`.

---

## 1. `lib/album-builder/load-template-set.ts`

Новый файл. Извлекаем функцию из smoke-скрипта чтобы переиспользовать в endpoint.

```typescript
/**
 * Загрузка template_set из Supabase в формат, который ожидает buildAlbum().
 *
 * Используется:
 *  - В scripts/smoke-album-builder.ts для тестового прогона
 *  - В app/api/layout/route.ts (action=build_album_test) для UI Build Test
 *
 * @param supabase — клиент с правами SELECT на template_sets и spread_templates
 * @param slug — slug template_set'а (по умолчанию 'okeybook-default')
 */

import type { SupabaseClient } from '@supabase/supabase-js';
import type { TemplateSet } from './types';

export async function loadTemplateSet(
  supabase: SupabaseClient,
  slug = 'okeybook-default',
): Promise<TemplateSet> {
  const { data: ts, error: e1 } = await supabase
    .from('template_sets')
    .select('*')
    .eq('slug', slug)
    .single();
  if (e1 || !ts) {
    throw new Error(`template_set ${slug} not found: ${e1?.message ?? 'no row'}`);
  }

  const { data: spreads, error: e2 } = await supabase
    .from('spread_templates')
    .select('*')
    .eq('template_set_id', ts.id)
    .order('sort_order');
  if (e2 || !spreads) {
    throw new Error(`spread_templates for ${slug} not loaded: ${e2?.message ?? 'empty'}`);
  }

  return { ...ts, spreads } as TemplateSet;
}
```

После создания этого файла — обновить `scripts/smoke-album-builder.ts`:

Заменить inline `async function loadTemplateSet()` на импорт:
```typescript
import { loadTemplateSet } from '../lib/album-builder/load-template-set';
```

И в коде использовать:
```typescript
const ts = await loadTemplateSet(supabase);
```

(Удалить локальную функцию из smoke-скрипта.)

Прогнать smoke после рефакторинга — должно остаться 58/58. Это страховка что рефакторинг не сломал поведение.

---

## 2. `app/api/layout/route.ts`

### 2.1 Расширить POST handler

В существующем `POST` функции добавить ветку для `build_album_test` ДО существующей проверки `if (action !== 'import_idml')`.

Новый action `build_album_test` принимает **JSON** (не multipart). Структура входа:

```typescript
type BuildAlbumTestInput = {
  config_type: 'standard' | 'universal' | 'maximum' | 'medium' | 'light' | 'mini' | 'individual';
  print_type: 'layflat' | 'soft';
  students_count: number;
  subjects_count: number;
  with_head_teacher: boolean;
  common_photos: {
    full_class?: number;
    half?: number;
    quarter?: number;
    sixth?: number;
    collage?: number;
  };
  /** Опционально — массив длин friend_photos для каждого ученика (для Индивидуального). Default — все 0. */
  friend_photos_per_student?: number[];
};
```

### 2.2 Структура нового кода в POST

Замени текущую логику в начале POST handler на:

```typescript
export async function POST(req: NextRequest) {
  const auth = await requireAuth(req, ['superadmin'])
  if (isAuthError(auth)) return auth

  const action = req.nextUrl.searchParams.get('action')

  if (action === 'build_album_test') {
    return handleBuildAlbumTest(req, auth)
  }

  if (action === 'import_idml') {
    return handleImportIdml(req, auth)
  }

  return NextResponse.json({ error: 'unknown action' }, { status: 400 })
}
```

И вынести существующую логику multipart-импорта в отдельную функцию `handleImportIdml(req, auth)` (просто переместить код).

### 2.3 Функция `handleBuildAlbumTest`

```typescript
import { buildAlbum } from '@/lib/album-builder/build'
import { loadTemplateSet } from '@/lib/album-builder/load-template-set'
import type {
  Student,
  Subject,
  HeadTeacher,
  AlbumInput,
  Config,
  ConfigType,
  PrintType,
} from '@/lib/album-builder/types'

const VALID_CONFIG_TYPES: ConfigType[] = [
  'standard', 'universal', 'maximum', 'medium',
  'light', 'mini', 'individual',
]

const VALID_PRINT_TYPES: PrintType[] = ['layflat', 'soft']

async function handleBuildAlbumTest(
  req: NextRequest,
  auth: { tenantId: string; userId: string }, // тип из requireAuth
): Promise<NextResponse> {
  let body: unknown
  try {
    body = await req.json()
  } catch {
    return NextResponse.json({ error: 'invalid JSON body' }, { status: 400 })
  }

  // Валидация
  if (typeof body !== 'object' || body === null) {
    return NextResponse.json({ error: 'body must be object' }, { status: 400 })
  }
  const b = body as Record<string, unknown>

  const configType = b.config_type
  if (typeof configType !== 'string' || !VALID_CONFIG_TYPES.includes(configType as ConfigType)) {
    return NextResponse.json(
      { error: `config_type must be one of ${VALID_CONFIG_TYPES.join(', ')}` },
      { status: 400 },
    )
  }

  const printType = b.print_type
  if (typeof printType !== 'string' || !VALID_PRINT_TYPES.includes(printType as PrintType)) {
    return NextResponse.json(
      { error: `print_type must be layflat or soft` },
      { status: 400 },
    )
  }

  const studentsCount = b.students_count
  if (typeof studentsCount !== 'number' || studentsCount < 0 || studentsCount > 100) {
    return NextResponse.json({ error: 'students_count must be number 0-100' }, { status: 400 })
  }

  const subjectsCount = b.subjects_count
  if (typeof subjectsCount !== 'number' || subjectsCount < 0 || subjectsCount > 30) {
    return NextResponse.json({ error: 'subjects_count must be number 0-30' }, { status: 400 })
  }

  const withHeadTeacher = b.with_head_teacher === true
  const commonPhotosInput = (b.common_photos ?? {}) as Record<string, unknown>
  const friendPhotosPerStudent = (b.friend_photos_per_student ?? []) as unknown[]

  // Сборка синтетических данных
  const students: Student[] = []
  for (let i = 0; i < studentsCount; i++) {
    const friendCount =
      typeof friendPhotosPerStudent[i] === 'number'
        ? Math.min(4, Math.max(0, friendPhotosPerStudent[i] as number))
        : 0
    students.push({
      full_name: `Ученик ${i + 1}`,
      quote: `Цитата ${i + 1}`,
      portrait: `https://fake/student-${i + 1}.jpg`,
      friend_photos: Array.from(
        { length: friendCount },
        (_, j) => `https://fake/student-${i + 1}-friend-${j + 1}.jpg`,
      ),
    })
  }

  const subjects: Subject[] = Array.from({ length: subjectsCount }, (_, i) => ({
    name: `Предметник ${i + 1}`,
    role: 'учитель',
    photo: `https://fake/subject-${i + 1}.jpg`,
  }))

  const head_teacher: HeadTeacher | null = withHeadTeacher
    ? {
        name: 'Иванова Мария Петровна',
        role: 'классный руководитель',
        photo: 'https://fake/head.jpg',
        text: 'Дорогие выпускники, желаю вам успехов.',
      }
    : null

  // common photos — массивы fake URL длиной commonPhotosInput[key]
  const makeUrls = (n: number, prefix: string): string[] =>
    Array.from({ length: n }, (_, i) => `https://fake/${prefix}-${i + 1}.jpg`)

  const common_photos = {
    full_class: makeUrls(Number(commonPhotosInput.full_class ?? 0), 'class'),
    half: makeUrls(Number(commonPhotosInput.half ?? 0), 'half'),
    quarter: makeUrls(Number(commonPhotosInput.quarter ?? 0), 'quarter'),
    sixth: makeUrls(Number(commonPhotosInput.sixth ?? 0), 'sixth'),
    collage: makeUrls(Number(commonPhotosInput.collage ?? 0), 'collage'),
  }

  // Загрузка template_set
  let template_set
  try {
    template_set = await loadTemplateSet(supabaseAdmin)
  } catch (e) {
    return NextResponse.json(
      { error: `failed to load template_set: ${(e as Error).message}` },
      { status: 500 },
    )
  }

  // Сборка
  const input: AlbumInput = {
    students,
    subjects,
    head_teacher,
    common_photos,
  }
  const config: Config = {
    config_type: configType as ConfigType,
    print_type: printType as PrintType,
    template_set,
  }

  const result = buildAlbum(input, config)

  return NextResponse.json({
    spreads: result.spreads,
    warnings: result.warnings,
    summary: {
      total_spreads: result.spreads.length,
      total_warnings: result.warnings.length,
      config_type: config.config_type,
      print_type: config.print_type,
      students_count: studentsCount,
      subjects_count: subjectsCount,
    },
  })
}
```

**Важно:** существующий код multipart import_idml перенести в `handleImportIdml(req, auth)` функцию (просто copy-paste с изменением сигнатуры).

---

## 3. `app/super/templates/[id]/page.tsx`

Это страница где уже работает Konva-просмотрщик template_set'а от 0.8. Добавляем туда панель «Build Test».

### 3.1 Состояние компонента

Добавить state'ы:

```typescript
const [buildOpen, setBuildOpen] = useState(false)
const [buildLoading, setBuildLoading] = useState(false)
const [buildResult, setBuildResult] = useState<BuildAlbumResult | null>(null)
const [buildError, setBuildError] = useState<string | null>(null)

// Параметры формы
const [bConfigType, setBConfigType] = useState<ConfigType>('standard')
const [bPrintType, setBPrintType] = useState<PrintType>('layflat')
const [bStudentsCount, setBStudentsCount] = useState(5)
const [bSubjectsCount, setBSubjectsCount] = useState(0)
const [bWithHeadTeacher, setBWithHeadTeacher] = useState(false)
const [bFullClass, setBFullClass] = useState(0)
const [bHalf, setBHalf] = useState(0)
const [bFriendsPerStudent, setBFriendsPerStudent] = useState(0)  // 0 = у всех 0 фото; 4 = у всех 4
```

Тип `BuildAlbumResult`:
```typescript
type BuildAlbumResult = {
  spreads: Array<{
    spread_index: number
    template_id: string
    template_name: string
    data: Record<string, string | null>
  }>
  warnings: Array<{ code: string; detail: string }>
  summary: {
    total_spreads: number
    total_warnings: number
    config_type: string
    print_type: string
    students_count: number
    subjects_count: number
  }
}
```

### 3.2 Функция отправки запроса

```typescript
const runBuildTest = async () => {
  setBuildLoading(true)
  setBuildError(null)
  setBuildResult(null)

  try {
    const friendPhotos =
      bFriendsPerStudent > 0
        ? Array.from({ length: bStudentsCount }, () => bFriendsPerStudent)
        : []

    const r = await fetch('/api/layout?action=build_album_test', {
      method: 'POST',
      credentials: 'include',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        config_type: bConfigType,
        print_type: bPrintType,
        students_count: bStudentsCount,
        subjects_count: bSubjectsCount,
        with_head_teacher: bWithHeadTeacher,
        common_photos: {
          full_class: bFullClass,
          half: bHalf,
        },
        friend_photos_per_student: friendPhotos,
      }),
    })

    if (!r.ok) {
      const err = await r.json().catch(() => ({ error: 'unknown error' }))
      setBuildError(err.error ?? `HTTP ${r.status}`)
      setBuildLoading(false)
      return
    }

    const data: BuildAlbumResult = await r.json()
    setBuildResult(data)
  } catch (e) {
    setBuildError((e as Error).message)
  } finally {
    setBuildLoading(false)
  }
}
```

### 3.3 UI панели

Добавить кнопку и панель в существующий JSX страницы — например, рядом с заголовком template_set:

```tsx
<button
  onClick={() => setBuildOpen(!buildOpen)}
  className="px-3 py-1.5 text-sm bg-blue-600 text-white rounded hover:bg-blue-700"
>
  {buildOpen ? '▲ Скрыть Build Test' : '▼ Build Test'}
</button>

{buildOpen && (
  <div className="mt-4 p-4 bg-gray-50 border border-gray-200 rounded-lg">
    <h3 className="font-bold mb-3">Build Test — синтетический альбом</h3>
    <div className="grid grid-cols-2 gap-3 text-sm">
      <label>
        config_type:
        <select
          value={bConfigType}
          onChange={(e) => setBConfigType(e.target.value as ConfigType)}
          className="ml-2 px-2 py-1 border rounded"
        >
          {['standard', 'universal', 'maximum', 'medium', 'light', 'mini', 'individual'].map((c) => (
            <option key={c} value={c}>{c}</option>
          ))}
        </select>
      </label>
      <label>
        print_type:
        <select
          value={bPrintType}
          onChange={(e) => setBPrintType(e.target.value as PrintType)}
          className="ml-2 px-2 py-1 border rounded"
        >
          <option value="layflat">layflat</option>
          <option value="soft">soft</option>
        </select>
      </label>
      <label>
        Учеников:
        <input
          type="number"
          min={0}
          max={100}
          value={bStudentsCount}
          onChange={(e) => setBStudentsCount(Number(e.target.value))}
          className="ml-2 px-2 py-1 border rounded w-20"
        />
      </label>
      <label>
        Предметников:
        <input
          type="number"
          min={0}
          max={30}
          value={bSubjectsCount}
          onChange={(e) => setBSubjectsCount(Number(e.target.value))}
          className="ml-2 px-2 py-1 border rounded w-20"
        />
      </label>
      <label className="flex items-center gap-2">
        <input
          type="checkbox"
          checked={bWithHeadTeacher}
          onChange={(e) => setBWithHeadTeacher(e.target.checked)}
        />
        head_teacher
      </label>
      <label>
        full_class фото:
        <input
          type="number"
          min={0}
          max={5}
          value={bFullClass}
          onChange={(e) => setBFullClass(Number(e.target.value))}
          className="ml-2 px-2 py-1 border rounded w-16"
        />
      </label>
      <label>
        half фото:
        <input
          type="number"
          min={0}
          max={5}
          value={bHalf}
          onChange={(e) => setBHalf(Number(e.target.value))}
          className="ml-2 px-2 py-1 border rounded w-16"
        />
      </label>
      <label>
        friend_photos на ученика (0-4):
        <input
          type="number"
          min={0}
          max={4}
          value={bFriendsPerStudent}
          onChange={(e) => setBFriendsPerStudent(Number(e.target.value))}
          className="ml-2 px-2 py-1 border rounded w-16"
        />
      </label>
    </div>

    <button
      onClick={runBuildTest}
      disabled={buildLoading}
      className="mt-4 px-4 py-2 bg-green-600 text-white rounded hover:bg-green-700 disabled:opacity-50"
    >
      {buildLoading ? 'Building...' : 'Build'}
    </button>

    {buildError && (
      <div className="mt-3 p-3 bg-red-50 border border-red-200 rounded text-red-700 text-sm">
        Ошибка: {buildError}
      </div>
    )}

    {buildResult && (
      <div className="mt-4 space-y-3">
        <div className="p-3 bg-white border border-gray-200 rounded text-sm">
          <div className="font-bold mb-2">Сводка:</div>
          <div>spreads: {buildResult.summary.total_spreads}</div>
          <div>warnings: {buildResult.summary.total_warnings}</div>
        </div>

        {buildResult.warnings.length > 0 && (
          <div className="p-3 bg-yellow-50 border border-yellow-200 rounded text-sm">
            <div className="font-bold mb-2">Warnings:</div>
            {buildResult.warnings.map((w, i) => (
              <div key={i} className="text-xs mb-1">
                <span className="font-mono text-yellow-700">{w.code}</span>
                {' — '}
                {w.detail}
              </div>
            ))}
          </div>
        )}

        <div className="p-3 bg-white border border-gray-200 rounded">
          <div className="font-bold mb-2">Spreads:</div>
          {buildResult.spreads.map((s) => (
            <div key={s.spread_index} className="border-b border-gray-100 py-2 text-sm">
              <div>
                <span className="font-mono text-gray-500">[{s.spread_index}]</span>{' '}
                <span className="font-bold">{s.template_name}</span>
              </div>
              {Object.keys(s.data).length > 0 && (
                <details className="mt-1 text-xs text-gray-600">
                  <summary className="cursor-pointer">data ({Object.keys(s.data).length} ключ)</summary>
                  <pre className="mt-1 p-2 bg-gray-50 rounded overflow-auto">
                    {JSON.stringify(s.data, null, 2)}
                  </pre>
                </details>
              )}
            </div>
          ))}
        </div>
      </div>
    )}
  </div>
)}
```

(Конкретное расположение и стилистика — на твоё усмотрение, главное чтобы работало визуально аккуратно. Если нужно использовать существующие компоненты `_components/` — переиспользуй их.)

### 3.4 Импорты типов

Добавить в начало файла:
```typescript
import type { ConfigType, PrintType } from '@/lib/album-builder/types'
```

---

## Порядок действий

1. Создать `lib/album-builder/load-template-set.ts`
2. Обновить `scripts/smoke-album-builder.ts` — заменить inline `loadTemplateSet` на импорт
3. Прогнать smoke — должно остаться 58/58 (рефакторинг не должен ничего сломать)
4. Расширить `app/api/layout/route.ts` — новый action `build_album_test` (вынести существующий код import_idml в `handleImportIdml`, добавить `handleBuildAlbumTest`)
5. Расширить `app/super/templates/[id]/page.tsx` — UI панель Build Test
6. `cp .env.local .env.local.real-backup`
7. fake `.env.local`
8. `npx tsc --noEmit --project .` → пусто
9. `npx next build` → зелёный
10. `rm -f .env.local && mv .env.local.real-backup .env.local`
11. `set -a && . ./.env.local && set +a && npx tsx scripts/smoke-album-builder.ts` → 58/58
12. **Тестирование UI пропускаем** — Сергей это будет смотреть на продакшне после пуша. Не запускаем dev-сервер.
13. Удалить файл-инструкцию: `rm docs/internal/0.13-instructions.md && rmdir docs/internal 2>/dev/null || true`
14. `git add lib/album-builder/ scripts/ app/api/layout/ app/super/templates/ docs/internal/`
15. `git status` — должно быть:
    ```
    A  lib/album-builder/load-template-set.ts
    D  docs/internal/0.13-instructions.md
    M  app/api/layout/route.ts
    M  app/super/templates/[id]/page.tsx
    M  scripts/smoke-album-builder.ts
    ```
16. `git commit -m "feat(layout/0.13): POST /api/layout?action=build_album_test + UI Build Test"`
17. **НЕ пушить** — дождись OK

## Отчёт

- Hash коммита (локальный)
- tsc/build статусы
- Smoke результат (58/58 после рефакторинга loadTemplateSet)
- Размеры файлов в строках (изменения)

## Анти-цели

- Никакого canvas-рендера с подставленными данными — это для будущего (0.13.5 / фаза 2)
- Никаких миграций БД, никакого реального альбома — только синтетика
- Не пытаться оптимизировать UI — сделать функционально, стилистика на минимум
- Не запускать dev-сервер для проверки UI — Сергей проверит после пуша на продакшне
- Контекст-файл — НЕ обновляем (это 0.14)
