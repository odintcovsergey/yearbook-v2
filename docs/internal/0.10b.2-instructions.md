# Инструкция для подэтапа 0.10b.2

> Это временный файл-инструкция. После выполнения подэтапа удалить и закоммитить вместе с основными изменениями.

## Цель подэтапа

Реализовать учительский раздел layflat (F+G мастера, 7 вариантов по `subjects.length`) в `buildAlbum`. Применяется к Стандарту, Универсалу, Максимуму. Медиум добавим в 0.10b.3.

Один коммит:
```
feat(layout/0.10b.2): учительский раздел layflat (F+G, 7 вариантов)
```

## Источники истины

- `yearbook-context-v38.md` (раздел "ПРОДУКТ B")
- `docs/templates/idml-recon-notes.md` (§4 имена меток, §9 общий раздел не генерируем)
- `build_album.jsx` (старый InDesign скрипт, лежит в репо)

Ключевые факты из build_album.jsx:
- `subjects=0` → F-Head-WithPhoto + dynamic G (HalfClass/FullClass/null)
- `subjects=1-4` → F-Head-SmallGrid + dynamic G
- `subjects=5-8` → F-Head-LargeGrid + dynamic G
- `subjects=9` → F-Head-WithPhoto + G-Teachers-3x3
- `subjects=10-12` → F-Head-WithPhoto + G-Teachers-4x3
- `subjects=13-16` → F-Head-WithPhoto + G-Teachers-4x4
- `subjects=17-24` → F-Head-LargeGrid + G-Teachers-4x4 (slice(0,8) на F + slice(8) на G)
- `subjects ≥25` → degraded: warning + обрезка до 24
- F-Head-WithPhoto имеет `classPhotoFrame` (заполняется из `common_photos.full_class[0]`)
- F-Head-SmallGrid и F-Head-LargeGrid НЕ имеют `classPhotoFrame`
- Dynamic G:
  - `common_photos.half.length >= 2` → G-HalfClass (`halfLeftPhoto` + `halfRightPhoto`)
  - `common_photos.full_class.length >= 1` → G-FullClass (`classPhotoFrame`)
  - иначе → null (правая страница пропускается)

## Файлы к правке

5 файлов:
1. `lib/album-builder/types.ts` — расширить `PageRole` и `BuildWarningCode`
2. `lib/album-builder/scenarios.ts` — новые типы `TeacherSpreadVariant`, `TeacherSection`, расширить `ScenarioDef`, добавить `TEACHER_SECTION_LAYFLAT`, привязать к 3 сценариям
3. `lib/album-builder/build.ts` — `buildTeacherSection`, `buildTeacherLeftData`, `buildTeacherRightData`, `pickRightTeacherMaster`, `hasPlaceholder` + вставка в `buildAlbum`
4. `lib/album-builder/index.ts` — экспорты
5. `scripts/smoke-album-builder.ts` — расширить `Scene`, `STUDENT_SETS`, добавить 10 новых сцен

---

## 1. `lib/album-builder/types.ts`

### 1.1 Расширить `PageRole`

Добавить `student_grid_left` и `student_grid_right` (миграция 0.10b.1 уже применена в БД):

```typescript
export type PageRole =
  | 'student'
  | 'student_left'
  | 'student_right'
  | 'student_grid'
  | 'student_grid_left'
  | 'student_grid_right'
  | 'student_overflow'
  | 'student_last'
  | 'teacher_left'
  | 'teacher_right'
  | 'common'
  | 'intro'
  | 'cover';
```

В JSDoc типа `PageRole` добавить две строки:
```
 * - `student_grid_left`  — левая страница сетки (D-Medium-Left, L-6-Left, N-12-Left)
 * - `student_grid_right` — правая страница сетки (D-Medium-Right, L-6-Right, N-12-Right)
```

И обновить ссылку на миграцию в JSDoc типа: `(миграция 0.8.6.1 + 0.10a.1 + 0.10b.1)`.

### 1.2 Расширить `BuildWarningCode`

Добавить 4 новых кода:

```typescript
export type BuildWarningCode =
  | 'master_not_found'
  | 'fallback_used'
  | 'name_mismatch'
  | 'no_head_teacher'
  | 'students_overflow'
  | 'students_too_few'
  | 'students_empty'
  | 'students_odd_in_standard'
  | 'subjects_overflow'         // ← новый
  | 'class_photo_missing'       // ← новый
  | 'half_class_missing'        // ← новый
  | 'no_right_teacher_master';  // ← новый
```

Обновить JSDoc-комментарий типа, добавить описания:
```
 * - `subjects_overflow`        — subjects.length > 24, лишние обрезаны (degraded)
 * - `class_photo_missing`      — мастер требует classPhotoFrame, в input нет full_class
 * - `half_class_missing`       — мастер требует halfLeftPhoto/halfRightPhoto, в input нет half
 * - `no_right_teacher_master`  — для subjects 0-8 нет ни half, ни full_class фото —
 *                                правая учительская страница пропущена
```

---

## 2. `lib/album-builder/scenarios.ts`

### 2.1 Новые типы

Добавить (после существующих типов):

```typescript
/**
 * Один вариант учительского разворота. Применяется когда subjects.length
 * попадает в диапазон [subjects_min, subjects_max] (включительно).
 */
export type TeacherSpreadVariant = {
  /** Минимум subjects.length для применения. */
  subjects_min: number;
  /** Максимум subjects.length для применения. */
  subjects_max: number;
  /** Сколько subjects уходит на левую страницу (F-*). */
  subjects_on_left: number;
  /** Сколько subjects уходит на правую страницу (G-*). undefined — динамический выбор. */
  subjects_on_right?: number;
  /** Фильтр для левого мастера (F-*). applies_to_config — заглушка, подменяется в runtime. */
  left_filter: MasterFilter;
  /**
   * Фильтр для правого мастера (G-*). undefined означает что правый мастер
   * выбирается динамически в build.ts → pickRightTeacherMaster по наличию
   * фото в common_photos. Актуально для subjects 0-8.
   */
  right_filter?: MasterFilter;
};

/**
 * Учительский раздел сценария — список вариантов в порядке проверки.
 * Алгоритм берёт первый где subjects.length ∈ [min,max].
 */
export type TeacherSection = {
  variants: TeacherSpreadVariant[];
};
```

### 2.2 Расширить `ScenarioDef`

```typescript
export type ScenarioDef = {
  config_type: ConfigType;
  print_type: PrintType;
  description: string;
  student_section: StudentSection;
  /** Опционально — учительский раздел. Если undefined, секция не генерируется. */
  teacher_section?: TeacherSection;
};
```

### 2.3 Объявить `TEACHER_SECTION_LAYFLAT`

Это общая константа для всех 4 layflat-комплектаций (Стандарт/Универсал/Максимум/Медиум). В 0.10b.2 ссылается из 3, Медиум добавим в 0.10b.3.

`applies_to_config: 'standard'` везде — это заглушка. `buildTeacherSection` подменяет на текущий `ctx.config.config_type` перед вызовом `findMaster`.

```typescript
/**
 * Учительский раздел для layflat-печати. Применим к Стандарту/Универсалу/
 * Максимуму/Медиуму. Mini-soft (одностраничная учительская секция с F-*-R)
 * — отдельная константа в 0.11.
 *
 * Семь вариантов по subjects.length. Поведение для subjects ≥25 — degraded:
 * buildAlbum обрезает до 24 и пишет warning 'subjects_overflow'.
 *
 * applies_to_config во всех filter'ах — заглушка ('standard'), фактическое
 * значение подменяется в buildTeacherSection из ctx.config.config_type.
 */
export const TEACHER_SECTION_LAYFLAT: TeacherSection = {
  variants: [
    {
      subjects_min: 0, subjects_max: 0,
      subjects_on_left: 0,
      // subjects_on_right: undefined → dynamic
      left_filter: {
        page_role: 'teacher_left',
        applies_to_config: 'standard',
        slot_capacity_min: { head_teacher: true },
        expected_name_hint: 'F-Head-WithPhoto',
      },
      // right_filter: undefined → dynamic в коде
    },
    {
      subjects_min: 1, subjects_max: 4,
      subjects_on_left: 4,
      left_filter: {
        page_role: 'teacher_left',
        applies_to_config: 'standard',
        slot_capacity_min: { head_teacher: true, teachers: 4 },
        expected_name_hint: 'F-Head-SmallGrid',
      },
    },
    {
      subjects_min: 5, subjects_max: 8,
      subjects_on_left: 8,
      left_filter: {
        page_role: 'teacher_left',
        applies_to_config: 'standard',
        slot_capacity_min: { head_teacher: true, teachers: 8 },
        expected_name_hint: 'F-Head-LargeGrid',
      },
    },
    {
      subjects_min: 9, subjects_max: 9,
      subjects_on_left: 0,
      subjects_on_right: 9,
      left_filter: {
        page_role: 'teacher_left',
        applies_to_config: 'standard',
        slot_capacity_min: { head_teacher: true },
        expected_name_hint: 'F-Head-WithPhoto',
      },
      right_filter: {
        page_role: 'teacher_right',
        applies_to_config: 'standard',
        slot_capacity_min: { teachers: 9 },
        expected_name_hint: 'G-Teachers-3x3',
      },
    },
    {
      subjects_min: 10, subjects_max: 12,
      subjects_on_left: 0,
      subjects_on_right: 12,
      left_filter: {
        page_role: 'teacher_left',
        applies_to_config: 'standard',
        slot_capacity_min: { head_teacher: true },
        expected_name_hint: 'F-Head-WithPhoto',
      },
      right_filter: {
        page_role: 'teacher_right',
        applies_to_config: 'standard',
        slot_capacity_min: { teachers: 12 },
        expected_name_hint: 'G-Teachers-4x3',
      },
    },
    {
      subjects_min: 13, subjects_max: 16,
      subjects_on_left: 0,
      subjects_on_right: 16,
      left_filter: {
        page_role: 'teacher_left',
        applies_to_config: 'standard',
        slot_capacity_min: { head_teacher: true },
        expected_name_hint: 'F-Head-WithPhoto',
      },
      right_filter: {
        page_role: 'teacher_right',
        applies_to_config: 'standard',
        slot_capacity_min: { teachers: 16 },
        expected_name_hint: 'G-Teachers-4x4',
      },
    },
    {
      subjects_min: 17, subjects_max: 24,
      subjects_on_left: 8,
      subjects_on_right: 16,
      left_filter: {
        page_role: 'teacher_left',
        applies_to_config: 'standard',
        slot_capacity_min: { head_teacher: true, teachers: 8 },
        expected_name_hint: 'F-Head-LargeGrid',
      },
      right_filter: {
        page_role: 'teacher_right',
        applies_to_config: 'standard',
        slot_capacity_min: { teachers: 16 },
        expected_name_hint: 'G-Teachers-4x4',
      },
    },
  ],
};
```

### 2.4 Привязать `teacher_section` к 3 сценариям

В `SCENARIOS_LAYFLAT` для standard, universal, maximum добавить поле `teacher_section: TEACHER_SECTION_LAYFLAT`. Не дублируем — все три ссылаются на одну константу.

```typescript
standard: {
  config_type: 'standard',
  print_type: 'layflat',
  description: '...',
  student_section: { /* без изменений */ },
  teacher_section: TEACHER_SECTION_LAYFLAT,  // ← добавить
},
universal: {
  // ... + teacher_section: TEACHER_SECTION_LAYFLAT
},
maximum: {
  // ... + teacher_section: TEACHER_SECTION_LAYFLAT
},
```

---

## 3. `lib/album-builder/build.ts`

### 3.1 Импорты

Добавить в начало файла:

```typescript
import type { TeacherSection } from './scenarios';
import type { Subject, HeadTeacher, SpreadTemplate } from './types';
```

### 3.2 Вставка в `buildAlbum`

После проверки `students.length === 0` и **перед** `buildStudentsSection`:

```typescript
// [SOFT-INTRO] — будет в 0.11
if (scenario.teacher_section) {
  if (input.head_teacher) {
    buildTeacherSection(ctx, scenario.teacher_section);
  } else {
    pushWarning(ctx, {
      code: 'no_head_teacher',
      detail: 'head_teacher is null, teacher section skipped',
    });
  }
}
buildStudentsSection(ctx, scenario.student_section);
// [COMMON] — НЕ генерируется (idml-recon §9)
```

### 3.3 Новые функции (внизу файла)

```typescript
/**
 * Подбор правого учительского мастера для variants 0-8 (без right_filter).
 * Логика из build_album.jsx (pickRightPhotoMaster):
 * 1) common_photos.half >= 2  → G-HalfClass
 * 2) common_photos.full_class >= 1 → G-FullClass
 * 3) иначе → null
 *
 * Внутренние warning'и о fallback/name_mismatch пушатся в ctx, но если мастер
 * не нашёлся в БД — молча идём к следующей ветке. Финальный warning
 * 'no_right_teacher_master' пишется в buildTeacherSection после возврата null.
 */
function pickRightTeacherMaster(ctx: BuildContext): SpreadTemplate | null {
  const cp = ctx.input.common_photos;
  const spreads = ctx.config.template_set.spreads;

  if (cp.half.length >= 2) {
    const r = findMaster(spreads, {
      page_role: 'teacher_right',
      applies_to_config: ctx.config.config_type,
      expected_name_hint: 'G-HalfClass',
    });
    if (r.ok) {
      if (r.warning) pushWarning(ctx, r.warning);
      return r.master;
    }
  }
  if (cp.full_class.length >= 1) {
    const r = findMaster(spreads, {
      page_role: 'teacher_right',
      applies_to_config: ctx.config.config_type,
      expected_name_hint: 'G-FullClass',
    });
    if (r.ok) {
      if (r.warning) pushWarning(ctx, r.warning);
      return r.master;
    }
  }
  return null;
}

/**
 * Сборка учительского раздела (F+G).
 *
 * Шаги:
 *  1. При subjects.length > 24 — обрезаем до 24 + warning 'subjects_overflow'.
 *  2. Поиск variant'а по диапазону.
 *  3. findMaster для left_filter (F-*). Если не найден — warning + return.
 *  4. Заполняем data левой страницы и пушим SpreadInstance.
 *  5. Для правой:
 *     - variant.right_filter задан → findMaster
 *     - undefined → pickRightTeacherMaster (dynamic)
 *  6. Если правый мастер найден — пушим SpreadInstance с правой данными.
 *     Иначе — правую страницу пропускаем (warning уже залогирован).
 */
function buildTeacherSection(ctx: BuildContext, section: TeacherSection): void {
  const head = ctx.input.head_teacher!; // вызывающий buildAlbum проверил выше
  let subjects = ctx.input.subjects;

  // 1. Degraded для subjects ≥25
  if (subjects.length > 24) {
    pushWarning(ctx, {
      code: 'subjects_overflow',
      detail: `subjects.length=${subjects.length} > 24, обрезано до 24 (см. master-cleanup-tz §C1)`,
    });
    subjects = subjects.slice(0, 24);
  }

  // 2. Поиск variant'а
  const variant = section.variants.find(
    (v) => subjects.length >= v.subjects_min && subjects.length <= v.subjects_max,
  );
  if (!variant) {
    pushWarning(ctx, {
      code: 'master_not_found',
      detail: `no teacher_section variant for subjects.length=${subjects.length}`,
    });
    return;
  }

  // 3. F-мастер
  const leftFilter = { ...variant.left_filter, applies_to_config: ctx.config.config_type };
  const leftR = findMaster(ctx.config.template_set.spreads, leftFilter);
  if (!leftR.ok) {
    pushWarning(ctx, {
      code: 'master_not_found',
      detail: `teacher left: ${variant.left_filter.expected_name_hint ?? '?'}`,
    });
    return; // F не найден — без G тоже, всю секцию skip
  }
  if (leftR.warning) pushWarning(ctx, leftR.warning);

  // 4. Левая страница
  const leftSubjects = subjects.slice(0, variant.subjects_on_left);
  ctx.spreads.push({
    spread_index: ctx.spreadCounter.value++,
    template_id: leftR.master.id,
    template_name: leftR.master.name,
    data: buildTeacherLeftData(ctx, leftR.master, head, leftSubjects),
  });

  // 5. G-мастер
  let rightMaster: SpreadTemplate | null = null;
  if (variant.right_filter) {
    const rightFilter = { ...variant.right_filter, applies_to_config: ctx.config.config_type };
    const rightR = findMaster(ctx.config.template_set.spreads, rightFilter);
    if (!rightR.ok) {
      pushWarning(ctx, {
        code: 'master_not_found',
        detail: `teacher right: ${variant.right_filter.expected_name_hint ?? '?'}`,
      });
    } else {
      if (rightR.warning) pushWarning(ctx, rightR.warning);
      rightMaster = rightR.master;
    }
  } else {
    rightMaster = pickRightTeacherMaster(ctx);
    if (!rightMaster) {
      pushWarning(ctx, {
        code: 'no_right_teacher_master',
        detail: `subjects.length=${subjects.length}: ни half (>=2) ни full_class (>=1) — правая учительская страница пропущена`,
      });
    }
  }

  if (!rightMaster) return; // G нет — пушим только F (уже сделано)

  // 6. Правая страница
  const rightSubjects =
    variant.subjects_on_right !== undefined
      ? subjects.slice(variant.subjects_on_left, variant.subjects_on_left + variant.subjects_on_right)
      : [];
  ctx.spreads.push({
    spread_index: ctx.spreadCounter.value++,
    template_id: rightMaster.id,
    template_name: rightMaster.name,
    data: buildTeacherRightData(ctx, rightMaster, rightSubjects),
  });
}

/**
 * Заполнение левой страницы (F-*).
 * - headTeacherName/Role/Photo, headTextFrame
 * - teacherName_1..N, teacherRole_1..N, teacherPhoto_1..N (для F-Head-SmallGrid/LargeGrid)
 * - classPhotoFrame (только для F-Head-WithPhoto, признак — наличие placeholder в master)
 */
function buildTeacherLeftData(
  ctx: BuildContext,
  master: SpreadTemplate,
  head: HeadTeacher,
  sideTeachers: Subject[],
): Record<string, string | null> {
  const data: Record<string, string | null> = {
    headTeacherName: head.name,
    headTeacherRole: head.role,
    headTeacherPhoto: head.photo,
    headTextFrame: head.text,
  };
  // F-Head-SmallGrid (4) или F-Head-LargeGrid (8). Локальная нумерация с 1.
  // Для F-Head-WithPhoto sideTeachers пустой (subjects_on_left=0) — цикл не выполнится.
  for (let i = 0; i < sideTeachers.length; i++) {
    const n = i + 1;
    data[`teacherName_${n}`] = sideTeachers[i].name;
    data[`teacherRole_${n}`] = sideTeachers[i].role;
    data[`teacherPhoto_${n}`] = sideTeachers[i].photo;
  }
  // classPhotoFrame есть только в F-Head-WithPhoto (idml-recon §4)
  if (hasPlaceholder(master, 'classPhotoFrame')) {
    const fc = ctx.input.common_photos.full_class;
    if (fc.length >= 1) {
      data.classPhotoFrame = fc[0];
    } else {
      data.classPhotoFrame = null;
      pushWarning(ctx, {
        code: 'class_photo_missing',
        detail: `master ${master.name} has classPhotoFrame placeholder but common_photos.full_class is empty`,
      });
    }
  }
  return data;
}

/**
 * Заполнение правой страницы (G-*).
 * - G-Teachers-3x3/4x3/4x4: teacherName_1..N/teacherRole_1..N/teacherPhoto_1..N (локальная с 1)
 * - G-HalfClass: halfLeftPhoto, halfRightPhoto из common_photos.half[0..1]
 * - G-FullClass: classPhotoFrame из common_photos.full_class[0]
 */
function buildTeacherRightData(
  ctx: BuildContext,
  master: SpreadTemplate,
  rightSubjects: Subject[],
): Record<string, string | null> {
  const data: Record<string, string | null> = {};

  // G-Teachers-* — заполнение по subjects (локальная нумерация с 1)
  for (let i = 0; i < rightSubjects.length; i++) {
    const n = i + 1;
    data[`teacherName_${n}`] = rightSubjects[i].name;
    data[`teacherRole_${n}`] = rightSubjects[i].role;
    data[`teacherPhoto_${n}`] = rightSubjects[i].photo;
  }

  // G-HalfClass
  if (hasPlaceholder(master, 'halfLeftPhoto') && hasPlaceholder(master, 'halfRightPhoto')) {
    const half = ctx.input.common_photos.half;
    if (half.length >= 2) {
      data.halfLeftPhoto = half[0];
      data.halfRightPhoto = half[1];
    } else {
      // Включая случай half.length === 1 — оба null + warning
      data.halfLeftPhoto = half[0] ?? null;
      data.halfRightPhoto = null;
      pushWarning(ctx, {
        code: 'half_class_missing',
        detail: `master ${master.name} requires 2 half photos, got ${half.length}`,
      });
    }
  }

  // G-FullClass
  if (hasPlaceholder(master, 'classPhotoFrame')) {
    const fc = ctx.input.common_photos.full_class;
    if (fc.length >= 1) {
      data.classPhotoFrame = fc[0];
    } else {
      data.classPhotoFrame = null;
      pushWarning(ctx, {
        code: 'class_photo_missing',
        detail: `master ${master.name} has classPhotoFrame but common_photos.full_class is empty`,
      });
    }
  }

  return data;
}

/** Проверка что в placeholders мастера есть placeholder с заданным label. */
function hasPlaceholder(master: SpreadTemplate, label: string): boolean {
  for (let i = 0; i < master.placeholders.length; i++) {
    if (master.placeholders[i].label === label) return true;
  }
  return false;
}
```

---

## 4. `lib/album-builder/index.ts`

Добавить:

```typescript
export type { TeacherSpreadVariant, TeacherSection } from './scenarios';
export { TEACHER_SECTION_LAYFLAT } from './scenarios';
```

---

## 5. `scripts/smoke-album-builder.ts`

### 5.1 Расширить тип `Scene`

Добавить опциональное поле `commonPhotos`:

```typescript
type Scene = {
  // ... существующие поля без изменений
  commonPhotos?: Partial<CommonPhotos>;
};
```

Импортировать `CommonPhotos` из `'../lib/album-builder'` (он уже там экспортируется — проверить).

### 5.2 В `buildInput` мерджить `commonPhotos`

Заменить блок `common_photos: { full_class: [], ... }` на:

```typescript
common_photos: {
  full_class: scene.commonPhotos?.full_class ?? [],
  half: scene.commonPhotos?.half ?? [],
  quarter: scene.commonPhotos?.quarter ?? [],
  sixth: scene.commonPhotos?.sixth ?? [],
  collage: scene.commonPhotos?.collage ?? [],
},
```

### 5.3 Добавить `'2'` в `STUDENT_SETS`

```typescript
'2': [makeStudent('Алексеев', 4), makeStudent('Бочкарёв', 4)],
```

### 5.4 Добавить 10 новых сцен в `SCENES`

Старые 5 сцен оставляем как есть. После них добавляем:

```typescript
// ─── Учительский раздел (0.10b.2) ───
{
  label: 'teachers / 0 subjects + half photos',
  configType: 'standard', studentsKey: '2',
  subjectsCount: 0, withHeadTeacher: true,
  expect: {
    spreadsCount: 3,
    noWarningCodes: ['master_not_found', 'no_right_teacher_master'],
    masterNameSequence: ['F-Head-WithPhoto', 'G-HalfClass', 'E-Student-Standard'],
  },
  commonPhotos: { half: ['url-half-1', 'url-half-2'] },
},
{
  label: 'teachers / 0 subjects + only full_class',
  configType: 'standard', studentsKey: '2',
  subjectsCount: 0, withHeadTeacher: true,
  expect: {
    spreadsCount: 3,
    noWarningCodes: ['master_not_found', 'no_right_teacher_master'],
    masterNameSequence: ['F-Head-WithPhoto', 'G-FullClass', 'E-Student-Standard'],
  },
  commonPhotos: { full_class: ['url-fc-1'] },
},
{
  label: 'teachers / 0 subjects + no common photos',
  configType: 'standard', studentsKey: '2',
  subjectsCount: 0, withHeadTeacher: true,
  expect: {
    spreadsCount: 2,
    warningCodes: ['no_right_teacher_master', 'class_photo_missing'],
    masterNameSequence: ['F-Head-WithPhoto', 'E-Student-Standard'],
  },
},
{
  label: 'teachers / 4 subjects + full_class',
  configType: 'standard', studentsKey: '2',
  subjectsCount: 4, withHeadTeacher: true,
  expect: {
    spreadsCount: 3,
    noWarningCodes: ['master_not_found'],
    masterNameSequence: ['F-Head-SmallGrid', 'G-FullClass', 'E-Student-Standard'],
  },
  commonPhotos: { full_class: ['url-fc-1'] },
},
{
  label: 'teachers / 9 subjects',
  configType: 'standard', studentsKey: '2',
  subjectsCount: 9, withHeadTeacher: true,
  expect: {
    spreadsCount: 3,
    noWarningCodes: ['master_not_found'],
    masterNameSequence: ['F-Head-WithPhoto', 'G-Teachers-3x3', 'E-Student-Standard'],
  },
  commonPhotos: { full_class: ['url-fc-1'] },
},
{
  label: 'teachers / 12 subjects',
  configType: 'standard', studentsKey: '2',
  subjectsCount: 12, withHeadTeacher: true,
  expect: {
    spreadsCount: 3,
    noWarningCodes: ['master_not_found'],
    masterNameSequence: ['F-Head-WithPhoto', 'G-Teachers-4x3', 'E-Student-Standard'],
  },
  commonPhotos: { full_class: ['url-fc-1'] },
},
{
  label: 'teachers / 16 subjects',
  configType: 'standard', studentsKey: '2',
  subjectsCount: 16, withHeadTeacher: true,
  expect: {
    spreadsCount: 3,
    noWarningCodes: ['master_not_found'],
    masterNameSequence: ['F-Head-WithPhoto', 'G-Teachers-4x4', 'E-Student-Standard'],
  },
  commonPhotos: { full_class: ['url-fc-1'] },
},
{
  label: 'teachers / 20 subjects (overflow scenario)',
  configType: 'standard', studentsKey: '2',
  subjectsCount: 20, withHeadTeacher: true,
  expect: {
    spreadsCount: 3,
    noWarningCodes: ['subjects_overflow'],
    masterNameSequence: ['F-Head-LargeGrid', 'G-Teachers-4x4', 'E-Student-Standard'],
  },
},
{
  label: 'teachers / 28 subjects (degraded — обрезка до 24)',
  configType: 'standard', studentsKey: '2',
  subjectsCount: 28, withHeadTeacher: true,
  expect: {
    spreadsCount: 3,
    warningCodes: ['subjects_overflow'],
    masterNameSequence: ['F-Head-LargeGrid', 'G-Teachers-4x4', 'E-Student-Standard'],
  },
},
{
  label: 'teachers / no head_teacher',
  configType: 'standard', studentsKey: '2',
  subjectsCount: 5, withHeadTeacher: false,
  expect: {
    spreadsCount: 1,
    warningCodes: ['no_head_teacher'],
    masterNameSequence: ['E-Student-Standard'],
  },
},
```

Итого должно быть **15 сцен** (5 старых + 10 новых).

---

## Порядок действий

1. Внести правки в 5 файлов (см. секции 1-5 выше)
2. `cp .env.local .env.local.real-backup`
3. Записать fake `.env.local` (стандартный шаблон из project instructions)
4. `npx tsc --noEmit --project .` → пусто
5. `npx next build` → зелёный
6. `rm -f .env.local && mv .env.local.real-backup .env.local`
7. `set -a && . ./.env.local && set +a && npx tsx scripts/smoke-album-builder.ts`
   → `Result: 15/15 scenes passed`, exit 0
8. Если меньше — НЕ коммить, репортни в чат с конкретным выводом
9. **Удалить этот файл-инструкцию:** `rm docs/internal/0.10b.2-instructions.md`
   (если папка `docs/internal/` после удаления станет пустой — удалить её тоже:
   `rmdir docs/internal 2>/dev/null || true`)
10. `git add lib/album-builder/ scripts/smoke-album-builder.ts docs/internal/`
    → проверить что файл-инструкция в staging как deleted
11. `git status` — должно быть:
    ```
    D  docs/internal/0.10b.2-instructions.md  (deleted)
    M  lib/album-builder/build.ts
    M  lib/album-builder/index.ts
    M  lib/album-builder/scenarios.ts
    M  lib/album-builder/types.ts
    M  scripts/smoke-album-builder.ts
    ```
12. `git commit -m "feat(layout/0.10b.2): учительский раздел layflat (F+G, 7 вариантов)"`
13. `git push origin feature/layout-engine`

## Отчёт

- Hash коммита
- tsc/build статусы
- Smoke результат (Result: N/N)
- Размеры файлов в строках (lib/album-builder/, изменения)

## Анти-цели

- Никакого Медиума — это для 0.10b.3
- Никакого Mini-soft — это для 0.11
- Никакого общего раздела J-* — НЕ генерируется в фазе 0
- Не трогать `student_section` Стандарта/Универсала/Максимума — они в 0.10a.2 готовы
- Контекст-файл — НЕ обновляем (это 0.14)
