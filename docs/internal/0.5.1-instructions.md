# Инструкция для подэтапа 0.5.1 — миграция БД для пресетов

> Это временный файл-инструкция. После выполнения подэтапа удалить и закоммитить вместе с основными изменениями.

## Цель подэтапа

Первый подэтап фазы 0.5. **Только структура БД** — никакого кода.

После этого подэтапа:
- В БД есть таблица `config_presets` (пустая, заполним в 0.5.2)
- В `albums` нет старого поля `config_type` (оно было временным из 1.0)
- В `albums` есть новые поля `config_preset_id` (FK на config_presets) и `template_set_id` (FK на template_sets)
- В `children` есть новое поле `config_preset_id` (для override per-child)
- Builder продолжает работать как раньше (использует жёстко-зашитые SCENARIOS)

Один коммит:
```
feat(layout/0.5.1): миграция БД — таблица config_presets + FK в albums и children
```

## Контекст

Из `docs/phase-0.5-spec.md` (v4):

> **Цель фазы 0.5:** перенести описание комплектаций из кода в БД. 7 текущих пресетов мигрируют в таблицу как «глобальные дефолты». Builder становится универсальным.

Подэтап 0.5.1 — закладывает фундамент. Создаёт пустые структуры в БД. Сами 7 пресетов будут добавлены seed-данными в 0.5.2.

**Backward compatibility:** после 0.5.1 builder продолжает работать на жёстко-зашитых SCENARIOS как в фазе 0. Никакой код не трогаем. Build Test, smoke-скрипт, всё работает как раньше.

## Точка старта

```bash
git checkout main
git pull origin main
git status  # должно быть чисто
```

HEAD = `717d345` (docs phase-0.5 v4).

## Файлы к правке

Только 1 элемент:
1. `migrations/2026-05-08-config-presets.sql` — **новый файл миграции**

Никакого TypeScript кода. Никакого UI. Никакого smoke.

---

## 1. Миграция БД

**ШАГ 1.1 — выполнить SQL в Supabase Studio.**

```sql
-- Миграция 0.5.1 — фундамент для гибких пресетов
-- 
-- Создаём таблицу config_presets (пустая, наполнится в 0.5.2 seed).
-- Удаляем временное поле albums.config_type (было добавлено в 1.0).
-- Добавляем FK в albums и children.

-- 1. Таблица config_presets
CREATE TABLE config_presets (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  
  tenant_id uuid REFERENCES tenants(id) ON DELETE CASCADE,
    -- NULL = глобальный пресет (доступен всем тенантам)
  
  slug text NOT NULL,
  name text NOT NULL,
  description text,
  
  print_type text NOT NULL CHECK (print_type IN ('layflat', 'soft')),
  
  config jsonb NOT NULL,
    -- Богатая структура (см. phase-0.5-spec.md)
  
  is_template boolean DEFAULT false,
  
  created_by uuid REFERENCES users(id),
  created_at timestamptz DEFAULT now(),
  updated_at timestamptz DEFAULT now(),
  
  CONSTRAINT unique_tenant_slug_preset UNIQUE NULLS NOT DISTINCT (tenant_id, slug)
);

CREATE INDEX idx_config_presets_tenant ON config_presets(tenant_id);

COMMENT ON TABLE config_presets IS 'Пресеты комплектаций альбомов. Заполняется в 0.5.2 (7 глобальных).';
COMMENT ON COLUMN config_presets.config IS 'Богатая структура параметров (JSONB). См. docs/phase-0.5-spec.md часть "Полная структура config".';
COMMENT ON COLUMN config_presets.tenant_id IS 'NULL = глобальный пресет, видимый всем тенантам. UUID = персональный пресет тенанта.';

-- 2. Изменения в albums
-- Удаляем временное поле config_type (было добавлено в подэтапе 1.0)
ALTER TABLE albums DROP COLUMN IF EXISTS config_type;

-- Добавляем новые FK
ALTER TABLE albums
ADD COLUMN config_preset_id uuid REFERENCES config_presets(id),
ADD COLUMN template_set_id uuid REFERENCES template_sets(id);

COMMENT ON COLUMN albums.config_preset_id IS 'Базовая комплектация альбома (FK на config_presets). NULL = не задана.';
COMMENT ON COLUMN albums.template_set_id IS 'Дизайн (набор мастеров) альбома (FK на template_sets). NULL = по умолчанию.';

-- print_type оставляем (был добавлен в 1.0)

-- 3. Изменения в children
ALTER TABLE children
ADD COLUMN config_preset_id uuid REFERENCES config_presets(id);

COMMENT ON COLUMN children.config_preset_id IS 'Персональная комплектация ученика (FK). NULL = используется albums.config_preset_id (альбомная).';

-- 4. Backfill — никакой. Все существующие альбомы и дети получают NULL.
-- В фазе 1.3 UI запросит выбор пресета перед первой автосборкой.

-- 5. Проверка
SELECT 
  (SELECT COUNT(*) FROM config_presets) AS presets_count,
  (SELECT COUNT(*) FROM albums) AS albums_count,
  (SELECT COUNT(*) FROM albums WHERE config_preset_id IS NULL) AS albums_without_preset,
  (SELECT COUNT(*) FROM children) AS children_count,
  (SELECT COUNT(*) FROM children WHERE config_preset_id IS NULL) AS children_without_preset;

-- Ожидаемый результат:
--   presets_count = 0 (пустая таблица — заполнится в 0.5.2)
--   albums_count = 9 (текущее состояние)
--   albums_without_preset = 9 (все NULL после миграции)
--   children_count = 157
--   children_without_preset = 157
```

**ШАГ 1.2 — после применения создать копию файла миграции в репо.**

Файл: `migrations/2026-05-08-config-presets.sql` с тем же содержимым SQL.

---

## 2. Что в коде НЕ трогаем

**Запрещено в 0.5.1:**
- Любые правки в `lib/album-builder/`
- Любые правки в `app/api/`
- Любые правки в `app/app/page.tsx`
- Любые правки smoke-скрипта
- Любые правки `app/super/templates/`

Это всё будет в 0.5.2-0.5.6. Сейчас **только** SQL миграция.

**Проверки которые нужны:**
- `npx tsc --noEmit --project .` → должно быть пусто
- `npx next build` → зелёный  
- Smoke 58/58 → как было
- (build/smoke даже не должны быть затронуты — мы код не меняли)

---

## Что произойдёт после 0.5.1

- БД готова к фазе 0.5
- Builder работает **как раньше** через старый `config_type`. Стоп — но мы же удалили это поле!

**Внимание:** удаление `albums.config_type` означает что текущий API (если он использует это поле) **может сломаться**. Нужно проверить.

Грепом найди все места в коде где используется `albums.config_type`:

```bash
grep -rn "config_type" --include="*.ts" --include="*.tsx" .
```

Если найдены ссылки в:
- `app/api/tenant/route.ts` — в allowedFields для update_album (был добавлен в 1.0)
- `app/app/page.tsx` — в типе Album и в отображении (было добавлено в 1.0)

То **они нужно временно нейтрализовать**, чтобы не было ошибок:
- Из allowedFields убрать `config_type` (оставить только `print_type` пока)
- В UI показ конфигурации `album.config_type` сделать ссылкой на `album.config_preset_id?.slug ?? null` или просто закомментировать показ `config_type` (`print_type` пока показывает)

Это **минимальная зачистка** — мы потом в 0.5.6 сделаем полноценный UI.

**Грепом проверь и убери все ссылки на `albums.config_type` чтобы код не падал.**

## Порядок действий

1. **СНАЧАЛА** выполнить SQL в Supabase Studio (см. шаг 1.1)
2. Создать `migrations/2026-05-08-config-presets.sql`
3. Грепом найти все ссылки `albums.config_type`:
   ```bash
   grep -rn "config_type" --include="*.ts" --include="*.tsx" .
   ```
   (Должны найтись в `app/api/tenant/route.ts` и `app/app/page.tsx`)
4. Из allowedFields в tenant/route.ts убрать `'config_type'`
5. В `app/app/page.tsx` убрать показ `album.config_type` (или оставить только `album.print_type` если хочется)
6. `npx tsc --noEmit --project .` → пусто
7. `npx next build` → зелёный
8. `cp .env.local .env.local.real-backup`
9. fake `.env.local`
10. (повторить tsc/build с fake env, потом восстановить)
11. `set -a && . ./.env.local && set +a && npx tsx scripts/smoke-album-builder.ts` → 58/58 (как было — мы код album-builder не трогали)
12. Удалить файл-инструкцию: `rm docs/internal/0.5.1-instructions.md && rmdir docs/internal 2>/dev/null || true`
13. `git add migrations/ app/ docs/internal/`
14. `git status` — должно быть:
    ```
    A  migrations/2026-05-08-config-presets.sql
    D  docs/internal/0.5.1-instructions.md
    M  app/api/tenant/route.ts        (убран 'config_type' из allowedFields)
    M  app/app/page.tsx               (убран показ album.config_type)
    ```
15. `git commit -m "feat(layout/0.5.1): миграция БД — таблица config_presets + FK в albums и children"`
16. **НЕ пушить** — дождись OK Сергея

## Отчёт

- Hash коммита (локальный)
- tsc/build/smoke статусы
- Список файлов где ссылка на config_type была удалена
- Размеры изменений в строках

## Анти-цели

- Никакой работы с lib/album-builder/
- Никакого создания TypeScript типов Preset (это 0.5.2)
- Никаких seed-данных в config_presets (это 0.5.2)
- Контекст-файл — НЕ обновляем (это 0.5.7)
