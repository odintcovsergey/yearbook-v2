# Инструкция для подэтапа 0.11.2 (ВЕРСИЯ 2 — переписана после отката)

> Это временный файл-инструкция. После выполнения подэтапа удалить и закоммитить вместе с основными изменениями.

## Контекст

Первая попытка 0.11.2 была откатана через `git reset --soft`. В ней были архитектурные костыли:
- `base_slots_per_page` поле в StudentSection (костыль из-за неправильного использования pickAdaptiveGrid для total > maxOnBase)
- `findOverflowRowRight` функция с поиском по имени (вместо семантического поиска)

Эта версия инструкции исправляет оба костыля архитектурно правильным способом:
1. `pickAdaptiveGrid` вызывается с **clamped total** = `min(total, basePages × maxCapacity)`. Так выбор мастера для сетки определяется только тем что помещается на базовых страницах. Overflow-логика отдельно.
2. L-Overflow-Row-Right теперь имеет page_role `student_overflow_right` (миграция БД уже применена). Поиск через стандартный findMaster.

## Цель подэтапа

Добавить overflow-логику для Лайт (25-32) и Мини (25-36).

Один коммит:
```
feat(layout/0.11.2): overflow Лайт (25-32) и Мини (25-36) + page_role student_overflow_right
```

## Состояние БД (миграция уже применена через Supabase Studio)

```
L-Overflow-Row:        page_role=student_overflow,       slot_capacity={students:3, photos_full:1}
L-Overflow-Row-Right:  page_role=student_overflow_right, slot_capacity={students:3, photos_full:1}
N-Overflow-Row:        page_role=student_overflow,       slot_capacity={students:4, photos_full:1}
```

Изменения в БД: добавлен `page_role='student_overflow_right'` в CHECK-constraint, L-Overflow-Row-Right переведён на эту роль. Файл миграции **нужно создать** в этом подэтапе (см. шаг 1).

## Точка старта работы

HEAD = `55817dc` (инструкция-коммит). Working tree содержит изменения от первой попытки 0.11.2 — их **полностью отменить**:

```bash
git restore --staged --worktree lib/album-builder/build.ts
git restore --staged --worktree lib/album-builder/scenarios.ts
git restore --staged --worktree scripts/smoke-album-builder.ts
git status
```

После этого build.ts/scenarios.ts/smoke идентичны коммиту e7176a9 (0.11.1).

## Файлы к правке

5 элементов:
1. `migrations/2026-05-07-page-role-overflow-right.sql` — **новый файл миграции**
2. `lib/album-builder/types.ts` — расширить `PageRole` (добавить `student_overflow_right`)
3. `lib/album-builder/scenarios.ts` — новый тип `AdaptiveGridOverflow`, расширить `StudentSection`, добавить `overflow` в `light` и `mini`
4. `lib/album-builder/build.ts` — `pickAdaptiveGridMaxCapacity` + clamping в buildAdaptiveGridStudents + overflow-логика + buildOverflowRow
5. `scripts/smoke-album-builder.ts` — 8 сцен overflow

---

## 1. `migrations/2026-05-07-page-role-overflow-right.sql`

Новый файл (миграция уже выполнена в Supabase Studio, фиксируем для repeatability):

```sql
-- Миграция 0.11.2 — добавление page_role 'student_overflow_right'
-- для L-Overflow-Row-Right (правая страница overflow в Лайт 31-32).
-- Семантическое различие убирает костыль "поиск по имени".

ALTER TABLE spread_templates
DROP CONSTRAINT IF EXISTS spread_templates_page_role_check;

ALTER TABLE spread_templates
ADD CONSTRAINT spread_templates_page_role_check CHECK (
  page_role IS NULL OR page_role IN (
    'student',
    'student_left',
    'student_right',
    'student_grid',
    'student_grid_left',
    'student_grid_right',
    'student_overflow',
    'student_overflow_right',
    'student_last',
    'teacher_left',
    'teacher_right',
    'common',
    'intro',
    'cover'
  )
);

UPDATE spread_templates
SET page_role = 'student_overflow_right'
WHERE template_set_id = (SELECT id FROM template_sets WHERE slug = 'okeybook-default')
  AND name = 'L-Overflow-Row-Right';

-- Проверка
SELECT name, page_role, slot_capacity
FROM spread_templates
WHERE template_set_id = (SELECT id FROM template_sets WHERE slug = 'okeybook-default')
  AND name LIKE '%Overflow%'
ORDER BY name;
```

---

## 2. `lib/album-builder/types.ts`

Расширить `PageRole`:

```typescript
export type PageRole =
  | 'student'
  | 'student_left'
  | 'student_right'
  | 'student_grid'
  | 'student_grid_left'
  | 'student_grid_right'
  | 'student_overflow'
  | 'student_overflow_right'  // ← НОВОЕ
  | 'student_last'
  | 'teacher_left'
  | 'teacher_right'
  | 'common'
  | 'intro'
  | 'cover';
```

В JSDoc:
```
 * - `student_overflow_right` — правая overflow-страница в Лайт 31-32
 *                              (L-Overflow-Row-Right, добавлено в 0.11.2)
```

И обновить ссылку на миграции: `(миграции 0.8.6.1 + 0.10a.1 + 0.10b.1 + 0.11.2)`.

`BuildWarningCode` — без изменений.

---

## 3. `lib/album-builder/scenarios.ts`

### 3.1 Новый тип `AdaptiveGridOverflow`

Добавить ПОСЛЕ типа `LastSpread`:

```typescript
/**
 * Конфигурация overflow для adaptive_grid комплектаций (Лайт/Мини).
 *
 * Применяется когда total > base_pages * slotsPerPage в buildAdaptiveGridStudents.
 *
 * Алгоритм веток (после заполнения base_pages):
 *  - overflow ≤ row_capacity (3 для L, 4 для N) → single overflow-row
 *  - overflow ≤ slotsPerPage → extra обычная сетка (страница leftMaster, неполная)
 *  - overflow > slotsPerPage → extra полная сетка + overflow-row-right
 *    (только Лайт 31-32; для Мини не достижимо: 36-24=12, 12 ≤ slotsPerPage=12)
 *
 * row_capacity берётся из найденного master.slot_capacity.students.
 */
export type AdaptiveGridOverflow = {
  /**
   * Фильтр для overflow-row LEFT мастера (page_role='student_overflow').
   * Дискриминация L vs N через slot_capacity_min.students.
   */
  row_filter: MasterFilter;
  /**
   * Фильтр для overflow-row RIGHT мастера (page_role='student_overflow_right').
   * Опционально, нужно только для Лайт 31-32.
   */
  row_right_filter?: MasterFilter;
};
```

### 3.2 Расширить `StudentSection`

Добавить поле:

```typescript
export type StudentSection = {
  // ... существующие поля
  /**
   * Overflow-стратегия для adaptive_grid комплектаций. Применяется только
   * при right_filter_mode === 'adaptive_grid' и total > base_pages*slotsPerPage.
   * Если не задана — overflow обрезается с warning students_overflow.
   */
  overflow?: AdaptiveGridOverflow;
};
```

### 3.3 Добавить `overflow` в `light` сценарий

В `SCENARIOS.light.student_section`:

```typescript
overflow: {
  row_filter: {
    page_role: 'student_overflow',
    applies_to_config: 'light',
    slot_capacity_min: { students: 3, photos_full: 1 },
    expected_name_hint: 'L-Overflow-Row',
  },
  row_right_filter: {
    page_role: 'student_overflow_right',
    applies_to_config: 'light',
    slot_capacity_min: { students: 3, photos_full: 1 },
    expected_name_hint: 'L-Overflow-Row-Right',
  },
},
```

**Примечание:** `students: 3` для row_filter (точная capacity L-Overflow-Row). У L-Overflow-Row и N-Overflow-Row одинаковая applies_to_configs (общеупотребимая), различаются по `students`. Без `students: 3` в light findMaster найдёт оба и возьмёт первого по sort_order. Дискриминация через ёмкость.

### 3.4 Добавить `overflow` в `mini` сценарий

```typescript
overflow: {
  row_filter: {
    page_role: 'student_overflow',
    applies_to_config: 'mini',
    slot_capacity_min: { students: 4, photos_full: 1 },
    expected_name_hint: 'N-Overflow-Row',
  },
  // row_right_filter не задаём — Мини max 36-24=12, не доходит до grid+row
},
```

---

## 4. `lib/album-builder/build.ts`

### 4.1 Импорт

Если ещё нет — добавить `AdaptiveGridOverflow` в импорт из './scenarios'. Но в коде build.ts он не используется явно (только через `section.overflow`) — если линтер запрещает unused, то не импортировать.

### 4.2 Новая функция `pickAdaptiveGridMaxCapacity`

Добавить ПЕРЕД `pickAdaptiveGrid`:

```typescript
/**
 * Возвращает adaptive_grid мастер с МАКСИМАЛЬНОЙ slot_capacity.students
 * среди доступных в БД для текущего page_role и applies_to_config.
 *
 * Используется в buildAdaptiveGridStudents для clamping: чтобы понять сколько
 * учеников максимум помещается на basePages с самым «вместительным» мастером,
 * нужно знать его capacity ДО выбора оптимального через pickAdaptiveGrid.
 *
 * Если кандидатов нет — возвращает null.
 */
function pickAdaptiveGridMaxCapacity(
  ctx: BuildContext,
  baseFilter: MasterFilter,
): SpreadTemplate | null {
  const filter = { ...baseFilter, applies_to_config: ctx.config.config_type };
  const candidates = ctx.config.template_set.spreads.filter((s) => {
    if (s.page_role !== filter.page_role) return false;
    if (s.applies_to_configs.indexOf(filter.applies_to_config) < 0) return false;
    return s.slot_capacity !== null && typeof s.slot_capacity.students === 'number';
  });

  if (candidates.length === 0) return null;

  candidates.sort((a, b) => {
    const aCap = a.slot_capacity?.students ?? 0;
    const bCap = b.slot_capacity?.students ?? 0;
    return bCap - aCap;  // DESC — максимум первым
  });

  return candidates[0];
}
```

### 4.3 Полная переписка `buildAdaptiveGridStudents`

Заменить функцию целиком:

```typescript
function buildAdaptiveGridStudents(
  ctx: BuildContext,
  section: StudentSection,
): void {
  if (!section.base_pages || section.base_pages < 1) {
    pushWarning(ctx, {
      code: 'master_not_found',
      detail: 'adaptive_grid scenario requires base_pages >= 1',
    });
    return;
  }
  if (!section.grid_filter_left || !section.grid_filter_right) {
    pushWarning(ctx, {
      code: 'master_not_found',
      detail: 'adaptive_grid scenario requires grid_filter_left and grid_filter_right',
    });
    return;
  }

  const total = ctx.input.students.length;
  const basePages = section.base_pages;

  // Clamping: для подбора мастера используем не total, а min(total, basePages*maxCapacity).
  // Так overflow не «отравляет» выбор сетки.
  const maxLeftMaster = pickAdaptiveGridMaxCapacity(ctx, section.grid_filter_left);
  if (!maxLeftMaster) {
    pushWarning(ctx, {
      code: 'master_not_found',
      detail: `adaptive_grid LEFT: no candidates for page_role=${section.grid_filter_left.page_role} applies_to=${ctx.config.config_type}`,
    });
    return;
  }
  const maxCapacityPerPage = maxLeftMaster.slot_capacity?.students ?? 0;
  if (maxCapacityPerPage < 1) {
    pushWarning(ctx, {
      code: 'master_not_found',
      detail: `adaptive_grid LEFT: max master ${maxLeftMaster.name} has slot_capacity.students < 1`,
    });
    return;
  }
  const totalForBase = Math.min(total, basePages * maxCapacityPerPage);

  // Теперь подбираем оптимальный (минимально достаточный) мастер для totalForBase
  const leftMaster = pickAdaptiveGrid(ctx, section.grid_filter_left, totalForBase, basePages);
  const rightMaster = pickAdaptiveGrid(ctx, section.grid_filter_right, totalForBase, basePages);

  if (!leftMaster || !rightMaster) {
    pushWarning(ctx, {
      code: 'master_not_found',
      detail: `adaptive_grid: missing left or right master for ${ctx.config.config_type}`,
    });
    return;
  }

  const slotsPerPage = leftMaster.slot_capacity?.students ?? 0;
  if (slotsPerPage < 1) {
    pushWarning(ctx, {
      code: 'master_not_found',
      detail: `adaptive_grid LEFT: ${leftMaster.name} has slot_capacity.students < 1`,
    });
    return;
  }

  const hasQuote = section.has_quote === true;
  const maxOnBase = basePages * slotsPerPage;

  // 1. Базовые страницы
  const baseStudents = ctx.input.students.slice(0, Math.min(total, maxOnBase));
  for (let i = 0; i < basePages; i++) {
    const slice = baseStudents.slice(i * slotsPerPage, (i + 1) * slotsPerPage);
    if (slice.length === 0) break;
    const isLeft = i % 2 === 0;
    const master = isLeft ? leftMaster : rightMaster;

    ctx.spreads.push({
      spread_index: ctx.spreadCounter.value++,
      template_id: master.id,
      template_name: master.name,
      data: buildGridStudentData(slice, slotsPerPage, hasQuote),
    });
  }

  // 2. Overflow
  const overflowCount = total - maxOnBase;
  if (overflowCount <= 0) return;

  const ovf = section.overflow;
  if (!ovf) {
    pushWarning(ctx, {
      code: 'students_overflow',
      detail: `${ctx.config.config_type}: total=${total} > maxOnBase=${maxOnBase}, overflow strategy не задана`,
    });
    return;
  }

  // Найти overflow-row мастер чтобы узнать rowCapacity
  const rowFilter = { ...ovf.row_filter, applies_to_config: ctx.config.config_type };
  const rowR = findMaster(ctx.config.template_set.spreads, rowFilter);
  if (!rowR.ok) {
    pushWarning(ctx, {
      code: 'master_not_found',
      detail: `overflow row: ${ovf.row_filter.expected_name_hint ?? '?'}`,
    });
    return;
  }
  if (rowR.warning) pushWarning(ctx, rowR.warning);
  const rowCapacity = rowR.master.slot_capacity?.students ?? 0;

  const overflowStudents = ctx.input.students.slice(maxOnBase);

  if (overflowCount <= rowCapacity) {
    // Ветка single_row
    buildOverflowRow(ctx, rowR.master, overflowStudents, rowCapacity);
  } else if (overflowCount <= slotsPerPage) {
    // Ветка extra_grid: одна неполная сетка на нечётной странице (leftMaster)
    ctx.spreads.push({
      spread_index: ctx.spreadCounter.value++,
      template_id: leftMaster.id,
      template_name: leftMaster.name,
      data: buildGridStudentData(overflowStudents, slotsPerPage, hasQuote),
    });
  } else {
    // Ветка grid_plus_row (Лайт 31-32)
    if (!ovf.row_right_filter) {
      pushWarning(ctx, {
        code: 'master_not_found',
        detail: `overflow grid_plus_row: row_right_filter не задан, overflowCount=${overflowCount}`,
      });
      return;
    }
    const rightF = { ...ovf.row_right_filter, applies_to_config: ctx.config.config_type };
    const rightR = findMaster(ctx.config.template_set.spreads, rightF);
    if (!rightR.ok) {
      pushWarning(ctx, {
        code: 'master_not_found',
        detail: `overflow row_right: ${ovf.row_right_filter.expected_name_hint ?? '?'}`,
      });
      return;
    }
    if (rightR.warning) pushWarning(ctx, rightR.warning);

    // Полная extra-сетка (нечётная страница, leftMaster)
    const fullSlice = overflowStudents.slice(0, slotsPerPage);
    ctx.spreads.push({
      spread_index: ctx.spreadCounter.value++,
      template_id: leftMaster.id,
      template_name: leftMaster.name,
      data: buildGridStudentData(fullSlice, slotsPerPage, hasQuote),
    });

    // Overflow-row-right (чётная страница)
    const remainSlice = overflowStudents.slice(slotsPerPage);
    const rightRowCapacity = rightR.master.slot_capacity?.students ?? rowCapacity;
    buildOverflowRow(ctx, rightR.master, remainSlice, rightRowCapacity);
  }
}
```

### 4.4 Новая функция `buildOverflowRow`

Добавить после `buildAdaptiveGridStudents`:

```typescript
/**
 * Сборка overflow-row страницы для Лайт/Мини.
 *
 * Заполняет:
 *  - studentportrait_N, studentname_N для всех students (без quote — Лайт/Мини без цитат)
 *  - null'ы для пустых слотов
 *  - classphotoframe из common_photos.full_class[0] (если есть placeholder)
 */
function buildOverflowRow(
  ctx: BuildContext,
  master: SpreadTemplate,
  students: Student[],
  rowCapacity: number,
): void {
  const data = buildGridStudentData(students, rowCapacity, false);

  if (hasPlaceholder(master, 'classphotoframe')) {
    const fc = ctx.input.common_photos.full_class;
    if (fc.length >= 1) {
      data.classphotoframe = fc[0];
    } else {
      data.classphotoframe = null;
      pushWarning(ctx, {
        code: 'class_photo_missing',
        detail: `master ${master.name} has classphotoframe placeholder but common_photos.full_class is empty`,
      });
    }
  }

  ctx.spreads.push({
    spread_index: ctx.spreadCounter.value++,
    template_id: master.id,
    template_name: master.name,
    data,
  });
}
```

---

## 5. `scripts/smoke-album-builder.ts`

### 5.1 Расширить `STUDENT_SETS`

```typescript
'26': Array.from({ length: 26 }, (_, i) => makeStudent(`Ученик${i + 1}`, 0)),
'27': Array.from({ length: 27 }, (_, i) => makeStudent(`Ученик${i + 1}`, 0)),
'28': Array.from({ length: 28 }, (_, i) => makeStudent(`Ученик${i + 1}`, 0)),
'29': Array.from({ length: 29 }, (_, i) => makeStudent(`Ученик${i + 1}`, 0)),
'30': Array.from({ length: 30 }, (_, i) => makeStudent(`Ученик${i + 1}`, 0)),
'32': Array.from({ length: 32 }, (_, i) => makeStudent(`Ученик${i + 1}`, 0)),
'36': Array.from({ length: 36 }, (_, i) => makeStudent(`Ученик${i + 1}`, 0)),
```

### 5.2 Добавить 8 сцен overflow

После существующих 36 сцен:

```typescript
// ─── Лайт overflow (0.11.2) ───

{
  label: 'light / 27 students no full_class (warning class_photo_missing)',
  configType: 'light', studentsKey: '27',
  subjectsCount: 0, withHeadTeacher: false,
  expect: {
    spreadsCount: 5,
    warningCodes: ['class_photo_missing'],
    masterNameSequence: ['L-6-Left', 'L-6-Right', 'L-6-Left', 'L-6-Right', 'L-Overflow-Row'],
  },
},
{
  label: 'light / 27 students + full_class (overflow_row заполнен)',
  configType: 'light', studentsKey: '27',
  subjectsCount: 0, withHeadTeacher: false,
  expect: {
    spreadsCount: 5,
    noWarningCodes: ['master_not_found', 'students_overflow', 'class_photo_missing'],
    masterNameSequence: ['L-6-Left', 'L-6-Right', 'L-6-Left', 'L-6-Right', 'L-Overflow-Row'],
  },
  commonPhotos: { full_class: ['url-fc-1'] },
},
{
  label: 'light / 28 students (overflow 4 → extra L-6-Left неполная)',
  configType: 'light', studentsKey: '28',
  subjectsCount: 0, withHeadTeacher: false,
  expect: {
    spreadsCount: 5,
    noWarningCodes: ['master_not_found', 'students_overflow', 'class_photo_missing'],
    masterNameSequence: ['L-6-Left', 'L-6-Right', 'L-6-Left', 'L-6-Right', 'L-6-Left'],
  },
},
{
  label: 'light / 30 students (overflow 6 → полная extra L-6-Left)',
  configType: 'light', studentsKey: '30',
  subjectsCount: 0, withHeadTeacher: false,
  expect: {
    spreadsCount: 5,
    noWarningCodes: ['master_not_found', 'students_overflow', 'class_photo_missing'],
    masterNameSequence: ['L-6-Left', 'L-6-Right', 'L-6-Left', 'L-6-Right', 'L-6-Left'],
  },
},
{
  label: 'light / 32 students + full_class (extra grid + L-Overflow-Row-Right)',
  configType: 'light', studentsKey: '32',
  subjectsCount: 0, withHeadTeacher: false,
  expect: {
    spreadsCount: 6,
    noWarningCodes: ['master_not_found', 'students_overflow', 'class_photo_missing'],
    masterNameSequence: [
      'L-6-Left', 'L-6-Right', 'L-6-Left', 'L-6-Right',
      'L-6-Left',
      'L-Overflow-Row-Right',
    ],
  },
  commonPhotos: { full_class: ['url-fc-1'] },
},

// ─── Мини overflow (0.11.2) ───

{
  label: 'mini / 26 students + full_class (overflow 2 → N-Overflow-Row)',
  configType: 'mini', studentsKey: '26',
  subjectsCount: 0, withHeadTeacher: false,
  expect: {
    spreadsCount: 3,
    noWarningCodes: ['master_not_found', 'students_overflow', 'class_photo_missing'],
    masterNameSequence: ['N-12-Left', 'N-12-Right', 'N-Overflow-Row'],
  },
  commonPhotos: { full_class: ['url-fc-1'] },
},
{
  label: 'mini / 29 students (overflow 5 → extra N-12-Left неполная)',
  configType: 'mini', studentsKey: '29',
  subjectsCount: 0, withHeadTeacher: false,
  expect: {
    spreadsCount: 3,
    noWarningCodes: ['master_not_found', 'students_overflow', 'class_photo_missing'],
    masterNameSequence: ['N-12-Left', 'N-12-Right', 'N-12-Left'],
  },
},
{
  label: 'mini / 36 students (overflow 12 → полная extra N-12-Left)',
  configType: 'mini', studentsKey: '36',
  subjectsCount: 0, withHeadTeacher: false,
  expect: {
    spreadsCount: 3,
    noWarningCodes: ['master_not_found', 'students_overflow', 'class_photo_missing'],
    masterNameSequence: ['N-12-Left', 'N-12-Right', 'N-12-Left'],
  },
},
```

После добавления должно быть **44 сцены** (36 + 8).

---

## Порядок действий

1. Откатить старые изменения 0.11.2:
   ```bash
   git restore --staged --worktree lib/album-builder/build.ts
   git restore --staged --worktree lib/album-builder/scenarios.ts
   git restore --staged --worktree scripts/smoke-album-builder.ts
   ```
2. Создать `migrations/2026-05-07-page-role-overflow-right.sql` с содержимым из шага 1
3. Внести правки в 4 файла кода (types.ts, scenarios.ts, build.ts, scripts/smoke-album-builder.ts)
4. `cp .env.local .env.local.real-backup`
5. fake `.env.local`
6. `npx tsc --noEmit --project .` → пусто
7. `npx next build` → зелёный
8. `rm -f .env.local && mv .env.local.real-backup .env.local`
9. `set -a && . ./.env.local && set +a && npx tsx scripts/smoke-album-builder.ts`
   → `Result: 44/44 scenes passed`, exit 0
10. Если меньше — НЕ коммить, репортни в чат
11. Удалить файл-инструкцию: `rm docs/internal/0.11.2-instructions.md`
    `rmdir docs/internal 2>/dev/null || true`
12. `git add lib/album-builder/ scripts/smoke-album-builder.ts migrations/ docs/internal/`
13. `git status` — должно быть:
    ```
    A  migrations/2026-05-07-page-role-overflow-right.sql
    D  docs/internal/0.11.2-instructions.md
    M  lib/album-builder/build.ts
    M  lib/album-builder/scenarios.ts
    M  lib/album-builder/types.ts
    M  scripts/smoke-album-builder.ts
    ```
    (index.ts, find-master.ts НЕ должны быть в staging)
14. `git commit -m "feat(layout/0.11.2): overflow Лайт (25-32) и Мини (25-36) + page_role student_overflow_right"`
15. **НЕ пушить** — дождись OK от Сергея

## Отчёт

- Hash коммита (локальный)
- tsc/build статусы
- Smoke результат (Result: N/N)
- Размеры файлов в строках (изменения)
- **Подтверждение что нет `findOverflowRowRight` или `base_slots_per_page` в коде** (грепом)

## Анти-цели

- Никаких костылей с поиском по имени (`findOverflowRowRight` забыть)
- Никаких полей `base_slots_per_page`, `base_max_capacity` и подобных — только pickAdaptiveGridMaxCapacity
- Никакого Индивидуального — это 0.11.3
- Никакого Mini-soft — это 0.11.4
- Не пытаться угадывать — если smoke падает, репортнуть и ждать
- Контекст-файл — НЕ обновляем (это 0.14)
