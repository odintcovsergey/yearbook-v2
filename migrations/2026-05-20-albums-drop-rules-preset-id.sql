-- РЭ.21.8.чистка-1 (20.05.2026): удаление колонки albums.rules_preset_id
--
-- Колонка использовалась движком 2 (buildFromRules, РЭ.16.2) для
-- маршрутизации сборки альбома. Движок 2 удалён в РЭ.21.8.чистка-1.
-- Колонка больше не читается ни одним endpoint'ом.
--
-- Если у вас в БД есть альбомы с непустым rules_preset_id — их сборка
-- автоматически продолжит работать через legacy buildAlbum (config_preset_id)
-- или section_structure (если задано). Никакого функционального изменения
-- не происходит.
--
-- ВНИМАНИЕ: операция необратима. Если нужно сохранить значения для
-- архива — снять SELECT перед применением.

-- 1. Удалить indexes (если были)
DROP INDEX IF EXISTS idx_albums_rules_preset_id;

-- 2. Удалить колонку
ALTER TABLE albums DROP COLUMN IF EXISTS rules_preset_id;
