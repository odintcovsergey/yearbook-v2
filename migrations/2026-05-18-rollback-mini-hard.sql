-- Rollback миграции 2026-05-18-presets-density-sheet-type-values.sql
-- в части INSERT mini-hard.
--
-- Контекст: Сергей разъяснил 18.05.2026, что разница hard vs soft —
-- это атомарная настройка, а не отдельный пресет. Один пресет должен
-- покрывать обе версии (плотные/мягкие), а на первой/последней
-- странице мастер выбирается в зависимости от значения sheet_type.
--
-- Поэтому 'mini-hard' создавать было не нужно — Мини уже есть в виде
-- 'mini-soft', и его правила в РЭ.20.6 будут выбирать S-Intro/S-Final
-- только если sheet_type='soft', иначе ставить учительский разворот
-- сразу с первой страницы.
--
-- На следующем этапе (РЭ.12 — UI редактор пресетов / альбома):
-- sheet_type переедет на уровень албома (не пресета) — это позволит
-- партнёру выбрать плотные/мягкие для каждого конкретного альбома без
-- создания нового пресета.
--
-- Удаляем строго запись 'mini-hard'. Все альбомы продолжают использовать
-- свои оригинальные preset_id (mini-hard ни на каком альбоме ещё не
-- использовался, т.к. был создан несколько минут назад этим же чатом).
--
-- Защита: DELETE отрабатывает только если на mini-hard нет
-- ссылающихся album_layouts (FK ON DELETE SET NULL уже стоит в схеме
-- из rule-engine-migration.sql, поэтому FK не помешает). Если у тебя
-- уже появился альбом на mini-hard — он просто получит preset_id=NULL.

DELETE FROM presets WHERE id = 'mini-hard';

-- Проверочный запрос после применения:
-- SELECT id, display_name, density, sheet_type FROM presets ORDER BY id;
-- Ожидание: 7 строк (mini-hard исчез), mini-soft остаётся.
